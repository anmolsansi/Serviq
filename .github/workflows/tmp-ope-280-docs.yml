name: Temporary OPE-280 documentation sync

on:
  push:
    branches:
      - ope-280-workforce-oidc-validation

permissions:
  contents: write

jobs:
  docs:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Append ticket documentation and clean helper
        run: |
          python .github/scripts/tmp_append_ope280_docs.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/tmp-ope-280-docs.yml
          git rm .github/scripts/tmp_append_ope280_docs.py
          git add docs/OPE_279_285_IMPLEMENTATION_GUIDE.md docs/SERVIQ_BUILD_GUIDE.md
          git commit -m "docs(ope-280): explain workforce OIDC validation"
          git push origin HEAD:${GITHUB_REF_NAME}
