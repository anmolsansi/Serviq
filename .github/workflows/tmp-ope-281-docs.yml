name: Temporary OPE-281 documentation sync

on:
  push:
    branches:
      - ope-281-upsert-workforce-user

permissions:
  contents: write

jobs:
  docs:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Append ticket documentation and clean helper
        run: |
          python .github/scripts/tmp_append_ope281_docs.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/tmp-ope-281-docs.yml
          git rm .github/scripts/tmp_append_ope281_docs.py
          git add docs/OPE_279_285_IMPLEMENTATION_GUIDE.md docs/SERVIQ_BUILD_GUIDE.md
          git commit -m "docs(ope-281): explain internal workforce user upsert"
          git push origin HEAD:${GITHUB_REF_NAME}
