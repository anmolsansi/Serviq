# Serviq — Complete Remaining Linear-Style Tickets

> **Combined:** 2026-08-22; reconciled 2026-09-12 at `3e1b9aa`.
> **Repository:** https://github.com/anmolsansi/Serviq
> **Linear project:** Serviq
> **Canonical source:** This file contains every remaining ticket in full Linear-style format.
> **Status reconciliation:** 14 implemented records are retained below and excluded from implementation backlog counts. OPE-306/OPE-307 still need Linear closeout; known defects have separate follow-ups. See [audit evidence](SYSTEM_AUDIT_2026-09-12.md).
> **Reference format:** [OPE-286](https://linear.app/openclaw-neutron/issue/OPE-286/v1112-implement-invitation-acceptance-api).

## Inventory

| Source | V1 | V2 | V3 | V4 | Total |
|---|---:|---:|---:|---:|---:|
| Original staged roadmap | 77 | 38 | 41 | 42 | 198 |
| Original audit discoveries | 6 | 0 | 0 | 0 | 6 |
| Previously omitted publisher record V1.3.06A | 1 | 0 | 0 | 0 | 1 |
| New 2026-09-12 audit follow-ups | 10 | 0 | 0 | 0 | 10 |
| **All records in this file** | **94** | **38** | **41** | **42** | **215** |
| Implemented records (subtract) | 14 | 0 | 0 | 0 | 14 |
| **Remaining implementation/backlog records** | **80** | **38** | **41** | **42** | **201** |

Counts are planning records, not effort, product-completion percentages, or live
Linear issue totals. Two implemented records retain tracker-closeout debt.
The live project query returned 17 issues (12 Done, 3 In Progress, 2 In Review).
New audit IDs are local candidates; no new external issues were created. V1.0.29
maps to existing GitHub #205. The 70 unimplemented original V1 phase tickets plus
10 audit follow-ups make the 80 remaining V1 records.

## Intake rules

- One remaining ticket heading is one local candidate; reuse existing issues before creating anything. Implemented records are historical references, not new issues.
- Each staged ticket preserves its original compact requirement under **Frozen source requirements**.
- Tickets marked **FAIL** or **BLOCKED** must not be handed to a builder.
- Resolve every `Needs Architect Decision`, freeze exact contracts and file paths, verify merged dependencies, and rerun intake before implementation.
- V2, V3, and V4 implementation remains blocked behind the relevant phase audit, PRD delta, and architecture/contract delta.
- Recheck Linear before creating an issue so existing issues are not duplicated.
- Create issues by dependency tranche; do not bulk-authorize the entire roadmap.

## Table of contents

### Audit-discovered V1 records (6 implemented + 10 open follow-ups)

- [V1.0.26 — Lock LLM Gateway Dependencies and Make Local Audits Reproducible](#v1026-lock-llm-gateway-dependencies-and-make-local-audits-reproducible)
- [V1.0.27 — Pin Every GitHub Action to an Immutable Commit](#v1027-pin-every-github-action-to-an-immutable-commit)
- [V1.1.15 — Add Live Keycloak Workforce OIDC Integration Coverage](#v1115-add-live-keycloak-workforce-oidc-integration-coverage)
- [V1.3.04A — Freeze and Implement Durable Knowledge Upload Consistency](#v1304a-freeze-and-implement-durable-knowledge-upload-consistency)
- [V1.3.04B — Add Knowledge Upload Quota and Abuse Controls](#v1304b-add-knowledge-upload-quota-and-abuse-controls)
- [V1.9.00 — Establish Frontend Component and Browser Test Harnesses](#v1900-establish-frontend-component-and-browser-test-harnesses)

- [V1.0.28 — Declare portable SQLAlchemy asyncio dependencies](#v1028-declare-portable-sqlalchemy-asyncio-dependencies)
- [V1.0.29 — Enforce main branch quality and security checks](#v1029-enforce-main-branch-quality-and-security-checks)
- [V1.1.16 — Connect workforce sessions to trusted API request context](#v1116-connect-workforce-sessions-to-trusted-api-request-context)
- [V1.3.04C — Bound upload bodies before multipart spooling](#v1304c-bound-upload-bodies-before-multipart-spooling)
- [V1.3.04D — Activate durable upload cleanup reconciliation](#v1304d-activate-durable-upload-cleanup-reconciliation)
- [V1.3.07A — Bound and reconcile knowledge sync raw objects](#v1307a-bound-and-reconcile-knowledge-sync-raw-objects)
- [V1.3.07B — Resolve sitemap registration versus ingestion support](#v1307b-resolve-sitemap-registration-versus-ingestion-support)
- [V1.3.09A — Consume parse events and persist normalized handoff](#v1309a-consume-parse-events-and-persist-normalized-handoff)
- [V1.3.11A — Redact gateway validation errors and bound authenticated requests](#v1311a-redact-gateway-validation-errors-and-bound-authenticated-requests)
- [V1.3.11B — Implement approved semantic embedding transport](#v1311b-implement-approved-semantic-embedding-transport)

### Phase 1 — V1 Production Foundation (70 remaining + 8 implemented records)

- [V1.3.05 — Implement SSRF-safe public knowledge fetch helper — implemented](#v1305-implement-ssrf-safe-public-knowledge-fetch-helper)
- [V1.3.06 — Implement source sync command and durable outbox event](#v1306-implement-source-sync-command-and-durable-outbox-event)
- [V1.3.06A — Publish transactional outbox events to Kafka — implemented](#v1306a-publish-transactional-outbox-events-to-kafka)
- [V1.3.07 — Implement knowledge sync fetch worker](#v1307-implement-knowledge-sync-fetch-worker)
- [V1.3.08 — Implement PDF/Markdown/text normalization parser](#v1308-implement-pdfmarkdowntext-normalization-parser)
- [V1.3.09 — Implement HTML/help-center normalization parser](#v1309-implement-htmlhelp-center-normalization-parser)
- [V1.3.10 — Implement deterministic heading-aware chunker](#v1310-implement-deterministic-heading-aware-chunker)
- [V1.3.11 — Freeze embedding profile ADR and implement embedding adapter](#v1311-freeze-embedding-profile-adr-and-implement-embedding-adapter)
- [V1.3.12 — Add pgvector index migration](#v1312-add-pgvector-index-migration)
- [V1.3.13 — Implement chunk/embed/index worker](#v1313-implement-chunkembedindex-worker)
- [V1.4.01 — Implement tenant-scoped lexical FTS repository](#v1401-implement-tenant-scoped-lexical-fts-repository)
- [V1.4.02 — Implement tenant-scoped pgvector repository](#v1402-implement-tenant-scoped-pgvector-repository)
- [V1.4.03 — Implement deterministic hybrid ranker](#v1403-implement-deterministic-hybrid-ranker)
- [V1.4.04 — Implement Retrieval Contract C-3 service](#v1404-implement-retrieval-contract-c-3-service)
- [V1.4.05 — Implement retrieval debugger API](#v1405-implement-retrieval-debugger-api)
- [V1.5.01 — Create customer/conversation/message/feedback migration](#v1501-create-customerconversationmessagefeedback-migration)
- [V1.5.02 — Implement public support tenant/deployment resolver](#v1502-implement-public-support-tenantdeployment-resolver)
- [V1.5.03 — Implement signed anonymous customer session](#v1503-implement-signed-anonymous-customer-session)
- [V1.5.04 — Implement customer conversation create API](#v1504-implement-customer-conversation-create-api)
- [V1.5.05 — Implement customer message append + C-2 outbox event](#v1505-implement-customer-message-append-c-2-outbox-event)
- [V1.5.06 — Implement customer conversation history API](#v1506-implement-customer-conversation-history-api)
- [V1.5.07 — Implement authenticated customer SSE stream](#v1507-implement-authenticated-customer-sse-stream)
- [V1.5.08 — Implement customer human-request and feedback APIs](#v1508-implement-customer-human-request-and-feedback-apis)
- [V1.6.01 — Create agent run/step/retrieval persistence migration](#v1601-create-agent-runstepretrieval-persistence-migration)
- [V1.6.02 — Consume C-2 and create agent run idempotently](#v1602-consume-c-2-and-create-agent-run-idempotently)
- [V1.6.03 — Implement central agent budget tracker](#v1603-implement-central-agent-budget-tracker)
- [V1.6.04 — Implement strict request classifier](#v1604-implement-strict-request-classifier)
- [V1.6.05 — Implement budgeted retrieval agent step](#v1605-implement-budgeted-retrieval-agent-step)
- [V1.6.06 — Implement budgeted generation agent step](#v1606-implement-budgeted-generation-agent-step)
- [V1.6.07 — Implement ordered provider fallback router](#v1607-implement-ordered-provider-fallback-router)
- [V1.6.08 — Implement grounded output guardrail](#v1608-implement-grounded-output-guardrail)
- [V1.6.09 — Implement end-to-end knowledge-question state path](#v1609-implement-end-to-end-knowledge-question-state-path)
- [V1.7.01 — Create tool/policy/execution/confirmation/approval migration](#v1701-create-toolpolicyexecutionconfirmationapproval-migration)
- [V1.7.02 — Implement typed tool registry + JSON Schema validation](#v1702-implement-typed-tool-registry-json-schema-validation)
- [V1.7.03 — Implement policy engine C-6](#v1703-implement-policy-engine-c-6)
- [V1.7.04 — Implement selected synthetic read-only demo tool](#v1704-implement-selected-synthetic-read-only-demo-tool)
- [V1.7.05 — Implement selected synthetic protected mutation tool](#v1705-implement-selected-synthetic-protected-mutation-tool)
- [V1.7.06 — Implement customer confirmation lifecycle](#v1706-implement-customer-confirmation-lifecycle)
- [V1.7.07 — Implement human approval lifecycle and C-7 event](#v1707-implement-human-approval-lifecycle-and-c-7-event)
- [V1.7.08 — Implement policy-enforced tool execution coordinator](#v1708-implement-policy-enforced-tool-execution-coordinator)
- [V1.7.09 — Implement transactional agent state path](#v1709-implement-transactional-agent-state-path)
- [V1.8.01 — Create support queue/escalation/internal-note migration](#v1801-create-support-queueescalationinternal-note-migration)
- [V1.8.02 — Create default support queue for new tenant](#v1802-create-default-support-queue-for-new-tenant)
- [V1.8.03 — Implement escalation service C-8](#v1803-implement-escalation-service-c-8)
- [V1.8.04 — Implement support queue/escalation list/detail/assign APIs](#v1804-implement-support-queueescalation-listdetailassign-apis)
- [V1.8.05 — Implement human message/internal note/resolve/reopen APIs](#v1805-implement-human-messageinternal-noteresolvereopen-apis)
- [V1.9.01 — Build shared Serviq design tokens and base UI primitives](#v1901-build-shared-serviq-design-tokens-and-base-ui-primitives)
- [V1.9.02 — Build authenticated client-console application shell](#v1902-build-authenticated-client-console-application-shell)
- [V1.9.03 — Build organization onboarding checklist UI](#v1903-build-organization-onboarding-checklist-ui)
- [V1.9.04 — Build provider/model management UI](#v1904-build-providermodel-management-ui)
- [V1.9.05 — Build knowledge source management UI](#v1905-build-knowledge-source-management-ui)
- [V1.9.06 — Build retrieval debugger UI](#v1906-build-retrieval-debugger-ui)
- [V1.9.07 — Build customer support chat shell and history](#v1907-build-customer-support-chat-shell-and-history)
- [V1.9.08 — Add customer SSE streaming, citations, confirmation, approval cards](#v1908-add-customer-sse-streaming-citations-confirmation-approval-cards)
- [V1.9.09 — Build client conversation review list/detail](#v1909-build-client-conversation-review-listdetail)
- [V1.9.10 — Build human support inbox and case workspace](#v1910-build-human-support-inbox-and-case-workspace)
- [V1.9.11 — Build agent configuration/version/publish UI](#v1911-build-agent-configurationversionpublish-ui)
- [V1.9.12 — Build team/access management UI](#v1912-build-teamaccess-management-ui)
- [V1.10.01 — Create usage/audit/platform-control/privacy migration](#v11001-create-usageauditplatform-controlprivacy-migration)
- [V1.10.02 — Implement immutable audit writer + query API](#v11002-implement-immutable-audit-writer-query-api)
- [V1.10.03 — Implement usage event ingestion + overview analytics API](#v11003-implement-usage-event-ingestion-overview-analytics-api)
- [V1.10.04 — Build analytics and audit client-console screens](#v11004-build-analytics-and-audit-client-console-screens)
- [V1.10.05 — Implement platform feature-flag/rate-policy APIs and Valkey cache](#v11005-implement-platform-feature-flagrate-policy-apis-and-valkey-cache)
- [V1.10.06 — Implement privacy export request/job](#v11006-implement-privacy-export-requestjob)
- [V1.10.07 — Implement privacy deletion/pseudonymization job](#v11007-implement-privacy-deletionpseudonymization-job)
- [V1.10.08 — Build platform-operator console V1 screens](#v11008-build-platform-operator-console-v1-screens)
- [V1.10.09 — Implement dead-letter list/replay platform APIs](#v11009-implement-dead-letter-listreplay-platform-apis)
- [V1.10.10 — Implement retention cleanup scheduler](#v11010-implement-retention-cleanup-scheduler)
- [V1.11.01 — Add end-to-end OpenTelemetry trace propagation](#v11101-add-end-to-end-opentelemetry-trace-propagation)
- [V1.11.02 — Add structured JSON logging + secret/PII redaction](#v11102-add-structured-json-logging-secretpii-redaction)
- [V1.11.03 — Add required V1 10-scenario E2E suite](#v11103-add-required-v1-10-scenario-e2e-suite)
- [V1.11.04 — Add adversarial tenant-isolation API suite](#v11104-add-adversarial-tenant-isolation-api-suite)
- [V1.11.05 — Add baseline k6 REST/SSE load scenarios](#v11105-add-baseline-k6-restsse-load-scenarios)
- [V1.11.06 — Implement outbound webhook HMAC delivery with SSRF protections](#v11106-implement-outbound-webhook-hmac-delivery-with-ssrf-protections)
- [V1.11.07 — Add release security/quality CI gate](#v11107-add-release-securityquality-ci-gate)
- [V1.11.08 — Publish V1 benchmark evidence report](#v11108-publish-v1-benchmark-evidence-report)
- [V1.11.09 — Complete V1 public-demo README and non-affiliation disclosure](#v11109-complete-v1-public-demo-readme-and-non-affiliation-disclosure)
- [V1.11.10 — Run V1 release readiness review and create fix-only backlog](#v11110-run-v1-release-readiness-review-and-create-fix-only-backlog)

### Phase 2 — V2 Integrations & Omnichannel Scale (38)

- [V2.0.01 — Run post-V1 repository audit for V2](#v2001-run-post-v1-repository-audit-for-v2)
- [V2.0.02 — Write V2 PRD delta](#v2002-write-v2-prd-delta)
- [V2.0.03 — Write V2 architecture/CCR delta](#v2003-write-v2-architectureccr-delta)
- [V2.1.01 — Create Shopify development-store integration metadata migration](#v2101-create-shopify-development-store-integration-metadata-migration)
- [V2.1.02 — Implement Shopify secret/auth adapter](#v2102-implement-shopify-secretauth-adapter)
- [V2.1.03 — Implement Shopify connection create/test/disconnect APIs](#v2103-implement-shopify-connection-createtestdisconnect-apis)
- [V2.1.04 — Implement Shopify customer/order read adapter](#v2104-implement-shopify-customerorder-read-adapter)
- [V2.1.05 — Implement Shopify cancel-order action adapter](#v2105-implement-shopify-cancel-order-action-adapter)
- [V2.1.06 — Implement Shopify refund/request action adapter](#v2106-implement-shopify-refundrequest-action-adapter)
- [V2.1.07 — Add Shopify integration UI](#v2107-add-shopify-integration-ui)
- [V2.1.08 — Add Shopify V2 E2E scenarios](#v2108-add-shopify-v2-e2e-scenarios)
- [V2.2.01 — Freeze generic help-desk/CRM connector interface](#v2201-freeze-generic-help-deskcrm-connector-interface)
- [V2.2.02 — Create generic integration registry and credential metadata schema](#v2202-create-generic-integration-registry-and-credential-metadata-schema)
- [V2.2.03 — Implement connector health/test framework](#v2203-implement-connector-healthtest-framework)
- [V2.2.04 — Implement Zendesk-style ticket adapter against sandbox/dev account](#v2204-implement-zendesk-style-ticket-adapter-against-sandboxdev-account)
- [V2.2.05 — Implement external ticket handoff mapping](#v2205-implement-external-ticket-handoff-mapping)
- [V2.2.06 — Build integration management UI](#v2206-build-integration-management-ui)
- [V2.3.01 — Freeze omnichannel inbound/outbound message contract](#v2301-freeze-omnichannel-inboundoutbound-message-contract)
- [V2.3.02 — Add channel connection metadata migration](#v2302-add-channel-connection-metadata-migration)
- [V2.3.03 — Implement inbound email adapter using test mailbox/provider](#v2303-implement-inbound-email-adapter-using-test-mailboxprovider)
- [V2.3.04 — Implement outbound email response adapter](#v2304-implement-outbound-email-response-adapter)
- [V2.3.05 — Implement SMS-like channel adapter against sandbox provider](#v2305-implement-sms-like-channel-adapter-against-sandbox-provider)
- [V2.3.06 — Implement WhatsApp-like channel adapter against sandbox provider](#v2306-implement-whatsapp-like-channel-adapter-against-sandbox-provider)
- [V2.3.07 — Add omnichannel conversation indicators to support inbox](#v2307-add-omnichannel-conversation-indicators-to-support-inbox)
- [V2.4.01 — Add exact response cache layer](#v2401-add-exact-response-cache-layer)
- [V2.4.02 — Add semantic cache candidate lookup](#v2402-add-semantic-cache-candidate-lookup)
- [V2.4.03 — Add semantic cache safety verifier](#v2403-add-semantic-cache-safety-verifier)
- [V2.4.04 — Implement capability-aware model router](#v2404-implement-capability-aware-model-router)
- [V2.4.05 — Add shadow evaluation mode for candidate model/config](#v2405-add-shadow-evaluation-mode-for-candidate-modelconfig)
- [V2.4.06 — Build evaluation comparison UI](#v2406-build-evaluation-comparison-ui)
- [V2.5.01 — Containerize API/worker/gateway with production images](#v2501-containerize-apiworkergateway-with-production-images)
- [V2.5.02 — Add local multi-replica load profile](#v2502-add-local-multi-replica-load-profile)
- [V2.5.03 — Add clustered Valkey test profile](#v2503-add-clustered-valkey-test-profile)
- [V2.5.04 — Add Kafka partitioning/consumer-group scale tests](#v2504-add-kafka-partitioningconsumer-group-scale-tests)
- [V2.5.05 — Create first AWS single-region Terraform foundation](#v2505-create-first-aws-single-region-terraform-foundation)
- [V2.5.06 — Deploy V2 staging in one AWS region](#v2506-deploy-v2-staging-in-one-aws-region)
- [V2.5.07 — Add horizontal autoscaling policy and test](#v2507-add-horizontal-autoscaling-policy-and-test)
- [V2.5.08 — Publish V2 single-region benchmark report](#v2508-publish-v2-single-region-benchmark-report)

### Phase 3 — V3 Autonomous Enterprise Operations (41)

- [V3.0.01 — Run post-V2 repository audit](#v3001-run-post-v2-repository-audit)
- [V3.0.02 — Write V3 PRD delta](#v3002-write-v3-prd-delta)
- [V3.0.03 — Write V3 architecture/contract delta](#v3003-write-v3-architecturecontract-delta)
- [V3.1.01 — Create proactive workflow definition/version schema](#v3101-create-proactive-workflow-definitionversion-schema)
- [V3.1.02 — Implement proactive trigger evaluator](#v3102-implement-proactive-trigger-evaluator)
- [V3.1.03 — Implement proactive workflow run persistence](#v3103-implement-proactive-workflow-run-persistence)
- [V3.1.04 — Implement workflow step executor shell](#v3104-implement-workflow-step-executor-shell)
- [V3.1.05 — Implement proactive outbound notification gate](#v3105-implement-proactive-outbound-notification-gate)
- [V3.1.06 — Add proactive workflow client UI](#v3106-add-proactive-workflow-client-ui)
- [V3.1.07 — Add proactive workflow E2E suite](#v3107-add-proactive-workflow-e2e-suite)
- [V3.2.01 — Freeze specialist-agent registry contract](#v3201-freeze-specialist-agent-registry-contract)
- [V3.2.02 — Add specialist-agent configuration schema](#v3202-add-specialist-agent-configuration-schema)
- [V3.2.03 — Implement agent-to-agent handoff contract](#v3203-implement-agent-to-agent-handoff-contract)
- [V3.2.04 — Implement child-run budget inheritance](#v3204-implement-child-run-budget-inheritance)
- [V3.2.05 — Implement parallel read-only specialist execution](#v3205-implement-parallel-read-only-specialist-execution)
- [V3.2.06 — Implement multi-agent trace visualization](#v3206-implement-multi-agent-trace-visualization)
- [V3.2.07 — Add multi-agent regression/evaluation suite](#v3207-add-multi-agent-regressionevaluation-suite)
- [V3.3.01 — Add tenant locale/language configuration schema](#v3301-add-tenant-localelanguage-configuration-schema)
- [V3.3.02 — Implement language detection contract](#v3302-implement-language-detection-contract)
- [V3.3.03 — Add multilingual knowledge ingestion metadata](#v3303-add-multilingual-knowledge-ingestion-metadata)
- [V3.3.04 — Implement language-aware retrieval](#v3304-implement-language-aware-retrieval)
- [V3.3.05 — Implement localized response generation guard](#v3305-implement-localized-response-generation-guard)
- [V3.3.06 — Internationalize customer/client UI framework](#v3306-internationalize-customerclient-ui-framework)
- [V3.3.07 — Add multilingual E2E/evaluation suite](#v3307-add-multilingual-e2eevaluation-suite)
- [V3.4.01 — Add tenant isolation mode configuration](#v3401-add-tenant-isolation-mode-configuration)
- [V3.4.02 — Implement tenant data-source routing interface](#v3402-implement-tenant-data-source-routing-interface)
- [V3.4.03 — Implement dedicated worker-pool routing](#v3403-implement-dedicated-worker-pool-routing)
- [V3.4.04 — Add per-tenant encryption-key reference support](#v3404-add-per-tenant-encryption-key-reference-support)
- [V3.4.05 — Implement enterprise data-retention overrides within platform bounds](#v3405-implement-enterprise-data-retention-overrides-within-platform-bounds)
- [V3.4.06 — Build enterprise isolation controls in platform console](#v3406-build-enterprise-isolation-controls-in-platform-console)
- [V3.4.07 — Add enterprise isolation adversarial test suite](#v3407-add-enterprise-isolation-adversarial-test-suite)
- [V3.5.01 — Create quality-review sample schema](#v3501-create-quality-review-sample-schema)
- [V3.5.02 — Implement deterministic conversation sampling job](#v3502-implement-deterministic-conversation-sampling-job)
- [V3.5.03 — Implement AI quality evaluator with strict rubric output](#v3503-implement-ai-quality-evaluator-with-strict-rubric-output)
- [V3.5.04 — Implement human QA review API](#v3504-implement-human-qa-review-api)
- [V3.5.05 — Build QA review workspace](#v3505-build-qa-review-workspace)
- [V3.5.06 — Implement support-agent coaching summary job](#v3506-implement-support-agent-coaching-summary-job)
- [V3.5.07 — Extract retrieval service behind stable network contract](#v3507-extract-retrieval-service-behind-stable-network-contract)
- [V3.5.08 — Extract tool execution service behind stable network contract](#v3508-extract-tool-execution-service-behind-stable-network-contract)
- [V3.5.09 — Introduce partition-key routing for high-volume tenant data](#v3509-introduce-partition-key-routing-for-high-volume-tenant-data)
- [V3.5.10 — Publish V3 partitioned single-region benchmark](#v3510-publish-v3-partitioned-single-region-benchmark)

### Phase 4 — V4 Hyperscale & Multi-Region (42)

- [V4.0.01 — Run post-V3 hyperscale repository/infrastructure audit](#v4001-run-post-v3-hyperscale-repositoryinfrastructure-audit)
- [V4.0.02 — Write V4 scale PRD with explicit capacity definitions](#v4002-write-v4-scale-prd-with-explicit-capacity-definitions)
- [V4.0.03 — Write multi-region architecture and data-placement contracts](#v4003-write-multi-region-architecture-and-data-placement-contracts)
- [V4.1.01 — Add tenant region/placement metadata migration](#v4101-add-tenant-regionplacement-metadata-migration)
- [V4.1.02 — Implement region-aware tenant placement resolver](#v4102-implement-region-aware-tenant-placement-resolver)
- [V4.1.03 — Add signed global routing token contract](#v4103-add-signed-global-routing-token-contract)
- [V4.1.04 — Implement region-aware API ingress middleware](#v4104-implement-region-aware-api-ingress-middleware)
- [V4.1.05 — Implement region-aware customer SSE routing](#v4105-implement-region-aware-customer-sse-routing)
- [V4.1.06 — Add regional service-discovery configuration to IaC](#v4106-add-regional-service-discovery-configuration-to-iac)
- [V4.1.07 — Build platform placement view](#v4107-build-platform-placement-view)
- [V4.2.01 — Freeze conversation/database shard key ADR](#v4201-freeze-conversationdatabase-shard-key-adr)
- [V4.2.02 — Implement shard routing library with shadow validation](#v4202-implement-shard-routing-library-with-shadow-validation)
- [V4.2.03 — Add shard-placement metadata and migration tooling](#v4203-add-shard-placement-metadata-and-migration-tooling)
- [V4.2.04 — Implement online tenant shard migration copy phase](#v4204-implement-online-tenant-shard-migration-copy-phase)
- [V4.2.05 — Implement shard migration change-capture/replay phase](#v4205-implement-shard-migration-change-capturereplay-phase)
- [V4.2.06 — Implement shard cutover and rollback switch](#v4206-implement-shard-cutover-and-rollback-switch)
- [V4.2.07 — Add hot-tenant workload isolation](#v4207-add-hot-tenant-workload-isolation)
- [V4.2.08 — Partition Kafka topics/consumer groups for hyperscale load](#v4208-partition-kafka-topicsconsumer-groups-for-hyperscale-load)
- [V4.2.09 — Add distributed SSE connection registry/metrics](#v4209-add-distributed-sse-connection-registrymetrics)
- [V4.2.10 — Implement edge-safe deterministic/semantic cache strategy](#v4210-implement-edge-safe-deterministicsemantic-cache-strategy)
- [V4.3.01 — Define and test regional health/failover decision contract](#v4301-define-and-test-regional-healthfailover-decision-contract)
- [V4.3.02 — Implement regional control-plane configuration replication](#v4302-implement-regional-control-plane-configuration-replication)
- [V4.3.03 — Implement regional knowledge/index replication workflow](#v4303-implement-regional-knowledgeindex-replication-workflow)
- [V4.3.04 — Implement regional conversation failover policy](#v4304-implement-regional-conversation-failover-policy)
- [V4.3.05 — Implement provider-routing by region](#v4305-implement-provider-routing-by-region)
- [V4.3.06 — Add backup/restore automation and deletion replay verification](#v4306-add-backuprestore-automation-and-deletion-replay-verification)
- [V4.3.07 — Add controlled regional failover runbook automation](#v4307-add-controlled-regional-failover-runbook-automation)
- [V4.3.08 — Run scheduled disaster-recovery game day](#v4308-run-scheduled-disaster-recovery-game-day)
- [V4.4.01 — Create distributed load-generator architecture](#v4401-create-distributed-load-generator-architecture)
- [V4.4.02 — Add staged SSE concurrency test: 100k connections](#v4402-add-staged-sse-concurrency-test-100k-connections)
- [V4.4.03 — Add staged SSE concurrency test: 500k connections](#v4403-add-staged-sse-concurrency-test-500k-connections)
- [V4.4.04 — Add staged SSE concurrency test: 1M connections](#v4404-add-staged-sse-concurrency-test-1m-connections)
- [V4.4.05 — Add staged concurrency tests beyond 1M toward 10M](#v4405-add-staged-concurrency-tests-beyond-1m-toward-10m)
- [V4.4.06 — Add REST/API high-throughput cached-path benchmark](#v4406-add-restapi-high-throughput-cached-path-benchmark)
- [V4.4.07 — Add agent-worker throughput benchmark with fake providers](#v4407-add-agent-worker-throughput-benchmark-with-fake-providers)
- [V4.4.08 — Add retrieval hyperscale benchmark](#v4408-add-retrieval-hyperscale-benchmark)
- [V4.4.09 — Add chaos test for API/worker node loss under load](#v4409-add-chaos-test-for-apiworker-node-loss-under-load)
- [V4.4.10 — Add chaos test for cache/broker degradation](#v4410-add-chaos-test-for-cachebroker-degradation)
- [V4.4.11 — Add chaos test for database shard impairment](#v4411-add-chaos-test-for-database-shard-impairment)
- [V4.4.12 — Add cost-per-million-requests/connections model](#v4412-add-cost-per-million-requestsconnections-model)
- [V4.4.13 — Publish V4 multi-region/hyperscale evidence report](#v4413-publish-v4-multi-regionhyperscale-evidence-report)
- [V4.4.14 — Run independent release/security architecture review](#v4414-run-independent-releasesecurity-architecture-review)


## Audit-discovered V1 tickets

<a id="v1026-lock-llm-gateway-dependencies-and-make-local-audits-reproducible"></a>

### V1.0.26 — Lock LLM Gateway Dependencies and Make Local Audits Reproducible

> **Record status:** IMPLEMENTED at audited main `3e1b9aa`; excluded from remaining implementation counts.
> **Tracker snapshot (2026-09-12):** No matching issue in current project query; GitHub #166 and follow-ups closed.
> **Intake:** Historical implementation record; do not re-create as new work. Follow-ups remain open where named.

#### Accepted implementation scope

Frozen dependency installation and audit commands across all three Python services.

#### Source and validation

`Makefile`; `services/api/uv.lock`; `services/worker/uv.lock`; `services/llm-gateway/uv.lock`.

All three uv lock checks and frozen service syncs passed; dependency audits passed. This does not solve the separate arm64 asyncio-extra defect (V1.0.28).

See [current system audit](SYSTEM_AUDIT_2026-09-12.md) for local versus CI evidence,
release limits and tracker closeout. The [Build Guide](SERVIQ_BUILD_GUIDE.md)
retains the behavior, usage, design and rollback explanation. Historical DRAFT
intake checklists have been replaced by this verified implementation record;
unrelated downstream requirements have not been marked complete.

---

<a id="v1027-pin-every-github-action-to-an-immutable-commit"></a>

### V1.0.27 — Pin Every GitHub Action to an Immutable Commit

> **Record status:** IMPLEMENTED at audited main `3e1b9aa`; excluded from remaining implementation counts.
> **Tracker snapshot (2026-09-12):** OPE-306 — In Progress; GitHub #173 closed, PR #174 merged.
> **Intake:** Historical implementation record; do not re-create as new work. Follow-ups remain open where named.

#### Accepted implementation scope

Immutable action references and executable CI policy guard.

#### Source and validation

`.github/workflows/ci.yml`; `.github/workflows/security.yml`; `.github/workflows/release.yml`.

Current-main quality/security jobs passed. Linear closeout remains pending. Repository branch protection is a different open item, GitHub #205 / V1.0.29.

See [current system audit](SYSTEM_AUDIT_2026-09-12.md) for local versus CI evidence,
release limits and tracker closeout. The [Build Guide](SERVIQ_BUILD_GUIDE.md)
retains the behavior, usage, design and rollback explanation. Historical DRAFT
intake checklists have been replaced by this verified implementation record;
unrelated downstream requirements have not been marked complete.

---

<a id="v1115-add-live-keycloak-workforce-oidc-integration-coverage"></a>

### V1.1.15 — Add Live Keycloak Workforce OIDC Integration Coverage

> **Record status:** IMPLEMENTED at audited main `3e1b9aa`; excluded from remaining implementation counts.
> **Tracker snapshot (2026-09-12):** OPE-307 — In Progress; GitHub #175 closed, PR #176 merged.
> **Intake:** Historical implementation record; do not re-create as new work. Follow-ups remain open where named.

#### Accepted implementation scope

Real Keycloak token/discovery/JWKS validation with invalid-issuer/audience and disabled/unknown-user tests.

#### Source and validation

`infra/keycloak/serviq-test-realm.json`; `services/api/tests/integration/test_keycloak_oidc_integration.py`; `.github/workflows/ci.yml`.

Current-main Keycloak job passed six tests. Linear closeout remains pending. This tests the validator directly; HTTP session/principal wiring is missing and belongs to V1.1.16.

See [current system audit](SYSTEM_AUDIT_2026-09-12.md) for local versus CI evidence,
release limits and tracker closeout. The [Build Guide](SERVIQ_BUILD_GUIDE.md)
retains the behavior, usage, design and rollback explanation. Historical DRAFT
intake checklists have been replaced by this verified implementation record;
unrelated downstream requirements have not been marked complete.

---

<a id="v1304a-freeze-and-implement-durable-knowledge-upload-consistency"></a>

### V1.3.04A — Freeze and Implement Durable Knowledge Upload Consistency

> **Record status:** IMPLEMENTED at audited main `3e1b9aa`; excluded from remaining implementation counts.
> **Tracker snapshot (2026-09-12):** OPE-308 — Done; PR #180 merged.
> **Intake:** Historical implementation record; do not re-create as new work. Follow-ups remain open where named.

#### Accepted implementation scope

Pre-PUT cleanup intent, atomic source/reference ownership, bounded durable cleanup replay and protected downgrade.

#### Source and validation

`services/api/app/modules/knowledge/cleanup.py`; `services/api/alembic/versions/20260824_0010_knowledge_upload_cleanups.py`.

Durability service and integration tests exist. No automatic cleanup caller is composed; V1.3.04D owns activation. Preserve unresolved obligations during rollback.

See [current system audit](SYSTEM_AUDIT_2026-09-12.md) for local versus CI evidence,
release limits and tracker closeout. The [Build Guide](SERVIQ_BUILD_GUIDE.md)
retains the behavior, usage, design and rollback explanation. Historical DRAFT
intake checklists have been replaced by this verified implementation record;
unrelated downstream requirements have not been marked complete.

---

<a id="v1304b-add-knowledge-upload-quota-and-abuse-controls"></a>

### V1.3.04B — Add Knowledge Upload Quota and Abuse Controls

> **Record status:** IMPLEMENTED at audited main `3e1b9aa`; excluded from remaining implementation counts.
> **Tracker snapshot (2026-09-12):** OPE-309 — Done; PR #185 merged.
> **Intake:** Historical implementation record; do not re-create as new work. Follow-ups remain open where named.

#### Accepted implementation scope

Tenant file-byte/source/concurrency reservations and a fail-closed Valkey upload-attempt limiter.

#### Source and validation

`services/api/app/modules/knowledge/quota.py`; `services/api/app/core/rate_limits.py`; `services/api/alembic/versions/20260828_0011_knowledge_upload_quotas.py`.

Current unit suite passed; earlier real-stack quota workflow passed. Pre-parser resource admission and URL fetched-byte accounting are separate gaps (V1.3.04C/V1.3.07A).

See [current system audit](SYSTEM_AUDIT_2026-09-12.md) for local versus CI evidence,
release limits and tracker closeout. The [Build Guide](SERVIQ_BUILD_GUIDE.md)
retains the behavior, usage, design and rollback explanation. Historical DRAFT
intake checklists have been replaced by this verified implementation record;
unrelated downstream requirements have not been marked complete.

---

<a id="v1900-establish-frontend-component-and-browser-test-harnesses"></a>

### V1.9.00 — Establish Frontend Component and Browser Test Harnesses

> **Record status:** IMPLEMENTED at audited main `3e1b9aa`; excluded from remaining implementation counts.
> **Tracker snapshot (2026-09-12):** OPE-310 — Done.
> **Intake:** Historical implementation record; do not re-create as new work. Follow-ups remain open where named.

#### Accepted implementation scope

Root Vitest/Testing Library scaffold checks and Playwright configuration.

#### Source and validation

`package.json`; `tests/frontend/app-smoke.test.tsx`; `playwright.config.ts`.

Four smoke/config tests passed. Browser list contains zero tests. V1.11.03 still owns full browser journeys; this record does not claim E2E completion.

See [current system audit](SYSTEM_AUDIT_2026-09-12.md) for local versus CI evidence,
release limits and tracker closeout. The [Build Guide](SERVIQ_BUILD_GUIDE.md)
retains the behavior, usage, design and rollback explanation. Historical DRAFT
intake checklists have been replaced by this verified implementation record;
unrelated downstream requirements have not been marked complete.

---

<a id="v1028-declare-portable-sqlalchemy-asyncio-dependencies"></a>

### V1.0.28 — Declare portable SQLAlchemy asyncio dependencies

> **Record status:** BACKLOG — audit follow-up, 2026-09-12.
> **Priority:** P1.
> **Tracker:** Local candidate; search current Linear/GitHub before issue creation.
> **Ticket intake:** Ready for focused implementation after dependency diff review; no architecture change required.

#### Goal and why

Frozen macOS arm64 environments omit greenlet and API AsyncSession cleanup returns a 500 instead of the intended authentication response.

#### Exact scope and intended behavior

Use the supported SQLAlchemy asyncio dependency surface in both services and refresh only affected lock entries. Add a session open/close smoke test without a database so Linux-only integration cannot hide optional dependency omissions.

#### Dependencies and contracts

None; prerequisite to local API/worker runtime validation.

Ready for focused implementation after dependency diff review; no architecture change required. Any new public API, database, event, error, resource-limit or
cross-service contract must be captured in an ADR/CCR and exact file allowlist
before coding. This audit does not approve unspecified contract changes.

#### Files to inspect first

`services/api/pyproject.toml`; `services/api/uv.lock`; `services/worker/pyproject.toml`; `services/worker/uv.lock`; `services/api/tests/test_database.py`; `services/worker/tests/test_database.py`.

The edit allowlist is limited to the inspected boundary and its focused tests
once intake resolves any new paths. Update this inventory, `repo_context.md`
and the relevant Build Guide section with final behavior and evidence.

#### Required validation and manual acceptance

Frozen installs on macOS arm64 and Linux; async session open/close succeeds; API unauthorized response is no longer masked by greenlet failure; locks, Ruff, mypy and service suites pass.

Record commands, environment, expected and actual outcome, and current commit.
Infrastructure skips and mocked boundaries must be labeled. Use synthetic data
and bounded error codes; never retain raw tokens, credentials, documents or
provider responses in evidence. Run affected Ruff/mypy/pytest or web quality
checks and applicable real integration gates. Review tenant isolation, duplicate
execution, partial failure and cancellation at the changed boundary.

#### Out of scope and stop conditions

Keep SQLAlchemy/Psycopg public contracts and versions within existing constraints. No schema or feature change.

Stop implementation of any unresolved contract and return the exact missing
architect decision. Preserve unrelated work and existing accepted contracts.

#### Definition of done

- [ ] Intake resolves exact contracts, dependencies, allowed edits and owner.
- [ ] The stated behavior and focused failure/replay/security tests pass.
- [ ] Required integration/manual evidence is attached; no skipped check is counted as a pass.
- [ ] Deployment sequence and rollback preserve pending state and secrets; demonstrate recovery where state changes.
- [ ] Safe observability and operational ownership are defined for runtime changes.
- [ ] Documentation describes what/why/how/where/usage/verification and remaining limits.
- [ ] Reviewed diff and applicable CI pass; existing tracker is reconciled only after acceptance.

---

<a id="v1029-enforce-main-branch-quality-and-security-checks"></a>

### V1.0.29 — Enforce main branch quality and security checks

> **Record status:** BACKLOG — audit follow-up, 2026-09-12.
> **Priority:** P1.
> **Tracker:** Reuse GitHub #205; no duplicate issue.
> **Ticket intake:** Needs Architect Decision: required contexts, conditional integration gate design and administrator bypass policy.

#### Goal and why

GitHub reports main unprotected and an empty ruleset list. Existing GitHub #205 already represents this work.

#### Exact scope and intended behavior

Reuse https://github.com/anmolsansi/Serviq/issues/205. Configure required PR/check enforcement and force-push restrictions. Use exact job contexts; account for path-filtered workflows so unrelated changes can merge safely.

#### Dependencies and contracts

Repository administration; approved emergency/bypass policy.

Needs Architect Decision: required contexts, conditional integration gate design and administrator bypass policy. Any new public API, database, event, error, resource-limit or
cross-service contract must be captured in an ADR/CCR and exact file allowlist
before coding. This audit does not approve unspecified contract changes.

#### Files to inspect first

`.github/workflows/ci.yml`; `.github/workflows/security.yml`; `.github/workflows/knowledge-sync-integration.yml`; `.github/workflows/outbox-publisher-integration.yml`; `.github/workflows/knowledge-quota-integration.yml`.

The edit allowlist is limited to the inspected boundary and its focused tests
once intake resolves any new paths. Update this inventory, `repo_context.md`
and the relevant Build Guide section with final behavior and evidence.

#### Required validation and manual acceptance

Read back protection/rulesets; demonstrate failing required checks block merge and relevant passing checks permit it; verify unrelated documentation PRs are not stuck on never-created path-filtered jobs.

Record commands, environment, expected and actual outcome, and current commit.
Infrastructure skips and mocked boundaries must be labeled. Use synthetic data
and bounded error codes; never retain raw tokens, credentials, documents or
provider responses in evidence. Run affected Ruff/mypy/pytest or web quality
checks and applicable real integration gates. Review tenant isolation, duplicate
execution, partial failure and cancellation at the changed boundary.

#### Out of scope and stop conditions

Do not manufacture another issue, bypass checks, or change runtime contracts.

Stop implementation of any unresolved contract and return the exact missing
architect decision. Preserve unrelated work and existing accepted contracts.

#### Definition of done

- [ ] Intake resolves exact contracts, dependencies, allowed edits and owner.
- [ ] The stated behavior and focused failure/replay/security tests pass.
- [ ] Required integration/manual evidence is attached; no skipped check is counted as a pass.
- [ ] Deployment sequence and rollback preserve pending state and secrets; demonstrate recovery where state changes.
- [ ] Safe observability and operational ownership are defined for runtime changes.
- [ ] Documentation describes what/why/how/where/usage/verification and remaining limits.
- [ ] Reviewed diff and applicable CI pass; existing tracker is reconciled only after acceptance.

---

<a id="v1116-connect-workforce-sessions-to-trusted-api-request-context"></a>

### V1.1.16 — Connect workforce sessions to trusted API request context

> **Record status:** BACKLOG — audit follow-up, 2026-09-12.
> **Priority:** P1.
> **Tracker:** Local candidate; search current Linear/GitHub before issue creation.
> **Ticket intake:** Needs Architect Decision: session persistence, exact routes, cookies/CSRF, logout and active-tenant selection contract.

#### Goal and why

The API reads trusted request state that no production entrypoint populates. Validator-only Keycloak tests and overridden principals conceal this missing connection.

#### Exact scope and intended behavior

Connect architecture-owned PKCE/server session identity to workforce user and active membership resolution. Populate trusted context only after verification. Define login/callback/logout/tenant switching and cookie/CSRF/session-expiry behavior before implementation.

#### Dependencies and contracts

V1.0.28 for portable runtime; existing OIDC/upsert/membership services. V1.9.02 depends on this API/session contract.

Needs Architect Decision: session persistence, exact routes, cookies/CSRF, logout and active-tenant selection contract. Any new public API, database, event, error, resource-limit or
cross-service contract must be captured in an ADR/CCR and exact file allowlist
before coding. This audit does not approve unspecified contract changes.

#### Files to inspect first

`services/api/app/main.py`; `services/api/app/core/auth.py`; `services/api/app/core/principal.py`; `services/api/app/core/tenancy.py`; `services/api/app/modules/workforce/service.py`; `services/api/app/modules/tenancy/service.py`; `services/api/tests/integration/test_keycloak_oidc_integration.py`.

The edit allowlist is limited to the inspected boundary and its focused tests
once intake resolves any new paths. Update this inventory, `repo_context.md`
and the relevant Build Guide section with final behavior and evidence.

#### Required validation and manual acceptance

Real issued identity reaches organization/provider/knowledge HTTP APIs without principal overrides; missing/expired/wrong issuer/disabled/foreign-tenant paths fail safely; concurrent initial provisioning and tenant switching preserve isolation.

Record commands, environment, expected and actual outcome, and current commit.
Infrastructure skips and mocked boundaries must be labeled. Use synthetic data
and bounded error codes; never retain raw tokens, credentials, documents or
provider responses in evidence. Run affected Ruff/mypy/pytest or web quality
checks and applicable real integration gates. Review tenant isolation, duplicate
execution, partial failure and cancellation at the changed boundary.

#### Out of scope and stop conditions

No browser token storage, client-supplied permissions, test-only identity header, customer auth or platform-operator shortcut.

Stop implementation of any unresolved contract and return the exact missing
architect decision. Preserve unrelated work and existing accepted contracts.

#### Definition of done

- [ ] Intake resolves exact contracts, dependencies, allowed edits and owner.
- [ ] The stated behavior and focused failure/replay/security tests pass.
- [ ] Required integration/manual evidence is attached; no skipped check is counted as a pass.
- [ ] Deployment sequence and rollback preserve pending state and secrets; demonstrate recovery where state changes.
- [ ] Safe observability and operational ownership are defined for runtime changes.
- [ ] Documentation describes what/why/how/where/usage/verification and remaining limits.
- [ ] Reviewed diff and applicable CI pass; existing tracker is reconciled only after acceptance.

---

<a id="v1304c-bound-upload-bodies-before-multipart-spooling"></a>

### V1.3.04C — Bound upload bodies before multipart spooling

> **Record status:** BACKLOG — audit follow-up, 2026-09-12.
> **Priority:** P1.
> **Tracker:** Local candidate; search current Linear/GitHub before issue creation.
> **Ticket intake:** Needs Architect Decision: total request/overhead cap, pre-body admission lease ownership and error precedence.

#### Goal and why

File validation and durable concurrency reservation occur after request.form has consumed and spooled the body. max_part_size limits non-file parts, not file bytes.

#### Exact scope and intended behavior

Implement byte-counting/admission before unbounded spooling. Cover absent or dishonest Content-Length and multipart overhead. Preserve existing per-type 5/25 MiB limits, safe quota failures and cleanup invariants; close form/files on every outcome.

#### Dependencies and contracts

V1.1.16 trust context; existing V1.3.04A/B accounting.

Needs Architect Decision: total request/overhead cap, pre-body admission lease ownership and error precedence. Any new public API, database, event, error, resource-limit or
cross-service contract must be captured in an ADR/CCR and exact file allowlist
before coding. This audit does not approve unspecified contract changes.

#### Files to inspect first

`services/api/app/modules/knowledge/router.py`; `services/api/app/modules/knowledge/uploads.py`; `services/api/app/modules/knowledge/quota.py`; `services/api/tests/integration/test_knowledge_file_upload_api.py`; `services/api/tests/integration/test_knowledge_quota_api.py`.

The edit allowlist is limited to the inspected boundary and its focused tests
once intake resolves any new paths. Update this inventory, `repo_context.md`
and the relevant Build Guide section with final behavior and evidence.

#### Required validation and manual acceptance

Streamed over-limit request aborts within bounded bytes; concurrent slow uploads cannot exceed admitted capacity; 413/429/503 behavior is stable; permission-denied/invalid/cancelled requests release resources and never PUT an object.

Record commands, environment, expected and actual outcome, and current commit.
Infrastructure skips and mocked boundaries must be labeled. Use synthetic data
and bounded error codes; never retain raw tokens, credentials, documents or
provider responses in evidence. Run affected Ruff/mypy/pytest or web quality
checks and applicable real integration gates. Review tenant isolation, duplicate
execution, partial failure and cancellation at the changed boundary.

#### Out of scope and stop conditions

No limit increase or quota bypass; no reliance on Content-Length alone.

Stop implementation of any unresolved contract and return the exact missing
architect decision. Preserve unrelated work and existing accepted contracts.

#### Definition of done

- [ ] Intake resolves exact contracts, dependencies, allowed edits and owner.
- [ ] The stated behavior and focused failure/replay/security tests pass.
- [ ] Required integration/manual evidence is attached; no skipped check is counted as a pass.
- [ ] Deployment sequence and rollback preserve pending state and secrets; demonstrate recovery where state changes.
- [ ] Safe observability and operational ownership are defined for runtime changes.
- [ ] Documentation describes what/why/how/where/usage/verification and remaining limits.
- [ ] Reviewed diff and applicable CI pass; existing tracker is reconciled only after acceptance.

---

<a id="v1304d-activate-durable-upload-cleanup-reconciliation"></a>

### V1.3.04D — Activate durable upload cleanup reconciliation

> **Record status:** BACKLOG — audit follow-up, 2026-09-12.
> **Priority:** P1.
> **Tracker:** Local candidate; search current Linear/GitHub before issue creation.
> **Ticket intake:** Needs Architect Decision: packaging/runtime ownership, cadence and operational alert owner; coordinate V1.10.09/10.

#### Goal and why

reconcile_due_upload_cleanups exists but no production process schedules it. Failed uploads may retain raw objects and quota obligations indefinitely without operator invocation.

#### Exact scope and intended behavior

Freeze runtime ownership and schedule the existing bounded sweep without copying its business rules. Preserve replay leases, validate object identifiers exactly, and retain retry/exhaustion state and reservation release. Add restart behavior and bounded operator visibility.

#### Dependencies and contracts

Existing V1.3.04A/B contracts; V1.0.28 async runtime.

Needs Architect Decision: packaging/runtime ownership, cadence and operational alert owner; coordinate V1.10.09/10. Any new public API, database, event, error, resource-limit or
cross-service contract must be captured in an ADR/CCR and exact file allowlist
before coding. This audit does not approve unspecified contract changes.

#### Files to inspect first

`services/api/app/modules/knowledge/cleanup.py`; `services/api/app/modules/knowledge/repository.py`; `services/api/app/modules/knowledge/quota.py`; `services/worker/app/main.py`; `services/api/tests/integration/test_knowledge_upload_cleanup.py`; `docs/KNOWLEDGE_UPLOAD_CONSISTENCY_RUNBOOK.md`.

The edit allowlist is limited to the inspected boundary and its focused tests
once intake resolves any new paths. Update this inventory, `repo_context.md`
and the relevant Build Guide section with final behavior and evidence.

#### Required validation and manual acceptance

Real DB/S3 double failure then restart/recovery deletes orphan and releases quota; duplicate scheduler workers do not cross tenant boundaries; exhaustion remains visible; shutdown and cadence are tested.

Record commands, environment, expected and actual outcome, and current commit.
Infrastructure skips and mocked boundaries must be labeled. Use synthetic data
and bounded error codes; never retain raw tokens, credentials, documents or
provider responses in evidence. Run affected Ruff/mypy/pytest or web quality
checks and applicable real integration gates. Review tenant isolation, duplicate
execution, partial failure and cancellation at the changed boundary.

#### Out of scope and stop conditions

No public unauthenticated cleanup endpoint, blanket object deletion or unresolved-state purge.

Stop implementation of any unresolved contract and return the exact missing
architect decision. Preserve unrelated work and existing accepted contracts.

#### Definition of done

- [ ] Intake resolves exact contracts, dependencies, allowed edits and owner.
- [ ] The stated behavior and focused failure/replay/security tests pass.
- [ ] Required integration/manual evidence is attached; no skipped check is counted as a pass.
- [ ] Deployment sequence and rollback preserve pending state and secrets; demonstrate recovery where state changes.
- [ ] Safe observability and operational ownership are defined for runtime changes.
- [ ] Documentation describes what/why/how/where/usage/verification and remaining limits.
- [ ] Reviewed diff and applicable CI pass; existing tracker is reconciled only after acceptance.

---

<a id="v1307a-bound-and-reconcile-knowledge-sync-raw-objects"></a>

### V1.3.07A — Bound and reconcile knowledge sync raw objects

> **Record status:** BACKLOG — audit follow-up, 2026-09-12.
> **Priority:** P1.
> **Tracker:** Local candidate; search current Linear/GitHub before issue creation.
> **Ticket intake:** Needs Architect Decision: retention, quota source of truth, object immutability/write strategy and ownership of migration/reconciliation.

#### Goal and why

URL versions bypass file-byte quota and durable upload cleanup. PUT precedes final version lock; stale/crashed jobs can retain raw objects, and competing same-version fetches require immutable-write evidence. S3 reads are unbounded.

#### Exact scope and intended behavior

Freeze fetched-version byte accounting, bounded object reads, raw-object identity/immutability and retention/recovery semantics. Account for DB failure after PUT and stale source version before persistence. Reuse durability patterns after ownership is explicit.

#### Dependencies and contracts

ADR-023; existing upload accounting/cleanup primitives; coordinate V1.3.09A and V1.10.10.

Needs Architect Decision: retention, quota source of truth, object immutability/write strategy and ownership of migration/reconciliation. Any new public API, database, event, error, resource-limit or
cross-service contract must be captured in an ADR/CCR and exact file allowlist
before coding. This audit does not approve unspecified contract changes.

#### Files to inspect first

`services/worker/app/jobs/knowledge_sync.py`; `services/worker/app/core/object_storage.py`; `services/worker/tests/integration/test_knowledge_sync_postgres.py`; `services/worker/tests/integration/test_knowledge_sync_end_to_end.py`; `services/api/app/modules/knowledge/quota.py`.

The edit allowlist is limited to the inspected boundary and its focused tests
once intake resolves any new paths. Update this inventory, `repo_context.md`
and the relevant Build Guide section with final behavior and evidence.

#### Required validation and manual acceptance

Repeated syncs remain within agreed budget; DB failure after PUT is discoverable/recoverable; stale events cannot leak objects; concurrent/rebalanced same-version different-content runs cannot alter committed raw bytes; oversized S3 object reads abort.

Record commands, environment, expected and actual outcome, and current commit.
Infrastructure skips and mocked boundaries must be labeled. Use synthetic data
and bounded error codes; never retain raw tokens, credentials, documents or
provider responses in evidence. Run affected Ruff/mypy/pytest or web quality
checks and applicable real integration gates. Review tenant isolation, duplicate
execution, partial failure and cancellation at the changed boundary.

#### Out of scope and stop conditions

No broad object-store sweep or silent loss of pending parse inputs. Race is a review finding requiring deterministic reproduction before claiming a fix.

Stop implementation of any unresolved contract and return the exact missing
architect decision. Preserve unrelated work and existing accepted contracts.

#### Definition of done

- [ ] Intake resolves exact contracts, dependencies, allowed edits and owner.
- [ ] The stated behavior and focused failure/replay/security tests pass.
- [ ] Required integration/manual evidence is attached; no skipped check is counted as a pass.
- [ ] Deployment sequence and rollback preserve pending state and secrets; demonstrate recovery where state changes.
- [ ] Safe observability and operational ownership are defined for runtime changes.
- [ ] Documentation describes what/why/how/where/usage/verification and remaining limits.
- [ ] Reviewed diff and applicable CI pass; existing tracker is reconciled only after acceptance.

---

<a id="v1307b-resolve-sitemap-registration-versus-ingestion-support"></a>

### V1.3.07B — Resolve sitemap registration versus ingestion support

> **Record status:** BACKLOG — audit follow-up, 2026-09-12.
> **Priority:** P2.
> **Tracker:** Local candidate; search current Linear/GitHub before issue creation.
> **Ticket intake:** Needs Architect Decision: V1 support policy and exact source contract before builder handoff.

#### Goal and why

Sitemap metadata is accepted but sync deliberately fails terminally as unsupported. No remaining ticket explicitly owns traversal.

#### Exact scope and intended behavior

Decide whether to retain clearly unsupported metadata registration, reject/defer it in V1, or implement explicitly permitted bounded traversal. Freeze public error/status behavior and, if enabled, URL/depth/page/byte limits and approval policy.

#### Dependencies and contracts

ADR-020/023; V1.3.07A resource budgets if traversal enabled.

Needs Architect Decision: V1 support policy and exact source contract before builder handoff. Any new public API, database, event, error, resource-limit or
cross-service contract must be captured in an ADR/CCR and exact file allowlist
before coding. This audit does not approve unspecified contract changes.

#### Files to inspect first

`services/api/app/modules/knowledge/schemas.py`; `services/api/app/modules/knowledge/service.py`; `services/worker/app/jobs/knowledge_sync.py`; `services/worker/app/core/public_knowledge_fetch.py`; `services/worker/tests/test_public_knowledge_fetch.py`.

The edit allowlist is limited to the inspected boundary and its focused tests
once intake resolves any new paths. Update this inventory, `repo_context.md`
and the relevant Build Guide section with final behavior and evidence.

#### Required validation and manual acceptance

Registration and sync communicate the same supported feature set; if traversal enabled, disallowed hosts, cycles, huge XML, redirects, partial failures and retries are bounded and tenant-safe.

Record commands, environment, expected and actual outcome, and current commit.
Infrastructure skips and mocked boundaries must be labeled. Use synthetic data
and bounded error codes; never retain raw tokens, credentials, documents or
provider responses in evidence. Run affected Ruff/mypy/pytest or web quality
checks and applicable real integration gates. Review tenant isolation, duplicate
execution, partial failure and cancellation at the changed boundary.

#### Out of scope and stop conditions

No broad crawler, automatic permission to crawl, anti-bot bypass or hidden change to the accepted source-type contract.

Stop implementation of any unresolved contract and return the exact missing
architect decision. Preserve unrelated work and existing accepted contracts.

#### Definition of done

- [ ] Intake resolves exact contracts, dependencies, allowed edits and owner.
- [ ] The stated behavior and focused failure/replay/security tests pass.
- [ ] Required integration/manual evidence is attached; no skipped check is counted as a pass.
- [ ] Deployment sequence and rollback preserve pending state and secrets; demonstrate recovery where state changes.
- [ ] Safe observability and operational ownership are defined for runtime changes.
- [ ] Documentation describes what/why/how/where/usage/verification and remaining limits.
- [ ] Reviewed diff and applicable CI pass; existing tracker is reconciled only after acceptance.

---

<a id="v1309a-consume-parse-events-and-persist-normalized-handoff"></a>

### V1.3.09A — Consume parse events and persist normalized handoff

> **Record status:** BACKLOG — audit follow-up, 2026-09-12.
> **Priority:** P1.
> **Tracker:** Local candidate; search current Linear/GitHub before issue creation.
> **Ticket intake:** Needs Architect Decision: normalized artifact layout, storage ownership, index event contract, retries/DLQ and rollback.

#### Goal and why

The durable serviq.knowledge.parse.v1 event has no consumer. Pure parsers are complete, while V1.3.13 assumes normalized/index work that no runtime producer creates.

#### Exact scope and intended behavior

Freeze a tenant/version-safe parse consumer, content-hash verification, normalized artifact schema/storage and atomic index handoff. Add exact new file allowlist and ADR/CCR before implementation; reuse pure parsers rather than rewriting them.

#### Dependencies and contracts

V1.3.07–10; V1.3.07A raw lifecycle contract. Required before V1.3.13.

Needs Architect Decision: normalized artifact layout, storage ownership, index event contract, retries/DLQ and rollback. Any new public API, database, event, error, resource-limit or
cross-service contract must be captured in an ADR/CCR and exact file allowlist
before coding. This audit does not approve unspecified contract changes.

#### Files to inspect first

`services/worker/app/main.py`; `services/worker/app/consumers/knowledge_sync.py`; `services/worker/app/core/knowledge_normalization.py`; `services/worker/app/core/object_storage.py`; `services/worker/app/jobs/knowledge_sync.py`; `services/api/alembic/versions/20260819_0008_knowledge_schema.py`.

The edit allowlist is limited to the inspected boundary and its focused tests
once intake resolves any new paths. Update this inventory, `repo_context.md`
and the relevant Build Guide section with final behavior and evidence.

#### Required validation and manual acceptance

Real broker/S3/DB file and URL inputs yield deterministic persisted normalized output and one logical index obligation; replay/crash/stale version/wrong tenant/hash mismatch/parse failure behave safely.

Record commands, environment, expected and actual outcome, and current commit.
Infrastructure skips and mocked boundaries must be labeled. Use synthetic data
and bounded error codes; never retain raw tokens, credentials, documents or
provider responses in evidence. Run affected Ruff/mypy/pytest or web quality
checks and applicable real integration gates. Review tenant isolation, duplicate
execution, partial failure and cancellation at the changed boundary.

#### Out of scope and stop conditions

Do not mark a source ready at parse completion; do not put raw document content in logs or invent an event contract in builder code.

Stop implementation of any unresolved contract and return the exact missing
architect decision. Preserve unrelated work and existing accepted contracts.

#### Definition of done

- [ ] Intake resolves exact contracts, dependencies, allowed edits and owner.
- [ ] The stated behavior and focused failure/replay/security tests pass.
- [ ] Required integration/manual evidence is attached; no skipped check is counted as a pass.
- [ ] Deployment sequence and rollback preserve pending state and secrets; demonstrate recovery where state changes.
- [ ] Safe observability and operational ownership are defined for runtime changes.
- [ ] Documentation describes what/why/how/where/usage/verification and remaining limits.
- [ ] Reviewed diff and applicable CI pass; existing tracker is reconciled only after acceptance.

---

<a id="v1311a-redact-gateway-validation-errors-and-bound-authenticated-requests"></a>

### V1.3.11A — Redact gateway validation errors and bound authenticated requests

> **Record status:** BACKLOG — audit follow-up, 2026-09-12.
> **Priority:** P1.
> **Tracker:** Local candidate; search current Linear/GitHub before issue creation.
> **Ticket intake:** Needs Architect Decision: exact safe 422 envelope, aggregate body cap and authentication/error precedence; record any contract change.

#### Goal and why

Default FastAPI 422 responses echo invalid embedding inputs; route-body authentication happens after model parsing. A synthetic oversized input reproduces reflection even without a token.

#### Exact scope and intended behavior

Use a safe validation-error boundary and pre-body authentication/resource enforcement for private gateway routes. Preserve accepted C-4 success shape/profile and exclude input/apiKey/token values from all validation errors.

#### Dependencies and contracts

ADR-027 privacy rule and existing internal token boundary; no provider call needed.

Needs Architect Decision: exact safe 422 envelope, aggregate body cap and authentication/error precedence; record any contract change. Any new public API, database, event, error, resource-limit or
cross-service contract must be captured in an ADR/CCR and exact file allowlist
before coding. This audit does not approve unspecified contract changes.

#### Files to inspect first

`services/llm-gateway/app/main.py`; `services/llm-gateway/app/connectivity.py`; `services/llm-gateway/app/routing/embeddings.py`; `services/llm-gateway/app/schemas/c4.py`; `services/llm-gateway/tests/test_embeddings_route.py`; `services/llm-gateway/tests/test_provider_connectivity.py`.

The edit allowlist is limited to the inspected boundary and its focused tests
once intake resolves any new paths. Update this inventory, `repo_context.md`
and the relevant Build Guide section with final behavior and evidence.

#### Required validation and manual acceptance

Oversized/invalid/extra-field requests with and without auth never reflect sentinel content or credentials; auth precedes expensive parsing; input/body limits and response cardinality remain enforced; full gateway suite/static checks pass.

Record commands, environment, expected and actual outcome, and current commit.
Infrastructure skips and mocked boundaries must be labeled. Use synthetic data
and bounded error codes; never retain raw tokens, credentials, documents or
provider responses in evidence. Run affected Ruff/mypy/pytest or web quality
checks and applicable real integration gates. Review tenant isolation, duplicate
execution, partial failure and cancellation at the changed boundary.

#### Out of scope and stop conditions

Do not weaken schema validation or expose a new public gateway API.

Stop implementation of any unresolved contract and return the exact missing
architect decision. Preserve unrelated work and existing accepted contracts.

#### Definition of done

- [ ] Intake resolves exact contracts, dependencies, allowed edits and owner.
- [ ] The stated behavior and focused failure/replay/security tests pass.
- [ ] Required integration/manual evidence is attached; no skipped check is counted as a pass.
- [ ] Deployment sequence and rollback preserve pending state and secrets; demonstrate recovery where state changes.
- [ ] Safe observability and operational ownership are defined for runtime changes.
- [ ] Documentation describes what/why/how/where/usage/verification and remaining limits.
- [ ] Reviewed diff and applicable CI pass; existing tracker is reconciled only after acceptance.

---

<a id="v1311b-implement-approved-semantic-embedding-transport"></a>

### V1.3.11B — Implement approved semantic embedding transport

> **Record status:** BACKLOG — audit follow-up, 2026-09-12.
> **Priority:** P1 before semantic retrieval acceptance.
> **Tracker:** Local candidate; search current Linear/GitHub before issue creation.
> **Ticket intake:** Needs Architect Decision: provider/model, profile compatibility, secret boundary, budget and reindex/cutover strategy.

#### Goal and why

Current fake hashes are deterministic but not semantic embeddings. No active provider embedding transport or meaningful retrieval evaluation exists.

#### Exact scope and intended behavior

Choose one supported semantic model/profile with credential resolution, input normalization, timeout/retry/cost bounds and response validation. Preserve fake offline tests. Define how fake or previous-model data is identified and reindexed; do not mix vector spaces under one alias without migration.

#### Dependencies and contracts

ADR-027; coordinate V1.3.12/13 persistence and V1.4 evaluation.

Needs Architect Decision: provider/model, profile compatibility, secret boundary, budget and reindex/cutover strategy. Any new public API, database, event, error, resource-limit or
cross-service contract must be captured in an ADR/CCR and exact file allowlist
before coding. This audit does not approve unspecified contract changes.

#### Files to inspect first

`services/llm-gateway/app/routing/embeddings.py`; `services/llm-gateway/app/adapters/base.py`; `services/llm-gateway/app/adapters/fake.py`; `services/llm-gateway/app/schemas/c4.py`; `docs/architecture-decisions/ADR-027-embedding-profile.md`.

The edit allowlist is limited to the inspected boundary and its focused tests
once intake resolves any new paths. Update this inventory, `repo_context.md`
and the relevant Build Guide section with final behavior and evidence.

#### Required validation and manual acceptance

Deterministic fake tests pass; mocked provider timeout/auth/rate-limit/malformed/nonfinite/count/dimension cases pass; approved non-empty real-provider integration and relevance benchmark recorded without sensitive content.

Record commands, environment, expected and actual outcome, and current commit.
Infrastructure skips and mocked boundaries must be labeled. Use synthetic data
and bounded error codes; never retain raw tokens, credentials, documents or
provider responses in evidence. Run affected Ruff/mypy/pytest or web quality
checks and applicable real integration gates. Review tenant isolation, duplicate
execution, partial failure and cancellation at the changed boundary.

#### Out of scope and stop conditions

No paid/provider calls until environment and access are configured; do not infer semantics from dimension equality.

Stop implementation of any unresolved contract and return the exact missing
architect decision. Preserve unrelated work and existing accepted contracts.

#### Definition of done

- [ ] Intake resolves exact contracts, dependencies, allowed edits and owner.
- [ ] The stated behavior and focused failure/replay/security tests pass.
- [ ] Required integration/manual evidence is attached; no skipped check is counted as a pass.
- [ ] Deployment sequence and rollback preserve pending state and secrets; demonstrate recovery where state changes.
- [ ] Safe observability and operational ownership are defined for runtime changes.
- [ ] Documentation describes what/why/how/where/usage/verification and remaining limits.
- [ ] Reviewed diff and applicable CI pass; existing tracker is reconciled only after acceptance.

---

## Phase 1 — V1 Production Foundation

<a id="v1305-implement-ssrf-safe-public-knowledge-fetch-helper"></a>

### V1.3.05 — Implement SSRF-safe public knowledge fetch helper

> **Record status:** IMPLEMENTED at audited main `3e1b9aa`; excluded from remaining implementation counts.
> **Tracker snapshot (2026-09-12):** OPE-311 — Done; PR #192 merged.
> **Intake:** Historical implementation record; do not re-create as new work. Follow-ups remain open where named.

#### Accepted implementation scope

Bounded SSRF-safe public fetch helper, approved exact-host policy, redirects and stable retryable failures.

#### Source and validation

`services/worker/app/core/public_knowledge_fetch.py`; `services/worker/tests/test_public_knowledge_fetch.py`.

Worker suite passed. Helper is active in URL sync. Crawl permission, sitemap traversal and aggregate storage budgets are not implied by SSRF safety.

See [current system audit](SYSTEM_AUDIT_2026-09-12.md) for local versus CI evidence,
release limits and tracker closeout. The [Build Guide](SERVIQ_BUILD_GUIDE.md)
retains the behavior, usage, design and rollback explanation. Historical DRAFT
intake checklists have been replaced by this verified implementation record;
unrelated downstream requirements have not been marked complete.

---

<a id="v1306-implement-source-sync-command-and-durable-outbox-event"></a>

### V1.3.06 — Implement source sync command and durable outbox event

> **Record status:** IMPLEMENTED at audited main `3e1b9aa`; excluded from remaining implementation counts.
> **Tracker snapshot (2026-09-12):** OPE-312 — Done; PR #195 merged.
> **Intake:** Historical implementation record; do not re-create as new work. Follow-ups remain open where named.

#### Accepted implementation scope

Tenant/source row lock, monotonic sync_version, syncing state and serviq.knowledge.sync.v1 insertion in one transaction; HTTP 202.

#### Source and validation

`services/api/app/modules/knowledge/service.py`; `services/api/alembic/versions/20260902_0012_outbox_events.py`; `services/api/tests/integration/test_knowledge_source_sync_api.py`.

Current-main PostgreSQL job passed. Route tests inject trusted principal dependencies; production auth composition remains V1.1.16.

See [current system audit](SYSTEM_AUDIT_2026-09-12.md) for local versus CI evidence,
release limits and tracker closeout. The [Build Guide](SERVIQ_BUILD_GUIDE.md)
retains the behavior, usage, design and rollback explanation. Historical DRAFT
intake checklists have been replaced by this verified implementation record;
unrelated downstream requirements have not been marked complete.

---

<a id="v1306a-publish-transactional-outbox-events-to-kafka"></a>

### V1.3.06A — Publish transactional outbox events to Kafka

> **Record status:** IMPLEMENTED at audited main `3e1b9aa`; excluded from remaining implementation counts.
> **Tracker snapshot (2026-09-12):** OPE-314 — Done; PR #200 merged as 2c8d1716.
> **Intake:** Historical implementation record; do not re-create as new work. Follow-ups remain open where named.

#### Accepted implementation scope

Generic PostgreSQL outbox publisher using FOR UPDATE SKIP LOCKED and at-least-once Kafka delivery; broker acknowledgement before published state.

#### Source and validation

`services/worker/app/jobs/outbox_publisher.py`; `services/worker/app/core/broker.py`; `services/worker/tests/integration/test_outbox_publisher_postgres.py`.

Current worker suite passed and earlier publisher integration passed. Broker outages retry with bounded delay; duplicate delivery remains possible and consumers must remain idempotent.

See [current system audit](SYSTEM_AUDIT_2026-09-12.md) for local versus CI evidence,
release limits and tracker closeout. The [Build Guide](SERVIQ_BUILD_GUIDE.md)
retains the behavior, usage, design and rollback explanation. Historical DRAFT
intake checklists have been replaced by this verified implementation record;
unrelated downstream requirements have not been marked complete.

---

<a id="v1307-implement-knowledge-sync-fetch-worker"></a>

### V1.3.07 — Implement knowledge sync fetch worker

> **Record status:** IMPLEMENTED at audited main `3e1b9aa`; excluded from remaining implementation counts.
> **Tracker snapshot (2026-09-12):** OPE-313 — Done; PRs #201/#202 merged.
> **Intake:** Historical implementation record; do not re-create as new work. Follow-ups remain open where named.

#### Accepted implementation scope

Consume URL/file sync work, preserve tenant/version/replay semantics, store raw content, commit document plus parse outbox, retry and DLQ safely.

#### Source and validation

`services/worker/app/consumers/knowledge_sync.py`; `services/worker/app/jobs/knowledge_sync.py`; `services/worker/tests/integration/test_knowledge_sync_end_to_end.py`.

Current worker suite passed; earlier PR-head integration passed. Success intentionally leaves syncing. Sitemap unsupported. Raw-object budget/recovery risks are separately tracked in V1.3.07A/B.

See [current system audit](SYSTEM_AUDIT_2026-09-12.md) for local versus CI evidence,
release limits and tracker closeout. The [Build Guide](SERVIQ_BUILD_GUIDE.md)
retains the behavior, usage, design and rollback explanation. Historical DRAFT
intake checklists have been replaced by this verified implementation record;
unrelated downstream requirements have not been marked complete.

---

<a id="v1308-implement-pdfmarkdowntext-normalization-parser"></a>

### V1.3.08 — Implement PDF/Markdown/text normalization parser

> **Record status:** IMPLEMENTED at audited main `3e1b9aa`; excluded from remaining implementation counts.
> **Tracker snapshot (2026-09-12):** OPE-315 — Done.
> **Intake:** Historical implementation record; do not re-create as new work. Follow-ups remain open where named.

#### Accepted implementation scope

Pure bounded PDF/Markdown/text normalization into deterministic segments with provenance; no I/O or persistence.

#### Source and validation

`services/worker/app/core/knowledge_normalization.py`; `services/worker/tests/test_knowledge_normalization.py`.

Current worker suite passed. No parse consumer was activated by this ticket; V1.3.09A now explicitly owns durable orchestration.

See [current system audit](SYSTEM_AUDIT_2026-09-12.md) for local versus CI evidence,
release limits and tracker closeout. The [Build Guide](SERVIQ_BUILD_GUIDE.md)
retains the behavior, usage, design and rollback explanation. Historical DRAFT
intake checklists have been replaced by this verified implementation record;
unrelated downstream requirements have not been marked complete.

---

<a id="v1309-implement-htmlhelp-center-normalization-parser"></a>

### V1.3.09 — Implement HTML/help-center normalization parser

> **Record status:** IMPLEMENTED at audited main `3e1b9aa`; excluded from remaining implementation counts.
> **Tracker snapshot (2026-09-12):** OPE-316 — Done; PR #212 merged.
> **Intake:** Historical implementation record; do not re-create as new work. Follow-ups remain open where named.

#### Accepted implementation scope

Pure inert HTML/help-center normalization with title, heading, paragraph/list extraction and noise removal.

#### Source and validation

`services/worker/app/core/knowledge_normalization.py`; `services/worker/tests/test_knowledge_normalization.py`.

Current worker suite passed. Reuses url source type; does not crawl links or persist normalized artifacts.

See [current system audit](SYSTEM_AUDIT_2026-09-12.md) for local versus CI evidence,
release limits and tracker closeout. The [Build Guide](SERVIQ_BUILD_GUIDE.md)
retains the behavior, usage, design and rollback explanation. Historical DRAFT
intake checklists have been replaced by this verified implementation record;
unrelated downstream requirements have not been marked complete.

---

<a id="v1310-implement-deterministic-heading-aware-chunker"></a>

### V1.3.10 — Implement deterministic heading-aware chunker

> **Record status:** IMPLEMENTED at audited main `3e1b9aa`; excluded from remaining implementation counts.
> **Tracker snapshot (2026-09-12):** OPE-317 — Done; PR #215 merged.
> **Intake:** Historical implementation record; do not re-create as new work. Follow-ups remain open where named.

#### Accepted implementation scope

Deterministic heading-aware chunking with frozen whitespace token units, overlap and ordered segment provenance (ADR-026).

#### Source and validation

`services/worker/app/core/knowledge_chunking.py`; `services/worker/tests/test_knowledge_chunking.py`.

Current worker suite passed. Pure library, no DB/index/event activation; token counts are not vendor billing-token evidence.

See [current system audit](SYSTEM_AUDIT_2026-09-12.md) for local versus CI evidence,
release limits and tracker closeout. The [Build Guide](SERVIQ_BUILD_GUIDE.md)
retains the behavior, usage, design and rollback explanation. Historical DRAFT
intake checklists have been replaced by this verified implementation record;
unrelated downstream requirements have not been marked complete.

---

<a id="v1311-freeze-embedding-profile-adr-and-implement-embedding-adapter"></a>

### V1.3.11 — Freeze embedding profile ADR and implement embedding adapter

> **Record status:** IMPLEMENTED at audited main `3e1b9aa`; excluded from remaining implementation counts.
> **Tracker snapshot (2026-09-12):** OPE-319 — Done; PR #222 merged as 26784e7.
> **Intake:** Historical implementation record; do not re-create as new work. Follow-ups remain open where named.

#### Accepted implementation scope

ADR-027 profile and private deterministic fake embedding path: alias serviq-embedding-v1, 1536 dimensions, 100 inputs, 32,000 characters/input.

#### Source and validation

`services/llm-gateway/app/routing/embeddings.py`; `services/llm-gateway/app/adapters/fake.py`; `services/llm-gateway/tests/test_embeddings_route.py`.

104 gateway tests passed. Fake vectors are not semantic; raw-input reflection on validation is an open regression gap (V1.3.11A), and real embedding transport is V1.3.11B.

See [current system audit](SYSTEM_AUDIT_2026-09-12.md) for local versus CI evidence,
release limits and tracker closeout. The [Build Guide](SERVIQ_BUILD_GUIDE.md)
retains the behavior, usage, design and rollback explanation. Historical DRAFT
intake checklists have been replaced by this verified implementation record;
unrelated downstream requirements have not been marked complete.

---

<a id="v1312-add-pgvector-index-migration"></a>

### V1.3.12 — Add pgvector index migration

> **2026-09-12 dependency correction:** ADR-027 now freezes dimension 1536. Do not reopen that resolved choice; freeze distance/operator/index, constraints, existing-data handling and safe migration/downgrade. Coordinate V1.3.11B so fake and semantic vectors are never mixed silently.

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

create the correct vector index/operator for the frozen embedding dimension.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

**Goal:** create the correct vector index/operator for the frozen embedding dimension.
**Steps:** verify ADR, create index, safe downgrade, test nearest-neighbor query and wrong dimension.
**Acceptance:** index exactly matches profile.

#### Exact scope

verify ADR, create index, safe downgrade, test nearest-neighbor query and wrong dimension.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

**Goal:** create the correct vector index/operator for the frozen embedding dimension.
**Steps:** verify ADR, create index, safe downgrade, test nearest-neighbor query and wrong dimension.
**Acceptance:** index exactly matches profile.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

verify ADR, create index, safe downgrade, test nearest-neighbor query and wrong dimension.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] index exactly matches profile.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1313-implement-chunkembedindex-worker"></a>

### V1.3.13 — Implement chunk/embed/index worker

> **2026-09-12 dependency correction:** Explicit prerequisite: V1.3.09A must produce persisted normalized artifacts and the frozen index handoff. V1.3.11 is fake-only; semantic acceptance additionally requires V1.3.11B. Source ready must wait for complete committed indexing.

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

consume index job, chunk normalized document, batch-embed, persist chunks, mark source/document ready, emit C-9.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

**Goal:** consume index job, chunk normalized document, batch-embed, persist chunks, mark source/document ready, emit C-9.
**Tests:** success, replay idempotency, embedding failure, stale version, wrong tenant.
**Acceptance:** source reaches ready only after complete successful indexing.

#### Exact scope

**Goal:** consume index job, chunk normalized document, batch-embed, persist chunks, mark source/document ready, emit C-9.
**Tests:** success, replay idempotency, embedding failure, stale version, wrong tenant.
**Acceptance:** source reaches ready only after complete successful indexing.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

**Goal:** consume index job, chunk normalized document, batch-embed, persist chunks, mark source/document ready, emit C-9.
**Tests:** success, replay idempotency, embedding failure, stale version, wrong tenant.
**Acceptance:** source reaches ready only after complete successful indexing.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

success, replay idempotency, embedding failure, stale version, wrong tenant.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] source reaches ready only after complete successful indexing.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1401-implement-tenant-scoped-lexical-fts-repository"></a>

### V1.4.01 — Implement tenant-scoped lexical FTS repository

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

return ranked active customer/internal-scoped lexical chunks.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

return ranked active customer/internal-scoped lexical chunks.

#### Frozen source requirements

**Goal:** return ranked active customer/internal-scoped lexical chunks.
**Tests:** match/no-match, disabled source excluded, internal excluded for customer, cross-tenant excluded.
**Acceptance:** filters execute in SQL, not only post-processing.

#### Exact scope

**Goal:** return ranked active customer/internal-scoped lexical chunks.
**Tests:** match/no-match, disabled source excluded, internal excluded for customer, cross-tenant excluded.
**Acceptance:** filters execute in SQL, not only post-processing.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

match/no-match, disabled source excluded, internal excluded for customer, cross-tenant excluded.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] filters execute in SQL, not only post-processing.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v1402-implement-tenant-scoped-pgvector-repository"></a>

### V1.4.02 — Implement tenant-scoped pgvector repository

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

embed query and return nearest active scoped chunks using frozen profile.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

**Goal:** embed query and return nearest active scoped chunks using frozen profile.
**Tests:** semantic match, no vector, disabled/foreign tenant excluded, embedding failure.
**Acceptance:** exact dimension/profile and query-level filters.

#### Exact scope

**Goal:** embed query and return nearest active scoped chunks using frozen profile.
**Tests:** semantic match, no vector, disabled/foreign tenant excluded, embedding failure.
**Acceptance:** exact dimension/profile and query-level filters.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

semantic match, no vector, disabled/foreign tenant excluded, embedding failure.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] exact dimension/profile and query-level filters.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1403-implement-deterministic-hybrid-ranker"></a>

### V1.4.03 — Implement deterministic hybrid ranker

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

merge lexical/vector candidates using architect-frozen RRF/weighted formula, dedupe chunks, stable tie-break, topK.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

**Goal:** merge lexical/vector candidates using architect-frozen RRF/weighted formula, dedupe chunks, stable tie-break, topK.
**Tests:** overlap, lexical-only, vector-only, ties, truncation.
**Acceptance:** identical inputs produce identical ordering.

#### Exact scope

**Goal:** merge lexical/vector candidates using architect-frozen RRF/weighted formula, dedupe chunks, stable tie-break, topK.
**Tests:** overlap, lexical-only, vector-only, ties, truncation.
**Acceptance:** identical inputs produce identical ordering.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

overlap, lexical-only, vector-only, ties, truncation.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] identical inputs produce identical ordering.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1404-implement-retrieval-contract-c-3-service"></a>

### V1.4.04 — Implement Retrieval Contract C-3 service

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

validate C-3, run hybrid retrieval, persist retrieval run/results, build citations, return exact response.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

validate C-3, run hybrid retrieval, persist retrieval run/results, build citations, return exact response.

#### Frozen source requirements

**Goal:** validate C-3, run hybrid retrieval, persist retrieval run/results, build citations, return exact response.
**Tests:** success, empty results, invalid topK, foreign sourceId, unavailable dependency.
**Acceptance:** exact C-3 field names/errors and persisted diagnostics.

#### Exact scope

**Goal:** validate C-3, run hybrid retrieval, persist retrieval run/results, build citations, return exact response.
**Tests:** success, empty results, invalid topK, foreign sourceId, unavailable dependency.
**Acceptance:** exact C-3 field names/errors and persisted diagnostics.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

**Goal:** validate C-3, run hybrid retrieval, persist retrieval run/results, build citations, return exact response.
**Tests:** success, empty results, invalid topK, foreign sourceId, unavailable dependency.
**Acceptance:** exact C-3 field names/errors and persisted diagnostics.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

success, empty results, invalid topK, foreign sourceId, unavailable dependency.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] exact C-3 field names/errors and persisted diagnostics.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1405-implement-retrieval-debugger-api"></a>

### V1.4.05 — Implement retrieval debugger API

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

authorized business user can POST a test query and GET stored retrieval diagnostics without running the agent.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

authorized business user can POST a test query and GET stored retrieval diagnostics without running the agent.

#### Frozen source requirements

**Goal:** authorized business user can POST a test query and GET stored retrieval diagnostics without running the agent.
**API:** architecture retrieval-query/run routes.
**Tests:** authorized success, empty result, invalid query/topK, foreign source, unauthorized.
**Acceptance:** customer users cannot access diagnostics or internal-scoped knowledge.

#### Exact scope

**Goal:** authorized business user can POST a test query and GET stored retrieval diagnostics without running the agent.
**API:** architecture retrieval-query/run routes.
**Tests:** authorized success, empty result, invalid query/topK, foreign source, unauthorized.
**Acceptance:** customer users cannot access diagnostics or internal-scoped knowledge.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

architecture retrieval-query/run routes.

Needs Architect Decision: before builder handoff, paste the complete auth rule, request fields and validation, response bodies for every status, and standard error envelope from the accepted architecture/current code.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

authorized success, empty result, invalid query/topK, foreign source, unauthorized.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] customer users cannot access diagnostics or internal-scoped knowledge.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1501-create-customerconversationmessagefeedback-migration"></a>

### V1.5.01 — Create customer/conversation/message/feedback migration

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Create `customers`, `customer_identities`, `conversations`, `messages`, `feedback` exactly from Architecture v1.2, including corrected partial unique index on non-null `external_ref` and `resolved_at`.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Create `customers`, `customer_identities`, `conversations`, `messages`, `feedback` exactly from Architecture v1.2, including corrected partial unique index on non-null `external_ref` and `resolved_at`.

#### Frozen source requirements

Create `customers`, `customer_identities`, `conversations`, `messages`, `feedback` exactly from Architecture v1.2, including corrected partial unique index on non-null `external_ref` and `resolved_at`. Add upgrade/downgrade and constraint tests. Acceptance: multiple NULL external refs work, duplicate non-null ref fails, message sequence unique per conversation, status checks exact.

#### Exact scope

Create `customers`, `customer_identities`, `conversations`, `messages`, `feedback` exactly from Architecture v1.2, including corrected partial unique index on non-null `external_ref` and `resolved_at`. Add upgrade/downgrade and constraint tests. Acceptance: multiple NULL external refs work, duplicate non-null ref fails, message sequence unique per conversation, status checks exact.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Create `customers`, `customer_identities`, `conversations`, `messages`, `feedback` exactly from Architecture v1.2, including corrected partial unique index on non-null `external_ref` and `resolved_at`. Add upgrade/downgrade and constraint tests. Acceptance: multiple NULL external refs work, duplicate non-null ref fails, message sequence unique per conversation, status checks exact.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] multiple NULL external refs work, duplicate non-null ref fails, message sequence unique per conversation, status checks exact.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1502-implement-public-support-tenantdeployment-resolver"></a>

### V1.5.02 — Implement public support tenant/deployment resolver

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Resolve active tenant by support slug plus active `customer_web` agent deployment, returning only public-safe metadata.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Resolve active tenant by support slug plus active `customer_web` agent deployment, returning only public-safe metadata.

#### Frozen source requirements

Resolve active tenant by support slug plus active `customer_web` agent deployment, returning only public-safe metadata. Test active, unknown, suspended, no-active-deployment. Never expose tenant secrets/internal suspension reason.

#### Exact scope

Resolve active tenant by support slug plus active `customer_web` agent deployment, returning only public-safe metadata. Test active, unknown, suspended, no-active-deployment. Never expose tenant secrets/internal suspension reason.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test active, unknown, suspended, no-active-deployment.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: AI, Serviq: Security, Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1503-implement-signed-anonymous-customer-session"></a>

### V1.5.03 — Implement signed anonymous customer session

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Create/validate short-lived signed anonymous session containing tenant, subject/session, audience, expiry, assurance=`anonymous`.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Create/validate short-lived signed anonymous session containing tenant, subject/session, audience, expiry, assurance=`anonymous`.

#### Frozen source requirements

Create/validate short-lived signed anonymous session containing tenant, subject/session, audience, expiry, assurance=`anonymous`. Use approved crypto/session library only. Test expiry, tamper, wrong audience, wrong tenant. Protected operations never silently downgrade to anonymous. Premium security review.

#### Exact scope

Create/validate short-lived signed anonymous session containing tenant, subject/session, audience, expiry, assurance=`anonymous`. Use approved crypto/session library only. Test expiry, tamper, wrong audience, wrong tenant. Protected operations never silently downgrade to anonymous. Premium security review.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

proved crypto/session library only. Test expiry, tamper, wrong audience, wrong tenant.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1504-implement-customer-conversation-create-api"></a>

### V1.5.04 — Implement customer conversation create API

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

`POST /api/v1/customer/conversations` creates an open AI-owned conversation using trusted tenant/deployment/session context.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

`POST /api/v1/customer/conversations` creates an open AI-owned conversation using trusted tenant/deployment/session context.

#### Frozen source requirements

`POST /api/v1/customer/conversations` creates an open AI-owned conversation using trusted tenant/deployment/session context. Request body must not accept tenantId/customerId. Test anonymous success, spoofed tenant input, inactive deployment, cross-tenant token, rate limit. Return safe DTO.

#### Exact scope

`POST /api/v1/customer/conversations` creates an open AI-owned conversation using trusted tenant/deployment/session context. Request body must not accept tenantId/customerId. Test anonymous success, spoofed tenant input, inactive deployment, cross-tenant token, rate limit. Return safe DTO.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

`POST /api/v1/customer/conversations`

Needs Architect Decision: before builder handoff, paste the complete auth rule, request fields and validation, response bodies for every status, and standard error envelope from the accepted architecture/current code.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test anonymous success, spoofed tenant input, inactive deployment, cross-tenant token, rate limit.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v1505-implement-customer-message-append-c-2-outbox-event"></a>

### V1.5.05 — Implement customer message append + C-2 outbox event

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

`POST /api/v1/customer/conversations/{id}/messages` accepts `{content}` and required idempotency key, allocates ordered message sequence safely, inserts customer message, updates last_message_at, and writes exact C-2 `agent.run.requested` event in same transaction.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

`POST /api/v1/customer/conversations/{id}/messages` accepts `{content}` and required idempotency key, allocates ordered message sequence safely, inserts customer message, updates last_message_at, and writes exact C-2 `agent.run.requested` event in same transaction.

#### Frozen source requirements

`POST /api/v1/customer/conversations/{id}/messages` accepts `{content}` and required idempotency key, allocates ordered message sequence safely, inserts customer message, updates last_message_at, and writes exact C-2 `agent.run.requested` event in same transaction. Test duplicate key, changed request with same key, concurrent messages, oversize, wrong customer, rollback.

#### Exact scope

`POST /api/v1/customer/conversations/{id}/messages` accepts `{content}` and required idempotency key, allocates ordered message sequence safely, inserts customer message, updates last_message_at, and writes exact C-2 `agent.run.requested` event in same transaction. Test duplicate key, changed request with same key, concurrent messages, oversize, wrong customer, rollback.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `apps/customer-web/src/app/page.tsx` — current customer-web entry surface.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

`POST /api/v1/customer/conversations/{id}/messages`

Needs Architect Decision: before builder handoff, paste the complete auth rule, request fields and validation, response bodies for every status, and standard error envelope from the accepted architecture/current code.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test duplicate key, changed request with same key, concurrent messages, oversize, wrong customer, rollback.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1506-implement-customer-conversation-history-api"></a>

### V1.5.06 — Implement customer conversation history API

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Return only customer-visible messages in sequence order for owning customer.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Return only customer-visible messages in sequence order for owning customer.

#### Frozen source requirements

Return only customer-visible messages in sequence order for owning customer. Internal notes, raw tool payloads, raw prompts and agent internals never appear. Test wrong customer/tenant hidden and internal visibility exclusion.

#### Exact scope

Return only customer-visible messages in sequence order for owning customer. Internal notes, raw tool payloads, raw prompts and agent internals never appear. Test wrong customer/tenant hidden and internal visibility exclusion.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test wrong customer/tenant hidden and internal visibility exclusion.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1507-implement-authenticated-customer-sse-stream"></a>

### V1.5.07 — Implement authenticated customer SSE stream

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Implement `GET /api/v1/customer/conversations/{id}/events` with exact public event allowlist from Architecture 5.3/C-12, keepalive, disconnect cleanup, bounded reconnect, strict serialization.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Implement `GET /api/v1/customer/conversations/{id}/events` with exact public event allowlist from Architecture 5.3/C-12, keepalive, disconnect cleanup, bounded reconnect, strict serialization.

#### Frozen source requirements

Implement `GET /api/v1/customer/conversations/{id}/events` with exact public event allowlist from Architecture 5.3/C-12, keepalive, disconnect cleanup, bounded reconnect, strict serialization. Test allowed event, internal event dropped, wrong customer, malformed event, disconnect cleanup.

#### Exact scope

Implement `GET /api/v1/customer/conversations/{id}/events` with exact public event allowlist from Architecture 5.3/C-12, keepalive, disconnect cleanup, bounded reconnect, strict serialization. Test allowed event, internal event dropped, wrong customer, malformed event, disconnect cleanup.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `apps/customer-web/src/app/page.tsx` — current customer-web entry surface.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

`GET /api/v1/customer/conversations/{id}/events`

Needs Architect Decision: before builder handoff, paste the complete auth rule, request fields and validation, response bodies for every status, and standard error envelope from the accepted architecture/current code.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test allowed event, internal event dropped, wrong customer, malformed event, disconnect cleanup.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1508-implement-customer-human-request-and-feedback-apis"></a>

### V1.5.08 — Implement customer human-request and feedback APIs

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Add human-request and feedback routes.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Add human-request and feedback routes.

#### Frozen source requirements

Add human-request and feedback routes. Human request idempotent with reason `customer_requested`; feedback rating 1–5 and comment <=2000. Test repeat human request, valid/invalid feedback, cross-customer access.

#### Exact scope

Add human-request and feedback routes. Human request idempotent with reason `customer_requested`; feedback rating 1–5 and comment <=2000. Test repeat human request, valid/invalid feedback, cross-customer access.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test repeat human request, valid/invalid feedback, cross-customer access.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1601-create-agent-runstepretrieval-persistence-migration"></a>

### V1.6.01 — Create agent run/step/retrieval persistence migration

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Create `agent_runs`, `agent_steps`, `retrieval_runs`, `retrieval_results` exactly from architecture.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Create `agent_runs`, `agent_steps`, `retrieval_runs`, `retrieval_results` exactly from architecture. Test request-class/status checks, unique step ordinals, retrieval ranks/results and downgrade.

#### Exact scope

Create `agent_runs`, `agent_steps`, `retrieval_runs`, `retrieval_results` exactly from architecture. Test request-class/status checks, unique step ordinals, retrieval ranks/results and downgrade.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Create `agent_runs`, `agent_steps`, `retrieval_runs`, `retrieval_results` exactly from architecture. Test request-class/status checks, unique step ordinals, retrieval ranks/results and downgrade.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test request-class/status checks, unique step ordinals, retrieval ranks/results and downgrade.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1602-consume-c-2-and-create-agent-run-idempotently"></a>

### V1.6.02 — Consume C-2 and create agent run idempotently

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Worker consumes `agent.run.requested`, verifies tenant/conversation/message/deployed agent version, creates one agent_run, carries correlation ID and invokes orchestrator entrypoint.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Worker consumes `agent.run.requested`, verifies tenant/conversation/message/deployed agent version, creates one agent_run, carries correlation ID and invokes orchestrator entrypoint.

#### Frozen source requirements

Worker consumes `agent.run.requested`, verifies tenant/conversation/message/deployed agent version, creates one agent_run, carries correlation ID and invokes orchestrator entrypoint. Duplicate event cannot duplicate run. Missing/retired version becomes failed/DLQ plus recoverable customer state.

#### Exact scope

Worker consumes `agent.run.requested`, verifies tenant/conversation/message/deployed agent version, creates one agent_run, carries correlation ID and invokes orchestrator entrypoint. Duplicate event cannot duplicate run. Missing/retired version becomes failed/DLQ plus recoverable customer state.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: AI, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1603-implement-central-agent-budget-tracker"></a>

### V1.6.03 — Implement central agent budget tracker

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Enforce architecture defaults/hard limits for steps, model calls, retrieval calls, tool calls, mutation count, wall clock, tokens and timeouts.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Enforce architecture defaults/hard limits for steps, model calls, retrieval calls, tool calls, mutation count, wall clock, tokens and timeouts. Tenant config can be stricter but never exceed hard limit. Test every boundary and wall-clock expiry. Exceeding budget returns `AGENT_BUDGET_EXHAUSTED` before attempted operation.

#### Exact scope

Enforce architecture defaults/hard limits for steps, model calls, retrieval calls, tool calls, mutation count, wall clock, tokens and timeouts. Tenant config can be stricter but never exceed hard limit. Test every boundary and wall-clock expiry. Exceeding budget returns `AGENT_BUDGET_EXHAUSTED` before attempted operation.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test every boundary and wall-clock expiry.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: AI, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1604-implement-strict-request-classifier"></a>

### V1.6.04 — Implement strict request classifier

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Classify into exactly `deterministic|knowledge|transactional|reasoning|escalation_first` using structured output or approved deterministic fast rule.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Classify into exactly `deterministic|knowledge|transactional|reasoning|escalation_first` using structured output or approved deterministic fast rule. Persist request_class. Unknown/malformed output fails safe. Test FAQ, status lookup, refund/action, explicit human request, malformed model output.

#### Exact scope

Classify into exactly `deterministic|knowledge|transactional|reasoning|escalation_first` using structured output or approved deterministic fast rule. Persist request_class. Unknown/malformed output fails safe. Test FAQ, status lookup, refund/action, explicit human request, malformed model output.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Classify into exactly `deterministic|knowledge|transactional|reasoning|escalation_first` using structured output or approved deterministic fast rule. Persist request_class. Unknown/malformed output fails safe. Test FAQ, status lookup, refund/action, explicit human request, malformed model output.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

proved deterministic fast rule. Test FAQ, status lookup, refund/action, explicit human request, malformed model output.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: AI, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v1605-implement-budgeted-retrieval-agent-step"></a>

### V1.6.05 — Implement budgeted retrieval agent step

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Consume retrieval budget, create agent_step, call exact C-3, complete/fail step, return evidence/retrievalRunId.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Consume retrieval budget, create agent_step, call exact C-3, complete/fail step, return evidence/retrievalRunId.

#### Frozen source requirements

Consume retrieval budget, create agent_step, call exact C-3, complete/fail step, return evidence/retrievalRunId. Customer-facing retrieval uses customer scope. Test success, empty, budget exhausted, unavailable dependency, scope enforcement.

#### Exact scope

Consume retrieval budget, create agent_step, call exact C-3, complete/fail step, return evidence/retrievalRunId. Customer-facing retrieval uses customer scope. Test success, empty, budget exhausted, unavailable dependency, scope enforcement.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test success, empty, budget exhausted, unavailable dependency, scope enforcement.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: AI, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1606-implement-budgeted-generation-agent-step"></a>

### V1.6.06 — Implement budgeted generation agent step

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Assemble approved system instructions + user message + retrieved evidence as untrusted data, call C-4, update token/model-call counters, persist safe step summary without raw sensitive prompt.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Assemble approved system instructions + user message + retrieved evidence as untrusted data, call C-4, update token/model-call counters, persist safe step summary without raw sensitive prompt.

#### Frozen source requirements

Assemble approved system instructions + user message + retrieved evidence as untrusted data, call C-4, update token/model-call counters, persist safe step summary without raw sensitive prompt. Test success, token budget, timeout, malicious retrieved instruction remains data, log redaction.

#### Exact scope

Assemble approved system instructions + user message + retrieved evidence as untrusted data, call C-4, update token/model-call counters, persist safe step summary without raw sensitive prompt. Test success, token budget, timeout, malicious retrieved instruction remains data, log redaction.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Assemble approved system instructions + user message + retrieved evidence as untrusted data, call C-4, update token/model-call counters, persist safe step summary without raw sensitive prompt. Test success, token budget, timeout, malicious retrieved instruction remains data, log redaction.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

proved system instructions + user message + retrieved evidence as untrusted data, call C-4, update token/model-call counters, persist safe step summary without raw sensitive prompt. Test success, token budget, timeout, malicious retrieved instruction remains data, log redaction.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: AI, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1607-implement-ordered-provider-fallback-router"></a>

### V1.6.07 — Implement ordered provider fallback router

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Load configured primary/fallback model aliases, skip inactive/invalid providers, respect model-call/wall budgets and provider health, advance only for allowed error classes.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Load configured primary/fallback model aliases, skip inactive/invalid providers, respect model-call/wall budgets and provider health, advance only for allowed error classes. No 429 loop. Test primary success, timeout->fallback, 429->fallback, no fallback, auth-error behavior, exhausted budget.

#### Exact scope

Load configured primary/fallback model aliases, skip inactive/invalid providers, respect model-call/wall budgets and provider health, advance only for allowed error classes. No 429 loop. Test primary success, timeout->fallback, 429->fallback, no fallback, auth-error behavior, exhausted budget.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test primary success, timeout->fallback, 429->fallback, no fallback, auth-error behavior, exhausted budget.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Security, Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1608-implement-grounded-output-guardrail"></a>

### V1.6.08 — Implement grounded output guardrail

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Before customer-visible answer is persisted, ensure required evidence exists, citation IDs subset actual retrieved chunks, internal-only metadata absent and response bounded.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Before customer-visible answer is persisted, ensure required evidence exists, citation IDs subset actual retrieved chunks, internal-only metadata absent and response bounded.

#### Frozen source requirements

Before customer-visible answer is persisted, ensure required evidence exists, citation IDs subset actual retrieved chunks, internal-only metadata absent and response bounded. Knowledge requests with zero evidence cannot pass unless approved deterministic path. Test real/fake citations, zero evidence, internal marker, overlength.

#### Exact scope

Before customer-visible answer is persisted, ensure required evidence exists, citation IDs subset actual retrieved chunks, internal-only metadata absent and response bounded. Knowledge requests with zero evidence cannot pass unless approved deterministic path. Test real/fake citations, zero evidence, internal marker, overlength.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `apps/customer-web/src/app/page.tsx` — current customer-web entry surface.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Before customer-visible answer is persisted, ensure required evidence exists, citation IDs subset actual retrieved chunks, internal-only metadata absent and response bounded. Knowledge requests with zero evidence cannot pass unless approved deterministic path. Test real/fake citations, zero evidence, internal marker, overlength.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

proved deterministic path. Test real/fake citations, zero evidence, internal marker, overlength.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1609-implement-end-to-end-knowledge-question-state-path"></a>

### V1.6.09 — Implement end-to-end knowledge-question state path

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Wire explicit states: classify knowledge -> retrieval -> generation -> guard -> persist AI message -> publish safe SSE -> complete run.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Wire explicit states: classify knowledge -> retrieval -> generation -> guard -> persist AI message -> publish safe SSE -> complete run.

#### Frozen source requirements

Wire explicit states: classify knowledge -> retrieval -> generation -> guard -> persist AI message -> publish safe SSE -> complete run. No unconstrained loop. Test success, no evidence, provider fallback, budget exhaustion, guard failure. Every run ends completed/escalated/failed.

#### Exact scope

Wire explicit states: classify knowledge -> retrieval -> generation -> guard -> persist AI message -> publish safe SSE -> complete run. No unconstrained loop. Test success, no evidence, provider fallback, budget exhaustion, guard failure. Every run ends completed/escalated/failed.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Wire explicit states: classify knowledge -> retrieval -> generation -> guard -> persist AI message -> publish safe SSE -> complete run. No unconstrained loop. Test success, no evidence, provider fallback, budget exhaustion, guard failure. Every run ends completed/escalated/failed.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test success, no evidence, provider fallback, budget exhaustion, guard failure.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: AI, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1701-create-toolpolicyexecutionconfirmationapproval-migration"></a>

### V1.7.01 — Create tool/policy/execution/confirmation/approval migration

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Create `tools`, `tool_versions`, `policies`, `policy_versions`, `tool_executions`, `action_confirmations`, `approvals` with exact statuses/indexes/uniques.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Create `tools`, `tool_versions`, `policies`, `policy_versions`, `tool_executions`, `action_confirmations`, `approvals` with exact statuses/indexes/uniques. Test duplicate tenant idempotency, one confirmation/approval per execution, invalid risk/status, downgrade.

#### Exact scope

Create `tools`, `tool_versions`, `policies`, `policy_versions`, `tool_executions`, `action_confirmations`, `approvals` with exact statuses/indexes/uniques. Test duplicate tenant idempotency, one confirmation/approval per execution, invalid risk/status, downgrade.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Create `tools`, `tool_versions`, `policies`, `policy_versions`, `tool_executions`, `action_confirmations`, `approvals` with exact statuses/indexes/uniques. Test duplicate tenant idempotency, one confirmation/approval per execution, invalid risk/status, downgrade.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test duplicate tenant idempotency, one confirmation/approval per execution, invalid risk/status, downgrade.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1702-implement-typed-tool-registry-json-schema-validation"></a>

### V1.7.02 — Implement typed tool registry + JSON Schema validation

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Resolve active tenant tool/version and strictly validate C-5 model arguments against frozen input schema, rejecting unknown fields.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Resolve active tenant tool/version and strictly validate C-5 model arguments against frozen input schema, rejecting unknown fields. Return normalized validated proposal or `TOOL_ARGUMENT_VALIDATION_FAILED`. Test missing/extra/wrong-type, inactive, wrong tenant.

#### Exact scope

Resolve active tenant tool/version and strictly validate C-5 model arguments against frozen input schema, rejecting unknown fields. Return normalized validated proposal or `TOOL_ARGUMENT_VALIDATION_FAILED`. Test missing/extra/wrong-type, inactive, wrong tenant.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Resolve active tenant tool/version and strictly validate C-5 model arguments against frozen input schema, rejecting unknown fields. Return normalized validated proposal or `TOOL_ARGUMENT_VALIDATION_FAILED`. Test missing/extra/wrong-type, inactive, wrong tenant.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test missing/extra/wrong-type, inactive, wrong tenant.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1703-implement-policy-engine-c-6"></a>

### V1.7.03 — Implement policy engine C-6

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Load correct versioned policy and deterministically return `allow|deny|require_confirmation|require_human_approval`.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Load correct versioned policy and deterministically return `allow|deny|require_confirmation|require_human_approval`.

#### Frozen source requirements

Load correct versioned policy and deterministically return `allow|deny|require_confirmation|require_human_approval`. Missing/malformed mutation policy must deny. Test read allow, missing mutation deny, confirmation, approval, malformed rules, anonymous blocked. Premium security review.

#### Exact scope

Load correct versioned policy and deterministically return `allow|deny|require_confirmation|require_human_approval`. Missing/malformed mutation policy must deny. Test read allow, missing mutation deny, confirmation, approval, malformed rules, anonymous blocked. Premium security review.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test read allow, missing mutation deny, confirmation, approval, malformed rules, anonymous blocked.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Security, Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v1704-implement-selected-synthetic-read-only-demo-tool"></a>

### V1.7.04 — Implement selected synthetic read-only demo tool

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Using exact V1.0.01 demo decision, implement first private status lookup against seeded synthetic data.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Using exact V1.0.01 demo decision, implement first private status lookup against seeded synthetic data.

#### Frozen source requirements

Using exact V1.0.01 demo decision, implement first private status lookup against seeded synthetic data. Validate customer ownership and normalize output. Test success, other customer denied, missing, timeout fixture, invalid args. Never use real private company data.

#### Exact scope

Using exact V1.0.01 demo decision, implement first private status lookup against seeded synthetic data. Validate customer ownership and normalize output. Test success, other customer denied, missing, timeout fixture, invalid args. Never use real private company data.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test success, other customer denied, missing, timeout fixture, invalid args.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1705-implement-selected-synthetic-protected-mutation-tool"></a>

### V1.7.05 — Implement selected synthetic protected mutation tool

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Implement one exact state-changing demo action from V1.0.01 with preconditions, tenant/customer ownership, idempotency, external_operation_ref and deterministic ambiguous-outcome simulation.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Implement one exact state-changing demo action from V1.0.01 with preconditions, tenant/customer ownership, idempotency, external_operation_ref and deterministic ambiguous-outcome simulation.

#### Frozen source requirements

Implement one exact state-changing demo action from V1.0.01 with preconditions, tenant/customer ownership, idempotency, external_operation_ref and deterministic ambiguous-outcome simulation. Duplicate idempotency returns original outcome. Unknown outcome enters reconciliation, never blind retry.

#### Exact scope

Implement one exact state-changing demo action from V1.0.01 with preconditions, tenant/customer ownership, idempotency, external_operation_ref and deterministic ambiguous-outcome simulation. Duplicate idempotency returns original outcome. Unknown outcome enters reconciliation, never blind retry.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Foundation, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1706-implement-customer-confirmation-lifecycle"></a>

### V1.7.06 — Implement customer confirmation lifecycle

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Create pending confirmation when C-6 requires it; implement confirm/decline routes, expiry, ownership, one terminal decision, safe SSE event.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Create pending confirmation when C-6 requires it; implement confirm/decline routes, expiry, ownership, one terminal decision, safe SSE event.

#### Frozen source requirements

Create pending confirmation when C-6 requires it; implement confirm/decline routes, expiry, ownership, one terminal decision, safe SSE event. Test confirm, decline, expired, wrong customer, concurrent double-submit.

#### Exact scope

Create pending confirmation when C-6 requires it; implement confirm/decline routes, expiry, ownership, one terminal decision, safe SSE event. Test confirm, decline, expired, wrong customer, concurrent double-submit.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `apps/customer-web/src/app/page.tsx` — current customer-web entry surface.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test confirm, decline, expired, wrong customer, concurrent double-submit.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1707-implement-human-approval-lifecycle-and-c-7-event"></a>

### V1.7.07 — Implement human approval lifecycle and C-7 event

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Create pending approval, emit exact C-7, expose list/get/approve/reject APIs, enforce approver capability/tenant/expiry and atomic terminal decision.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Create pending approval, emit exact C-7, expose list/get/approve/reject APIs, enforce approver capability/tenant/expiry and atomic terminal decision. Test approve/reject/expired/wrong tenant/no capability/concurrent decision.

#### Exact scope

Create pending approval, emit exact C-7, expose list/get/approve/reject APIs, enforce approver capability/tenant/expiry and atomic terminal decision. Test approve/reject/expired/wrong tenant/no capability/concurrent decision.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

prove/reject APIs, enforce approver capability/tenant/expiry and atomic terminal decision. Test approve/reject/expired/wrong tenant/no capability/concurrent decision.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1708-implement-policy-enforced-tool-execution-coordinator"></a>

### V1.7.08 — Implement policy-enforced tool execution coordinator

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Only execution path: C-5 validated proposal -> create tool_execution -> C-6 policy -> optional confirmation/approval -> idempotent adapter execution -> result verification/audit.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Only execution path: C-5 validated proposal -> create tool_execution -> C-6 policy -> optional confirmation/approval -> idempotent adapter execution -> result verification/audit. Blocked tools never call adapter. Unknown mutation outcome becomes reconciliation. Test all four policy outcomes, duplicate idempotency, failure, ambiguous result.

#### Exact scope

Only execution path: C-5 validated proposal -> create tool_execution -> C-6 policy -> optional confirmation/approval -> idempotent adapter execution -> result verification/audit. Blocked tools never call adapter. Unknown mutation outcome becomes reconciliation. Test all four policy outcomes, duplicate idempotency, failure, ambiguous result.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test all four policy outcomes, duplicate idempotency, failure, ambiguous result.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1709-implement-transactional-agent-state-path"></a>

### V1.7.09 — Implement transactional agent state path

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Orchestrator path: generate strict tool proposal -> validate C-5 -> coordinator -> pending confirmation/approval or result -> safe response.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Orchestrator path: generate strict tool proposal -> validate C-5 -> coordinator -> pending confirmation/approval or result -> safe response. Test read success, confirmation, approval, deny, tool failure, malformed args. Model never calls adapter directly.

#### Exact scope

Orchestrator path: generate strict tool proposal -> validate C-5 -> coordinator -> pending confirmation/approval or result -> safe response. Test read success, confirmation, approval, deny, tool failure, malformed args. Model never calls adapter directly.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test read success, confirmation, approval, deny, tool failure, malformed args.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1801-create-support-queueescalationinternal-note-migration"></a>

### V1.8.01 — Create support queue/escalation/internal-note migration

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Create `support_queues`, `escalations`, `internal_notes` with exact priorities/statuses/SLA/indexes.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Create `support_queues`, `escalations`, `internal_notes` with exact priorities/statuses/SLA/indexes.

#### Frozen source requirements

Create `support_queues`, `escalations`, `internal_notes` with exact priorities/statuses/SLA/indexes. Add up/down and constraint tests.

#### Exact scope

Create `support_queues`, `escalations`, `internal_notes` with exact priorities/statuses/SLA/indexes. Add up/down and constraint tests.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Create `support_queues`, `escalations`, `internal_notes` with exact priorities/statuses/SLA/indexes. Add up/down and constraint tests.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1802-create-default-support-queue-for-new-tenant"></a>

### V1.8.02 — Create default support queue for new tenant

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Ensure each tenant has exactly one `Default` support queue with approved SLA during tenant creation or frozen lifecycle hook.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Ensure each tenant has exactly one `Default` support queue with approved SLA during tenant creation or frozen lifecycle hook.

#### Frozen source requirements

Ensure each tenant has exactly one `Default` support queue with approved SLA during tenant creation or frozen lifecycle hook. Test creation, idempotency, rollback. No custom routing yet.

#### Exact scope

Ensure each tenant has exactly one `Default` support queue with approved SLA during tenant creation or frozen lifecycle hook. Test creation, idempotency, rollback. No custom routing yet.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

proved SLA during tenant creation or frozen lifecycle hook. Test creation, idempotency, rollback.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v1803-implement-escalation-service-c-8"></a>

### V1.8.03 — Implement escalation service C-8

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Validate evidence/tool IDs, choose queue, create handoff summary, set conversation `escalated` + human ownership, create escalation and outbox event atomically.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Validate evidence/tool IDs, choose queue, create handoff summary, set conversation `escalated` + human ownership, create escalation and outbox event atomically.

#### Frozen source requirements

Validate evidence/tool IDs, choose queue, create handoff summary, set conversation `escalated` + human ownership, create escalation and outbox event atomically. Repeated active escalation returns existing case. Test reason codes, duplicate, foreign IDs, rollback.

#### Exact scope

Validate evidence/tool IDs, choose queue, create handoff summary, set conversation `escalated` + human ownership, create escalation and outbox event atomically. Repeated active escalation returns existing case. Test reason codes, duplicate, foreign IDs, rollback.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test reason codes, duplicate, foreign IDs, rollback.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1804-implement-support-queueescalation-listdetailassign-apis"></a>

### V1.8.04 — Implement support queue/escalation list/detail/assign APIs

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Build architecture support routes with pagination/filtering/SLA, queue/object permissions, active tenant-member assignee validation and atomic assign/reassign.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Build architecture support routes with pagination/filtering/SLA, queue/object permissions, active tenant-member assignee validation and atomic assign/reassign.

#### Frozen source requirements

Build architecture support routes with pagination/filtering/SLA, queue/object permissions, active tenant-member assignee validation and atomic assign/reassign. Test assignment, invalid assignee, wrong tenant, concurrent assignment, role scopes.

#### Exact scope

Build architecture support routes with pagination/filtering/SLA, queue/object permissions, active tenant-member assignee validation and atomic assign/reassign. Test assignment, invalid assignee, wrong tenant, concurrent assignment, role scopes.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test assignment, invalid assignee, wrong tenant, concurrent assignment, role scopes.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1805-implement-human-messageinternal-noteresolvereopen-apis"></a>

### V1.8.05 — Implement human message/internal note/resolve/reopen APIs

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Human message becomes customer-visible message; internal note separate and never serialized to customer APIs/SSE; resolve/reopen update escalation/conversation and emit safe public events.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Human message becomes customer-visible message; internal note separate and never serialized to customer APIs/SSE; resolve/reopen update escalation/conversation and emit safe public events.

#### Frozen source requirements

Human message becomes customer-visible message; internal note separate and never serialized to customer APIs/SSE; resolve/reopen update escalation/conversation and emit safe public events. Test visibility isolation, unauthorized agent, resolve/reopen, stale conflict.

#### Exact scope

Human message becomes customer-visible message; internal note separate and never serialized to customer APIs/SSE; resolve/reopen update escalation/conversation and emit safe public events. Test visibility isolation, unauthorized agent, resolve/reopen, stale conflict.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test visibility isolation, unauthorized agent, resolve/reopen, stale conflict.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: AI, Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1901-build-shared-serviq-design-tokens-and-base-ui-primitives"></a>

### V1.9.01 — Build shared Serviq design tokens and base UI primitives

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Implement tokens plus Button, Input, Textarea, Select, Dialog, Toast, Skeleton, EmptyState, ErrorState, PermissionDenied and StatusBadge in `packages/ui`, with Storybook/accessibility tests.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Implement tokens plus Button, Input, Textarea, Select, Dialog, Toast, Skeleton, EmptyState, ErrorState, PermissionDenied and StatusBadge in `packages/ui`, with Storybook/accessibility tests.

#### Frozen source requirements

Implement tokens plus Button, Input, Textarea, Select, Dialog, Toast, Skeleton, EmptyState, ErrorState, PermissionDenied and StatusBadge in `packages/ui`, with Storybook/accessibility tests. Cover focus/disabled/error/loading. Keyboard-operable, no color-only status, no business logic.

#### Exact scope

Implement tokens plus Button, Input, Textarea, Select, Dialog, Toast, Skeleton, EmptyState, ErrorState, PermissionDenied and StatusBadge in `packages/ui`, with Storybook/accessibility tests. Cover focus/disabled/error/loading. Keyboard-operable, no color-only status, no business logic.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

Needs Architect Decision: freeze loading, empty, permission-denied, validation-error, dependency-error, success, and retry states with exact user-visible copy and accessibility behavior before builder handoff.

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Frontend, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1902-build-authenticated-client-console-application-shell"></a>

### V1.9.02 — Build authenticated client-console application shell

> **2026-09-12 dependency correction:** Explicit prerequisite: V1.1.16 must establish the real server session and trusted API context. The existing OIDC validator and injected-principal API tests are not an authenticated application shell.

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Responsive sidebar/header/layout, active route, mobile drawer, tenant display/switcher shell, permission-aware nav using backend session/membership.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Responsive sidebar/header/layout, active route, mobile drawer, tenant display/switcher shell, permission-aware nav using backend session/membership.

#### Frozen source requirements

Responsive sidebar/header/layout, active route, mobile drawer, tenant display/switcher shell, permission-aware nav using backend session/membership. Cover loading/no-memberships/auth-error/success. Hidden nav is not authorization. No horizontal scroll at 375px.

#### Exact scope

Responsive sidebar/header/layout, active route, mobile drawer, tenant display/switcher shell, permission-aware nav using backend session/membership. Cover loading/no-memberships/auth-error/success. Hidden nav is not authorization. No horizontal scroll at 375px.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

Needs Architect Decision: freeze loading, empty, permission-denied, validation-error, dependency-error, success, and retry states with exact user-visible copy and accessibility behavior before builder handoff.

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Frontend, Serviq: Security, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1903-build-organization-onboarding-checklist-ui"></a>

### V1.9.03 — Build organization onboarding checklist UI

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Render provider, knowledge, tool/policy, sandbox/evaluation and publish readiness from backend.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Render provider, knowledge, tool/policy, sandbox/evaluation and publish readiness from backend.

#### Frozen source requirements

Render provider, knowledge, tool/policy, sandbox/evaluation and publish readiness from backend. Each step incomplete/complete/error/retry and links to page. Never mark complete from local state alone. Test 0%, partial, ready, permission, dependency error.

#### Exact scope

Render provider, knowledge, tool/policy, sandbox/evaluation and publish readiness from backend. Each step incomplete/complete/error/retry and links to page. Never mark complete from local state alone. Test 0%, partial, ready, permission, dependency error.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

Needs Architect Decision: freeze loading, empty, permission-denied, validation-error, dependency-error, success, and retry states with exact user-visible copy and accessibility behavior before builder handoff.

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test 0%, partial, ready, permission, dependency error.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Frontend, Serviq: AI, Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1904-build-providermodel-management-ui"></a>

### V1.9.04 — Build provider/model management UI

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Provider list/add/test/replace/delete and model alias list/create/update.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Provider list/add/test/replace/delete and model alias list/create/update.

#### Frozen source requirements

Provider list/add/test/replace/delete and model alias list/create/update. Secret field never repopulated or retained after save. Cover testing pending/success/failure, permission, validation and key absence from DOM/client cache after save.

#### Exact scope

Provider list/add/test/replace/delete and model alias list/create/update. Secret field never repopulated or retained after save. Cover testing pending/success/failure, permission, validation and key absence from DOM/client cache after save.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

Needs Architect Decision: freeze loading, empty, permission-denied, validation-error, dependency-error, success, and retry states with exact user-visible copy and accessibility behavior before builder handoff.

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

test/replace/delete and model alias list/create/update. testing pending/success/failure, permission, validation and key absence from DOM/client cache after save.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Frontend, Serviq: AI, Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1905-build-knowledge-source-management-ui"></a>

### V1.9.05 — Build knowledge source management UI

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

URL/file forms, source table/statuses, sync/retry/disable actions, document list/detail provenance.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

URL/file forms, source table/statuses, sync/retry/disable actions, document list/detail provenance.

#### Frozen source requirements

URL/file forms, source table/statuses, sync/retry/disable actions, document list/detail provenance. Mirror server validation; server authoritative. Cover empty/syncing/ready/failed/disabled/permission.

#### Exact scope

URL/file forms, source table/statuses, sync/retry/disable actions, document list/detail provenance. Mirror server validation; server authoritative. Cover empty/syncing/ready/failed/disabled/permission.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

URL/file forms, source table/statuses, sync/retry/disable actions, document list/detail provenance. Mirror server validation; server authoritative. Cover empty/syncing/ready/failed/disabled/permission.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

Needs Architect Decision: freeze loading, empty, permission-denied, validation-error, dependency-error, success, and retry states with exact user-visible copy and accessibility behavior before builder handoff.

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

provenance.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Re-resolve and revalidate every outbound destination and redirect; block private, loopback, link-local, metadata, multicast, reserved, and disallowed targets.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Frontend, Serviq: Security, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v1906-build-retrieval-debugger-ui"></a>

### V1.9.06 — Build retrieval debugger UI

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Query form, permitted topK/source/access controls, ranked results with score/title/source/location, empty and infrastructure-error states.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Query form, permitted topK/source/access controls, ranked results with score/title/source/location, empty and infrastructure-error states.

#### Frozen source requirements

Query form, permitted topK/source/access controls, ranked results with score/title/source/location, empty and infrastructure-error states. Customer users cannot access. Browser does not perform ranking.

#### Exact scope

Query form, permitted topK/source/access controls, ranked results with score/title/source/location, empty and infrastructure-error states. Customer users cannot access. Browser does not perform ranking.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

Needs Architect Decision: freeze loading, empty, permission-denied, validation-error, dependency-error, success, and retry states with exact user-visible copy and accessibility behavior before builder handoff.

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Frontend, Serviq: AI, Serviq: Infrastructure, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1907-build-customer-support-chat-shell-and-history"></a>

### V1.9.07 — Build customer support chat shell and history

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Support bootstrap, conversation create/resume, ordered history, responsive composer, send pending/error/retry, preserve failed draft.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Support bootstrap, conversation create/resume, ordered history, responsive composer, send pending/error/retry, preserve failed draft.

#### Frozen source requirements

Support bootstrap, conversation create/resume, ordered history, responsive composer, send pending/error/retry, preserve failed draft. Do not send tenant/customer IDs from arbitrary browser state. Test new/resume/send/404 tenant/mobile keyboard.

#### Exact scope

Support bootstrap, conversation create/resume, ordered history, responsive composer, send pending/error/retry, preserve failed draft. Do not send tenant/customer IDs from arbitrary browser state. Test new/resume/send/404 tenant/mobile keyboard.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `apps/customer-web/src/app/page.tsx` — current customer-web entry surface.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

Needs Architect Decision: freeze loading, empty, permission-denied, validation-error, dependency-error, success, and retry states with exact user-visible copy and accessibility behavior before builder handoff.

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test new/resume/send/404 tenant/mobile keyboard.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1908-add-customer-sse-streaming-citations-confirmation-approval-cards"></a>

### V1.9.08 — Add customer SSE streaming, citations, confirmation, approval cards

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Subscribe exact C-12 events, dedupe eventId, render message deltas/completion, SourceCitation, ActionConfirmationCard, ApprovalStatusCard, bounded reconnect and recoverable errors.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Subscribe exact C-12 events, dedupe eventId, render message deltas/completion, SourceCitation, ActionConfirmationCard, ApprovalStatusCard, bounded reconnect and recoverable errors.

#### Frozen source requirements

Subscribe exact C-12 events, dedupe eventId, render message deltas/completion, SourceCitation, ActionConfirmationCard, ApprovalStatusCard, bounded reconnect and recoverable errors. Unknown/internal events ignored and telemetered. Prevent double confirmation.

#### Exact scope

Subscribe exact C-12 events, dedupe eventId, render message deltas/completion, SourceCitation, ActionConfirmationCard, ApprovalStatusCard, bounded reconnect and recoverable errors. Unknown/internal events ignored and telemetered. Prevent double confirmation.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `apps/customer-web/src/app/page.tsx` — current customer-web entry surface.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Foundation, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1909-build-client-conversation-review-listdetail"></a>

### V1.9.09 — Build client conversation review list/detail

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Paginated/filterable tenant conversation list and safe timeline with allowed run metadata.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Paginated/filterable tenant conversation list and safe timeline with allowed run metadata.

#### Frozen source requirements

Paginated/filterable tenant conversation list and safe timeline with allowed run metadata. Exclude raw prompts, chain-of-thought, secrets and tool internals. Cover loading/empty/error/permission/cross-tenant 404.

#### Exact scope

Paginated/filterable tenant conversation list and safe timeline with allowed run metadata. Exclude raw prompts, chain-of-thought, secrets and tool internals. Cover loading/empty/error/permission/cross-tenant 404.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1910-build-human-support-inbox-and-case-workspace"></a>

### V1.9.10 — Build human support inbox and case workspace

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Queues/case list, filters, assignment/SLA, handoff summary, evidence, tool history, approvals, customer context, internal notes, human response composer, resolve/reassign/reopen.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Queues/case list, filters, assignment/SLA, handoff summary, evidence, tool history, approvals, customer context, internal notes, human response composer, resolve/reassign/reopen.

#### Frozen source requirements

Queues/case list, filters, assignment/SLA, handoff summary, evidence, tool history, approvals, customer context, internal notes, human response composer, resolve/reassign/reopen. Internal notes visually distinct and never customer API. Test stale conflicts/permissions.

#### Exact scope

Queues/case list, filters, assignment/SLA, handoff summary, evidence, tool history, approvals, customer context, internal notes, human response composer, resolve/reassign/reopen. Internal notes visually distinct and never customer API. Test stale conflicts/permissions.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

Needs Architect Decision: freeze loading, empty, permission-denied, validation-error, dependency-error, success, and retry states with exact user-visible copy and accessibility behavior before builder handoff.

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test stale conflicts/permissions.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Frontend, Serviq: Backend, Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1911-build-agent-configurationversionpublish-ui"></a>

### V1.9.11 — Build agent configuration/version/publish UI

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Agent list/version tabs, draft config, primary/fallback models, budgets, retrieval/citation/tool/policy refs, evaluate, publish, rollback.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Agent list/version tabs, draft config, primary/fallback models, budgets, retrieval/citation/tool/policy refs, evaluate, publish, rollback.

#### Frozen source requirements

Agent list/version tabs, draft config, primary/fallback models, budgets, retrieval/citation/tool/policy refs, evaluate, publish, rollback. Published version immutable. Client mirrors hard limits. Test rejection/evaluation/publish/rollback.

#### Exact scope

Agent list/version tabs, draft config, primary/fallback models, budgets, retrieval/citation/tool/policy refs, evaluate, publish, rollback. Published version immutable. Client mirrors hard limits. Test rejection/evaluation/publish/rollback.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

Needs Architect Decision: freeze loading, empty, permission-denied, validation-error, dependency-error, success, and retry states with exact user-visible copy and accessibility behavior before builder handoff.

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test rejection/evaluation/publish/rollback.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Frontend, Serviq: AI, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v1912-build-teamaccess-management-ui"></a>

### V1.9.12 — Build team/access management UI

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

List active/suspended members and pending invitations; create invite with roles; one-time URL; revoke; update roles/suspend; protect last-owner conflict.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

List active/suspended members and pending invitations; create invite with roles; one-time URL; revoke; update roles/suspend; protect last-owner conflict.

#### Frozen source requirements

List active/suspended members and pending invitations; create invite with roles; one-time URL; revoke; update roles/suspend; protect last-owner conflict. Cover denied/read-only and expired/revoked invitation states.

#### Exact scope

List active/suspended members and pending invitations; create invite with roles; one-time URL; revoke; update roles/suspend; protect last-owner conflict. Cover denied/read-only and expired/revoked invitation states.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

Needs Architect Decision: freeze loading, empty, permission-denied, validation-error, dependency-error, success, and retry states with exact user-visible copy and accessibility behavior before builder handoff.

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Re-resolve and revalidate every outbound destination and redirect; block private, loopback, link-local, metadata, multicast, reserved, and disallowed targets.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Frontend, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v11001-create-usageauditplatform-controlprivacy-migration"></a>

### V1.10.01 — Create usage/audit/platform-control/privacy migration

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Create `usage_events`, `audit_events`, `platform_feature_flags`, `rate_limit_policies`, `data_subject_requests` exactly from Architecture v1.2 and seed frozen V1 rate defaults.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Create `usage_events`, `audit_events`, `platform_feature_flags`, `rate_limit_policies`, `data_subject_requests` exactly from Architecture v1.2 and seed frozen V1 rate defaults. Test checks/uniques/up/down.

#### Exact scope

Create `usage_events`, `audit_events`, `platform_feature_flags`, `rate_limit_policies`, `data_subject_requests` exactly from Architecture v1.2 and seed frozen V1 rate defaults. Test checks/uniques/up/down.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Create `usage_events`, `audit_events`, `platform_feature_flags`, `rate_limit_policies`, `data_subject_requests` exactly from Architecture v1.2 and seed frozen V1 rate defaults. Test checks/uniques/up/down.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test checks/uniques/up/down.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: Security, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v11002-implement-immutable-audit-writer-query-api"></a>

### V1.10.02 — Implement immutable audit writer + query API

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Consume exact C-10, allowlist metadata, append-only audit rows, tenant list/detail filters.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Consume exact C-10, allowlist metadata, append-only audit rows, tenant list/detail filters. Reject secret-bearing metadata. Test duplicate idempotency, tenant isolation, permissions, provider-key redaction.

#### Exact scope

Consume exact C-10, allowlist metadata, append-only audit rows, tenant list/detail filters. Reject secret-bearing metadata. Test duplicate idempotency, tenant isolation, permissions, provider-key redaction.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Consume exact C-10, allowlist metadata, append-only audit rows, tenant list/detail filters. Reject secret-bearing metadata. Test duplicate idempotency, tenant isolation, permissions, provider-key redaction.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test duplicate idempotency, tenant isolation, permissions, provider-key redaction.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v11003-implement-usage-event-ingestion-overview-analytics-api"></a>

### V1.10.03 — Implement usage event ingestion + overview analytics API

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Consume C-11 idempotently and expose date-filtered conversation volume, AI resolution/containment, escalation, latency, provider/tool outcomes, retrieval outcomes and estimated model usage/cost.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Consume C-11 idempotently and expose date-filtered conversation volume, AI resolution/containment, escalation, latency, provider/tool outcomes, retrieval outcomes and estimated model usage/cost.

#### Frozen source requirements

Consume C-11 idempotently and expose date-filtered conversation volume, AI resolution/containment, escalation, latency, provider/tool outcomes, retrieval outcomes and estimated model usage/cost. Distinguish estimated vs measured. Test duplicates/date/tenant.

#### Exact scope

Consume C-11 idempotently and expose date-filtered conversation volume, AI resolution/containment, escalation, latency, provider/tool outcomes, retrieval outcomes and estimated model usage/cost. Distinguish estimated vs measured. Test duplicates/date/tenant.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test duplicates/date/tenant.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v11004-build-analytics-and-audit-client-console-screens"></a>

### V1.10.04 — Build analytics and audit client-console screens

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Date-filtered metrics with measured/estimated labels plus audit filters/detail.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Date-filtered metrics with measured/estimated labels plus audit filters/detail.

#### Frozen source requirements

Date-filtered metrics with measured/estimated labels plus audit filters/detail. API error must never display as numeric zero. Cover loading/empty/error/permission.

#### Exact scope

Date-filtered metrics with measured/estimated labels plus audit filters/detail. API error must never display as numeric zero. Cover loading/empty/error/permission.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

Needs Architect Decision: freeze loading, empty, permission-denied, validation-error, dependency-error, success, and retry states with exact user-visible copy and accessibility behavior before builder handoff.

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Frontend, Serviq: Backend, Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v11005-implement-platform-feature-flagrate-policy-apis-and-valkey-cache"></a>

### V1.10.05 — Implement platform feature-flag/rate-policy APIs and Valkey cache

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

PostgreSQL authoritative; Valkey caches config <=60s and runtime counters.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

PostgreSQL authoritative; Valkey caches config <=60s and runtime counters. GET/PATCH platform routes validate keys/hard ceilings and invalidate cache. Cache outage falls back to DB/frozen safe defaults, never unlimited. Platform operator only; premium review.

#### Exact scope

PostgreSQL authoritative; Valkey caches config <=60s and runtime counters. GET/PATCH platform routes validate keys/hard ceilings and invalidate cache. Cache outage falls back to DB/frozen safe defaults, never unlimited. Platform operator only; premium review.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

PostgreSQL authoritative; Valkey caches config <=60s and runtime counters. GET/PATCH platform routes validate keys/hard ceilings and invalidate cache. Cache outage falls back to DB/frozen safe defaults, never unlimited. Platform operator only; premium review.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v11006-implement-privacy-export-requestjob"></a>

### V1.10.06 — Implement privacy export request/job

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Verified customer creates export request, polls, receives short-lived signed download.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Verified customer creates export request, polls, receives short-lived signed download.

#### Frozen source requirements

Verified customer creates export request, polls, receives short-lived signed download. Job gathers only that customer’s exportable data, writes artifact, expires artifact <=7 days and URL <=24h. Test anonymous denied, cross-customer exclusion, object failure, cleanup.

#### Exact scope

Verified customer creates export request, polls, receives short-lived signed download. Job gathers only that customer’s exportable data, writes artifact, expires artifact <=7 days and URL <=24h. Test anonymous denied, cross-customer exclusion, object failure, cleanup.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test anonymous denied, cross-customer exclusion, object failure, cleanup.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Re-resolve and revalidate every outbound destination and redirect; block private, loopback, link-local, metadata, multicast, reserved, and disallowed targets.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v11007-implement-privacy-deletionpseudonymization-job"></a>

### V1.10.07 — Implement privacy deletion/pseudonymization job

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Verified customer requests deletion.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Verified customer requests deletion.

#### Frozen source requirements

Verified customer requests deletion. Idempotent worker applies architecture retention store-by-store, marks customer deleted, removes direct identifiers/content where required, pseudonymizes retained audit refs, records completion. Test restart, tenant isolation, retained pseudonymous audit, duplicate request. Premium privacy review.

#### Exact scope

Verified customer requests deletion. Idempotent worker applies architecture retention store-by-store, marks customer deleted, removes direct identifiers/content where required, pseudonymizes retained audit refs, records completion. Test restart, tenant isolation, retained pseudonymous audit, duplicate request. Premium privacy review.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `apps/customer-web/src/app/page.tsx` — current customer-web entry surface.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test restart, tenant isolation, retained pseudonymous audit, duplicate request.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: Security, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v11008-build-platform-operator-console-v1-screens"></a>

### V1.10.08 — Build platform-operator console V1 screens

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Tenants, system health, provider health, queues/jobs/DLQ, feature flags, rate limits, usage and security pages using only platform APIs.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Tenants, system health, provider health, queues/jobs/DLQ, feature flags, rate limits, usage and security pages using only platform APIs.

#### Frozen source requirements

Tenants, system health, provider health, queues/jobs/DLQ, feature flags, rate limits, usage and security pages using only platform APIs. Tenant roles denied. No arbitrary SQL/shell/secret display.

#### Exact scope

Tenants, system health, provider health, queues/jobs/DLQ, feature flags, rate limits, usage and security pages using only platform APIs. Tenant roles denied. No arbitrary SQL/shell/secret display.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `apps/platform-console/src/app/page.tsx` — separate platform-operator trust surface.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

Needs Architect Decision: freeze loading, empty, permission-denied, validation-error, dependency-error, success, and retry states with exact user-visible copy and accessibility behavior before builder handoff.

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Frontend, Serviq: Backend, Serviq: Security, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v11009-implement-dead-letter-listreplay-platform-apis"></a>

### V1.10.09 — Implement dead-letter list/replay platform APIs

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Safe platform-only list/detail/replay.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Safe platform-only list/detail/replay. Replay preserves original idempotency key and adds replay sequence; one active replay command per item. Test operator-only, duplicate replay, invalid schema, already-successful conflict.

#### Exact scope

Safe platform-only list/detail/replay. Replay preserves original idempotency key and adds replay sequence; one active replay command per item. Test operator-only, duplicate replay, invalid schema, already-successful conflict.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test operator-only, duplicate replay, invalid schema, already-successful conflict.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v11010-implement-retention-cleanup-scheduler"></a>

### V1.10.10 — Implement retention cleanup scheduler

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Durable scheduled jobs for expired invitations, confirmations/approvals, export artifacts, expired idempotency records and app-owned retention.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Durable scheduled jobs for expired invitations, confirmations/approvals, export artifacts, expired idempotency records and app-owned retention. Idempotent, bounded, metric-emitting. Do not delete outside frozen policy.

#### Exact scope

Durable scheduled jobs for expired invitations, confirmations/approvals, export artifacts, expired idempotency records and app-owned retention. Idempotent, bounded, metric-emitting. Do not delete outside frozen policy.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: Data, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v11101-add-end-to-end-opentelemetry-trace-propagation"></a>

### V1.11.01 — Add end-to-end OpenTelemetry trace propagation

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Instrument ingress/auth/conversation/agent/retrieval/provider/policy/tool/worker/outbox and propagate trace/correlation.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Instrument ingress/auth/conversation/agent/retrieval/provider/policy/tool/worker/outbox and propagate trace/correlation.

#### Frozen source requirements

Instrument ingress/auth/conversation/agent/retrieval/provider/policy/tool/worker/outbox and propagate trace/correlation. Exporter failure cannot break request. No raw prompt, secret, cookie or unrestricted tool payload in spans. Verify full trace in Tempo.

#### Exact scope

Instrument ingress/auth/conversation/agent/retrieval/provider/policy/tool/worker/outbox and propagate trace/correlation. Exporter failure cannot break request. No raw prompt, secret, cookie or unrestricted tool payload in spans. Verify full trace in Tempo.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Verify full trace in Tempo.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Security, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v11102-add-structured-json-logging-secretpii-redaction"></a>

### V1.11.02 — Add structured JSON logging + secret/PII redaction

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Standardize architecture log fields and redact Authorization, cookies, API keys, passwords, session tokens, provider secrets and raw prompts.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Standardize architecture log fields and redact Authorization, cookies, API keys, passwords, session tokens, provider secrets and raw prompts. Plant fake secrets in tests and assert absence.

#### Exact scope

Standardize architecture log fields and redact Authorization, cookies, API keys, passwords, session tokens, provider secrets and raw prompts. Plant fake secrets in tests and assert absence.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests and assert absence.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v11103-add-required-v1-10-scenario-e2e-suite"></a>

### V1.11.03 — Add required V1 10-scenario E2E suite

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Using fake LLM/synthetic demo data automate grounded FAQ, status lookup, protected mutation, confirmation, approval, tool failure, escalation, support takeover, analytics, audit.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Using fake LLM/synthetic demo data automate grounded FAQ, status lookup, protected mutation, confirmation, approval, tool failure, escalation, support takeover, analytics, audit.

#### Frozen source requirements

Using fake LLM/synthetic demo data automate grounded FAQ, status lookup, protected mutation, confirmation, approval, tool failure, escalation, support takeover, analytics, audit. Tag `portfolio-v1`; failure artifacts secret-safe.

#### Exact scope

Using fake LLM/synthetic demo data automate grounded FAQ, status lookup, protected mutation, confirmation, approval, tool failure, escalation, support takeover, analytics, audit. Tag `portfolio-v1`; failure artifacts secret-safe.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: AI, Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v11104-add-adversarial-tenant-isolation-api-suite"></a>

### V1.11.04 — Add adversarial tenant-isolation API suite

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Seed tenant A/B with overlapping data and attack providers, knowledge, conversations, tools, approvals, support, analytics and audit across tenants.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Seed tenant A/B with overlapping data and attack providers, knowledge, conversations, tools, approvals, support, analytics and audit across tenants.

#### Frozen source requirements

Seed tenant A/B with overlapping data and attack providers, knowledge, conversations, tools, approvals, support, analytics and audit across tenants. Expected 403/404. Any cross-tenant data/side effect release blocker.

#### Exact scope

Seed tenant A/B with overlapping data and attack providers, knowledge, conversations, tools, approvals, support, analytics and audit across tenants. Expected 403/404. Any cross-tenant data/side effect release blocker.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v11105-add-baseline-k6-restsse-load-scenarios"></a>

### V1.11.05 — Add baseline k6 REST/SSE load scenarios

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Reproducible non-LLM REST, conversation/message fake-agent path, cached deterministic path and concurrent SSE.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Reproducible non-LLM REST, conversation/message fake-agent path, cached deterministic path and concurrent SSE.

#### Frozen source requirements

Reproducible non-LLM REST, conversation/message fake-agent path, cached deterministic path and concurrent SSE. Report commit, hardware, dataset, duration, concurrency/RPS, p50/p95/p99, errors, fake/real provider flag.

#### Exact scope

Reproducible non-LLM REST, conversation/message fake-agent path, cached deterministic path and concurrent SSE. Report commit, hardware, dataset, duration, concurrency/RPS, p50/p95/p99, errors, fake/real provider flag.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: AI, Serviq: Infrastructure, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v11106-implement-outbound-webhook-hmac-delivery-with-ssrf-protections"></a>

### V1.11.06 — Implement outbound webhook HMAC delivery with SSRF protections

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

CCR-001: production HTTPS/443 only, no credentials/fragments, block private/loopback/link-local/metadata/reserved, DNS check save+delivery, peer validation, redirects disabled, 5s connect/10s total, 64KiB response cap, HMAC, bounded retry/DLQ.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

CCR-001: production HTTPS/443 only, no credentials/fragments, block private/loopback/link-local/metadata/reserved, DNS check save+delivery, peer validation, redirects disabled, 5s connect/10s total, 64KiB response cap, HMAC, bounded retry/DLQ. Premium security review.

#### Exact scope

CCR-001: production HTTPS/443 only, no credentials/fragments, block private/loopback/link-local/metadata/reserved, DNS check save+delivery, peer validation, redirects disabled, 5s connect/10s total, 64KiB response cap, HMAC, bounded retry/DLQ. Premium security review.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Re-resolve and revalidate every outbound destination and redirect; block private, loopback, link-local, metadata, multicast, reserved, and disallowed targets.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v11107-add-release-securityquality-ci-gate"></a>

### V1.11.07 — Add release security/quality CI gate

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Block release on lint, typecheck, unit, integration, migration up/down, API contracts, tenant isolation, security/secret scans, E2E and docs/link checks.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Block release on lint, typecheck, unit, integration, migration up/down, API contracts, tenant isolation, security/secret scans, E2E and docs/link checks.

#### Frozen source requirements

Block release on lint, typecheck, unit, integration, migration up/down, API contracts, tenant isolation, security/secret scans, E2E and docs/link checks. Required jobs no `continue-on-error`. Safe failure reports.

#### Exact scope

Block release on lint, typecheck, unit, integration, migration up/down, API contracts, tenant isolation, security/secret scans, E2E and docs/link checks. Required jobs no `continue-on-error`. Safe failure reports.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Backend, Serviq: Security, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v11108-publish-v1-benchmark-evidence-report"></a>

### V1.11.08 — Publish V1 benchmark evidence report

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Write `docs/benchmarks/v1-baseline.md` with exact command, commit, infrastructure, data size, duration, RPS/concurrency, p50/p95/p99, error, bottlenecks and fake/real model.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Write `docs/benchmarks/v1-baseline.md` with exact command, commit, infrastructure, data size, duration, RPS/concurrency, p50/p95/p99, error, bottlenecks and fake/real model. Never claim 10M from architecture.

#### Exact scope

Write `docs/benchmarks/v1-baseline.md` with exact command, commit, infrastructure, data size, duration, RPS/concurrency, p50/p95/p99, error, bottlenecks and fake/real model. Never claim 10M from architecture.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: AI, Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v11109-complete-v1-public-demo-readme-and-non-affiliation-disclosure"></a>

### V1.11.09 — Complete V1 public-demo README and non-affiliation disclosure

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Product description, architecture links, real public support-corpus disclosure, synthetic private-data statement, non-affiliation disclaimer, verified local setup, demo flows, screenshots, benchmark link and honest limitations.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Product description, architecture links, real public support-corpus disclosure, synthetic private-data statement, non-affiliation disclaimer, verified local setup, demo flows, screenshots, benchmark link and honest limitations.

#### Frozen source requirements

Product description, architecture links, real public support-corpus disclosure, synthetic private-data statement, non-affiliation disclaimer, verified local setup, demo flows, screenshots, benchmark link and honest limitations. Do not publish copyrighted corpus copies.

#### Exact scope

Product description, architecture links, real public support-corpus disclosure, synthetic private-data statement, non-affiliation disclaimer, verified local setup, demo flows, screenshots, benchmark link and honest limitations. Do not publish copyrighted corpus copies.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v11110-run-v1-release-readiness-review-and-create-fix-only-backlog"></a>

### V1.11.10 — Run V1 release readiness review and create fix-only backlog

> **Milestone:** Phase 1 — V1 Production Foundation
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT — full Linear structure supplied; architect intake is still required before builder handoff.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Use `product_quality_bar.md` and `code_review.md`.

#### Why this exists

This item closes one bounded part of the remaining V1 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Use `product_quality_bar.md` and `code_review.md`. Record Critical/High/Medium findings with exact files/contracts and create micro-fix tasks. Do not broaden into V2. V1 ready only when release blockers and 10 E2E scenarios green.

#### Exact scope

Use `product_quality_bar.md` and `code_review.md`. Record Critical/High/Medium findings with exact files/contracts and create micro-fix tasks. Do not broaden into V2. V1 ready only when release blockers and 10 E2E scenarios green.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 1 — V1 Production Foundation
- **Labels:** Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** Current V1 foundations and every explicitly named predecessor must be merged and acceptance-verified.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

## Phase 2 — V2 Integrations & Omnichannel Scale

<a id="v2001-run-post-v1-repository-audit-for-v2"></a>

### V2.0.01 — Run post-V1 repository audit for V2

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT/BLOCKED — phase-gate planning ticket; start only after the predecessor release evidence named below exists.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Re-run Premium Product Builder `repo_audit.md`; update `docs/repo_context.md` with actual V1 paths, versions, service boundaries, tests, auth, deployment baseline and known constraints.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Re-run Premium Product Builder `repo_audit.md`; update `docs/repo_context.md` with actual V1 paths, versions, service boundaries, tests, auth, deployment baseline and known constraints.

#### Frozen source requirements

Re-run Premium Product Builder `repo_audit.md`; update `docs/repo_context.md` with actual V1 paths, versions, service boundaries, tests, auth, deployment baseline and known constraints. Acceptance: later V2 tickets reference current paths.

#### Exact scope

Re-run Premium Product Builder `repo_audit.md`; update `docs/repo_context.md` with actual V1 paths, versions, service boundaries, tests, auth, deployment baseline and known constraints. Acceptance: later V2 tickets reference current paths.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Re-run Premium Product Builder `repo_audit.md`; update `docs/repo_context.md` with actual V1 paths, versions, service boundaries, tests, auth, deployment baseline and known constraints. Acceptance: later V2 tickets reference current paths.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V1 release-readiness evidence must exist before this audit starts.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests, auth, deployment baseline and known constraints.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] later V2 tickets reference current paths.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Backend, Serviq: Security, Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V1 release-readiness evidence must exist before this audit starts.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2002-write-v2-prd-delta"></a>

### V2.0.02 — Write V2 PRD delta

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT/BLOCKED — phase-gate planning ticket; start only after the predecessor release evidence named below exists.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Create scoped PRD delta for integrations/channels/single-region scale.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Create scoped PRD delta for integrations/channels/single-region scale.

#### Frozen source requirements

Create scoped PRD delta for integrations/channels/single-region scale. Freeze vendors/channels, success metrics, non-goals, privacy/security impacts and migration compatibility. No builder ambiguity.

#### Exact scope

Create scoped PRD delta for integrations/channels/single-region scale. Freeze vendors/channels, success metrics, non-goals, privacy/security impacts and migration compatibility. No builder ambiguity.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01 must be complete and its repository audit accepted.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Backend, Serviq: Security, Serviq: Data, Serviq: Infrastructure, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01 must be complete and its repository audit accepted.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2003-write-v2-architectureccr-delta"></a>

### V2.0.03 — Write V2 architecture/CCR delta

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT/BLOCKED — phase-gate planning ticket; start only after the predecessor release evidence named below exists.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Freeze V2 API/event/schema/shared-type additions and compatibility plan, including integration adapter, channel envelope, webhook/callback auth, provider-routing cache and scaled deployment boundaries.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Freeze V2 API/event/schema/shared-type additions and compatibility plan, including integration adapter, channel envelope, webhook/callback auth, provider-routing cache and scaled deployment boundaries.

#### Exact scope

Freeze V2 API/event/schema/shared-type additions and compatibility plan, including integration adapter, channel envelope, webhook/callback auth, provider-routing cache and scaled deployment boundaries.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01 and V2.0.02 must be complete and accepted.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Re-resolve and revalidate every outbound destination and redirect; block private, loopback, link-local, metadata, multicast, reserved, and disallowed targets.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Backend, Serviq: Security, Serviq: Data, Serviq: Infrastructure, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01 and V2.0.02 must be complete and accepted.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2101-create-shopify-development-store-integration-metadata-migration"></a>

### V2.1.01 — Create Shopify development-store integration metadata migration

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Add tenant integration metadata: provider/type, store identifier/domain, secret_ref, scopes/version/status, last-verified timestamps.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Add tenant integration metadata: provider/type, store identifier/domain, secret_ref, scopes/version/status, last-verified timestamps.

#### Frozen source requirements

Add tenant integration metadata: provider/type, store identifier/domain, secret_ref, scopes/version/status, last-verified timestamps. No plaintext token. Up/down tests.

#### Exact scope

Add tenant integration metadata: provider/type, store identifier/domain, secret_ref, scopes/version/status, last-verified timestamps. No plaintext token. Up/down tests.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Add tenant integration metadata: provider/type, store identifier/domain, secret_ref, scopes/version/status, last-verified timestamps. No plaintext token. Up/down tests.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Backend, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2102-implement-shopify-secretauth-adapter"></a>

### V2.1.02 — Implement Shopify secret/auth adapter

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Implement exact Shopify development/test-store auth/token flow frozen by V2 architecture.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Implement exact Shopify development/test-store auth/token flow frozen by V2 architecture.

#### Frozen source requirements

Implement exact Shopify development/test-store auth/token flow frozen by V2 architecture. Server-only tokens, scope validation, rotation/revocation and safe metadata. Mock external calls. Premium security review.

#### Exact scope

Implement exact Shopify development/test-store auth/token flow frozen by V2 architecture. Server-only tokens, scope validation, rotation/revocation and safe metadata. Mock external calls. Premium security review.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

test-store auth/token flow frozen by V2 architecture.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Backend, Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v2103-implement-shopify-connection-createtestdisconnect-apis"></a>

### V2.1.03 — Implement Shopify connection create/test/disconnect APIs

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Tenant APIs save connection metadata, test access, list masked status and disconnect/revoke.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Tenant APIs save connection metadata, test access, list masked status and disconnect/revoke.

#### Frozen source requirements

Tenant APIs save connection metadata, test access, list masked status and disconnect/revoke. Validate store identity/scopes. Test bad token, wrong tenant, revoked token, timeout, rate limit.

#### Exact scope

Tenant APIs save connection metadata, test access, list masked status and disconnect/revoke. Validate store identity/scopes. Test bad token, wrong tenant, revoked token, timeout, rate limit.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

test access, list masked status and disconnect/revoke. Test bad token, wrong tenant, revoked token, timeout, rate limit.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2104-implement-shopify-customerorder-read-adapter"></a>

### V2.1.04 — Implement Shopify customer/order read adapter

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Typed read-only tools for frozen order/customer lookup against development-store data.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Typed read-only tools for frozen order/customer lookup against development-store data.

#### Frozen source requirements

Typed read-only tools for frozen order/customer lookup against development-store data. Validate ownership mapping, normalize bounded output, never raw Shopify response to LLM. Fixture contract tests.

#### Exact scope

Typed read-only tools for frozen order/customer lookup against development-store data. Validate ownership mapping, normalize bounded output, never raw Shopify response to LLM. Fixture contract tests.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2105-implement-shopify-cancel-order-action-adapter"></a>

### V2.1.05 — Implement Shopify cancel-order action adapter

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

One frozen protected Shopify mutation through Serviq tool/policy/idempotency pipeline.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

One frozen protected Shopify mutation through Serviq tool/policy/idempotency pipeline.

#### Frozen source requirements

One frozen protected Shopify mutation through Serviq tool/policy/idempotency pipeline. Validate order state/ownership/idempotency/ambiguous reconciliation. Never bypass C-5/C-6.

#### Exact scope

One frozen protected Shopify mutation through Serviq tool/policy/idempotency pipeline. Validate order state/ownership/idempotency/ambiguous reconciliation. Never bypass C-5/C-6.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Backend, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2106-implement-shopify-refundrequest-action-adapter"></a>

### V2.1.06 — Implement Shopify refund/request action adapter

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Exact V2 refund/return action selected by architect, with amount/currency validation, policy preconditions, idempotency/external ref, normalized result, timeout/reconciliation tests.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Exact V2 refund/return action selected by architect, with amount/currency validation, policy preconditions, idempotency/external ref, normalized result, timeout/reconciliation tests.

#### Frozen source requirements

Exact V2 refund/return action selected by architect, with amount/currency validation, policy preconditions, idempotency/external ref, normalized result, timeout/reconciliation tests.

#### Exact scope

Exact V2 refund/return action selected by architect, with amount/currency validation, policy preconditions, idempotency/external ref, normalized result, timeout/reconciliation tests.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Backend, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2107-add-shopify-integration-ui"></a>

### V2.1.07 — Add Shopify integration UI

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Connection card/form, test status, scopes, disconnect confirmation and read-only health metadata.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Connection card/form, test status, scopes, disconnect confirmation and read-only health metadata.

#### Frozen source requirements

Connection card/form, test status, scopes, disconnect confirmation and read-only health metadata. Never token. Loading/error/permission and development-store disclaimer.

#### Exact scope

Connection card/form, test status, scopes, disconnect confirmation and read-only health metadata. Never token. Loading/error/permission and development-store disclaimer.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

Needs Architect Decision: freeze loading, empty, permission-denied, validation-error, dependency-error, success, and retry states with exact user-visible copy and accessibility behavior before builder handoff.

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

test status, scopes, disconnect confirmation and read-only health metadata.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Frontend, Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2108-add-shopify-v2-e2e-scenarios"></a>

### V2.1.08 — Add Shopify V2 E2E scenarios

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Development/test-store lookup, protected cancel/refund, provider error, revoked auth, idempotent replay, human escalation on ambiguous result.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Development/test-store lookup, protected cancel/refund, provider error, revoked auth, idempotent replay, human escalation on ambiguous result.

#### Frozen source requirements

Development/test-store lookup, protected cancel/refund, provider error, revoked auth, idempotent replay, human escalation on ambiguous result.

#### Exact scope

Development/test-store lookup, protected cancel/refund, provider error, revoked auth, idempotent replay, human escalation on ambiguous result.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

test-store lookup, protected cancel/refund, provider error, revoked auth, idempotent replay, human escalation on ambiguous result.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2201-freeze-generic-help-deskcrm-connector-interface"></a>

### V2.2.01 — Freeze generic help-desk/CRM connector interface

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Architect defines exact adapter methods/events for customer lookup, ticket create/update, handoff, external refs and webhook callback.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Architect defines exact adapter methods/events for customer lookup, ticket create/update, handoff, external refs and webhook callback.

#### Frozen source requirements

Architect defines exact adapter methods/events for customer lookup, ticket create/update, handoff, external refs and webhook callback. No vendor yet. Fake connector contract tests.

#### Exact scope

Architect defines exact adapter methods/events for customer lookup, ticket create/update, handoff, external refs and webhook callback. No vendor yet. Fake connector contract tests.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Re-resolve and revalidate every outbound destination and redirect; block private, loopback, link-local, metadata, multicast, reserved, and disallowed targets.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Backend, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2202-create-generic-integration-registry-and-credential-metadata-schema"></a>

### V2.2.02 — Create generic integration registry and credential metadata schema

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Versioned connector registration, tenant connection metadata, capabilities, status, secret_ref, last sync/error.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Versioned connector registration, tenant connection metadata, capabilities, status, secret_ref, last sync/error. Migration/isolation tests. No vendor-specific columns outside frozen metadata.

#### Exact scope

Versioned connector registration, tenant connection metadata, capabilities, status, secret_ref, last sync/error. Migration/isolation tests. No vendor-specific columns outside frozen metadata.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Versioned connector registration, tenant connection metadata, capabilities, status, secret_ref, last sync/error. Migration/isolation tests. No vendor-specific columns outside frozen metadata.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Backend, Serviq: Security, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v2203-implement-connector-healthtest-framework"></a>

### V2.2.03 — Implement connector health/test framework

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Reusable service for testing tenant integration with timeout, normalized error, capability report, status persistence and rate limit.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Reusable service for testing tenant integration with timeout, normalized error, capability report, status persistence and rate limit. Fake connector tests.

#### Exact scope

Reusable service for testing tenant integration with timeout, normalized error, capability report, status persistence and rate limit. Fake connector tests.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Reusable service for testing tenant integration with timeout, normalized error, capability report, status persistence and rate limit. Fake connector tests.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

testing tenant integration with timeout, normalized error, capability report, status persistence and rate limit. tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Backend, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2204-implement-zendesk-style-ticket-adapter-against-sandboxdev-account"></a>

### V2.2.04 — Implement Zendesk-style ticket adapter against sandbox/dev account

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Only after V2 architecture names vendor/version.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Only after V2 architecture names vendor/version. Create ticket + public/internal note + status lookup through generic contract, fixture tests, secret-safe logs.

#### Exact scope

Only after V2 architecture names vendor/version. Create ticket + public/internal note + status lookup through generic contract, fixture tests, secret-safe logs.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests, secret-safe logs.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Backend, Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2205-implement-external-ticket-handoff-mapping"></a>

### V2.2.05 — Implement external ticket handoff mapping

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

When escalation selects external help desk, create one ticket, persist external ref, safe handoff summary, idempotent duplicate prevention.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

When escalation selects external help desk, create one ticket, persist external ref, safe handoff summary, idempotent duplicate prevention. Test retry/partial failure.

#### Exact scope

When escalation selects external help desk, create one ticket, persist external ref, safe handoff summary, idempotent duplicate prevention. Test retry/partial failure.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

When escalation selects external help desk, create one ticket, persist external ref, safe handoff summary, idempotent duplicate prevention. Test retry/partial failure.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test retry/partial failure.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2206-build-integration-management-ui"></a>

### V2.2.06 — Build integration management UI

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Generic list/detail capability/status plus vendor-specific config from frozen schema.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Generic list/detail capability/status plus vendor-specific config from frozen schema.

#### Frozen source requirements

Generic list/detail capability/status plus vendor-specific config from frozen schema. Cover disconnected/testing/active/error/revoked/permissions.

#### Exact scope

Generic list/detail capability/status plus vendor-specific config from frozen schema. Cover disconnected/testing/active/error/revoked/permissions.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

Needs Architect Decision: freeze loading, empty, permission-denied, validation-error, dependency-error, success, and retry states with exact user-visible copy and accessibility behavior before builder handoff.

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

testing/active/error/revoked/permissions.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Frontend, Serviq: Backend, Serviq: Security, Serviq: Data, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2301-freeze-omnichannel-inboundoutbound-message-contract"></a>

### V2.3.01 — Freeze omnichannel inbound/outbound message contract

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Normalized envelope for email/SMS/WhatsApp-like adapters: tenant/channel/externalConversation/externalMessage/sender/direction/content/attachment refs/timestamps/idempotency.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Normalized envelope for email/SMS/WhatsApp-like adapters: tenant/channel/externalConversation/externalMessage/sender/direction/content/attachment refs/timestamps/idempotency.

#### Frozen source requirements

Normalized envelope for email/SMS/WhatsApp-like adapters: tenant/channel/externalConversation/externalMessage/sender/direction/content/attachment refs/timestamps/idempotency. Contract fixtures.

#### Exact scope

Normalized envelope for email/SMS/WhatsApp-like adapters: tenant/channel/externalConversation/externalMessage/sender/direction/content/attachment refs/timestamps/idempotency. Contract fixtures.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Foundation, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2302-add-channel-connection-metadata-migration"></a>

### V2.3.02 — Add channel connection metadata migration

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Create channel_connections and external thread/message mappings exactly from V2 architecture with tenant/provider/status/secret refs and unique external IDs.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Create channel_connections and external thread/message mappings exactly from V2 architecture with tenant/provider/status/secret refs and unique external IDs.

#### Frozen source requirements

Create channel_connections and external thread/message mappings exactly from V2 architecture with tenant/provider/status/secret refs and unique external IDs.

#### Exact scope

Create channel_connections and external thread/message mappings exactly from V2 architecture with tenant/provider/status/secret refs and unique external IDs.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Create channel_connections and external thread/message mappings exactly from V2 architecture with tenant/provider/status/secret refs and unique external IDs.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Backend, Serviq: Security, Serviq: Data, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2303-implement-inbound-email-adapter-using-test-mailboxprovider"></a>

### V2.3.03 — Implement inbound email adapter using test mailbox/provider

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Verify webhook/poll auth, normalize envelope, dedupe external message, map/create Serviq conversation, persist customer message.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Verify webhook/poll auth, normalize envelope, dedupe external message, map/create Serviq conversation, persist customer message.

#### Frozen source requirements

Verify webhook/poll auth, normalize envelope, dedupe external message, map/create Serviq conversation, persist customer message. No direct agent call outside C-2.

#### Exact scope

Verify webhook/poll auth, normalize envelope, dedupe external message, map/create Serviq conversation, persist customer message. No direct agent call outside C-2.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Verify webhook/poll auth, normalize envelope, dedupe external message, map/create Serviq conversation, persist customer message. No direct agent call outside C-2.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Verify webhook/poll auth, normalize envelope, dedupe external message, map/create Serviq conversation, persist customer message.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Re-resolve and revalidate every outbound destination and redirect; block private, loopback, link-local, metadata, multicast, reserved, and disallowed targets.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2304-implement-outbound-email-response-adapter"></a>

### V2.3.04 — Implement outbound email response adapter

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Consume customer-safe Serviq message events and send one threaded reply.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Consume customer-safe Serviq message events and send one threaded reply.

#### Frozen source requirements

Consume customer-safe Serviq message events and send one threaded reply. Idempotency, bounce/failure status, no internal-note leakage.

#### Exact scope

Consume customer-safe Serviq message events and send one threaded reply. Idempotency, bounce/failure status, no internal-note leakage.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Backend, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v2305-implement-sms-like-channel-adapter-against-sandbox-provider"></a>

### V2.3.05 — Implement SMS-like channel adapter against sandbox provider

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

After ADR: inbound verification, phone identity normalization, conversation mapping, outbound replies, opt-out/compliance, rate limits, idempotency.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

After ADR: inbound verification, phone identity normalization, conversation mapping, outbound replies, opt-out/compliance, rate limits, idempotency.

#### Frozen source requirements

After ADR: inbound verification, phone identity normalization, conversation mapping, outbound replies, opt-out/compliance, rate limits, idempotency. Premium security/privacy review.

#### Exact scope

After ADR: inbound verification, phone identity normalization, conversation mapping, outbound replies, opt-out/compliance, rate limits, idempotency. Premium security/privacy review.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Backend, Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2306-implement-whatsapp-like-channel-adapter-against-sandbox-provider"></a>

### V2.3.06 — Implement WhatsApp-like channel adapter against sandbox provider

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Same normalized contract, approved template/session-window, signature, message dedupe, outbound mapping.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Same normalized contract, approved template/session-window, signature, message dedupe, outbound mapping.

#### Frozen source requirements

Same normalized contract, approved template/session-window, signature, message dedupe, outbound mapping. Vendor decisions frozen in ADR first.

#### Exact scope

Same normalized contract, approved template/session-window, signature, message dedupe, outbound mapping. Vendor decisions frozen in ADR first.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

proved template/session-window, signature, message dedupe, outbound mapping.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Backend, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2307-add-omnichannel-conversation-indicators-to-support-inbox"></a>

### V2.3.07 — Add omnichannel conversation indicators to support inbox

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Show channel badge, safe external thread ID, delivery status and send restrictions while reusing conversation model/workspace.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Show channel badge, safe external thread ID, delivery status and send restrictions while reusing conversation model/workspace.

#### Frozen source requirements

Show channel badge, safe external thread ID, delivery status and send restrictions while reusing conversation model/workspace.

#### Exact scope

Show channel badge, safe external thread ID, delivery status and send restrictions while reusing conversation model/workspace.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

Needs Architect Decision: freeze loading, empty, permission-denied, validation-error, dependency-error, success, and retry states with exact user-visible copy and accessibility behavior before builder handoff.

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Frontend, Serviq: AI, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2401-add-exact-response-cache-layer"></a>

### V2.4.01 — Add exact response cache layer

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Tenant/config/knowledge-version keyed exact cache for deterministic/grounded safe responses.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Tenant/config/knowledge-version keyed exact cache for deterministic/grounded safe responses. Frozen TTL/invalidation. No private tool result caching unless explicitly safe. Hit/miss/invalidation tests.

#### Exact scope

Tenant/config/knowledge-version keyed exact cache for deterministic/grounded safe responses. Frozen TTL/invalidation. No private tool result caching unless explicitly safe. Hit/miss/invalidation tests.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Tenant/config/knowledge-version keyed exact cache for deterministic/grounded safe responses. Frozen TTL/invalidation. No private tool result caching unless explicitly safe. Hit/miss/invalidation tests.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2402-add-semantic-cache-candidate-lookup"></a>

### V2.4.02 — Add semantic cache candidate lookup

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Tenant-scoped embedding lookup for cacheable public knowledge questions only.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Tenant-scoped embedding lookup for cacheable public knowledge questions only. Threshold frozen by evaluation; cached response stores evidence/version. Reject stale knowledge/agent version.

#### Exact scope

Tenant-scoped embedding lookup for cacheable public knowledge questions only. Threshold frozen by evaluation; cached response stores evidence/version. Reject stale knowledge/agent version.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Tenant-scoped embedding lookup for cacheable public knowledge questions only. Threshold frozen by evaluation; cached response stores evidence/version. Reject stale knowledge/agent version.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: AI, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2403-add-semantic-cache-safety-verifier"></a>

### V2.4.03 — Add semantic cache safety verifier

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Verify tenant, knowledge version, agent version, access scope, no customer-specific intent and threshold before serving hit.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Verify tenant, knowledge version, agent version, access scope, no customer-specific intent and threshold before serving hit.

#### Frozen source requirements

Verify tenant, knowledge version, agent version, access scope, no customer-specific intent and threshold before serving hit. Near-match private question must never get public cache.

#### Exact scope

Verify tenant, knowledge version, agent version, access scope, no customer-specific intent and threshold before serving hit. Near-match private question must never get public cache.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Verify tenant, knowledge version, agent version, access scope, no customer-specific intent and threshold before serving hit. Near-match private question must never get public cache.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Verify tenant, knowledge version, agent version, access scope, no customer-specific intent and threshold before serving hit.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: AI, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2404-implement-capability-aware-model-router"></a>

### V2.4.04 — Implement capability-aware model router

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Route classification/simple grounding/complex reasoning using tenant rules, capability, latency/cost budget and provider health.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Route classification/simple grounding/complex reasoning using tenant rules, capability, latency/cost budget and provider health. Strictly bounded/observable. Deterministic route tests.

#### Exact scope

Route classification/simple grounding/complex reasoning using tenant rules, capability, latency/cost budget and provider health. Strictly bounded/observable. Deterministic route tests.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2405-add-shadow-evaluation-mode-for-candidate-modelconfig"></a>

### V2.4.05 — Add shadow evaluation mode for candidate model/config

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Asynchronously duplicate selected production-safe requests to candidate, discard candidate output from user path, store eval metrics only.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Asynchronously duplicate selected production-safe requests to candidate, discard candidate output from user path, store eval metrics only. Privacy/redaction. No double tool execution.

#### Exact scope

Asynchronously duplicate selected production-safe requests to candidate, discard candidate output from user path, store eval metrics only. Privacy/redaction. No double tool execution.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: AI, Serviq: Security, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v2406-build-evaluation-comparison-ui"></a>

### V2.4.06 — Build evaluation comparison UI

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Compare versions on correctness, grounding, citations, tool selection, policy, escalation, latency and cost.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Compare versions on correctness, grounding, citations, tool selection, policy, escalation, latency and cost.

#### Frozen source requirements

Compare versions on correctness, grounding, citations, tool selection, policy, escalation, latency and cost. Explicit gates; model grade not sole truth.

#### Exact scope

Compare versions on correctness, grounding, citations, tool selection, policy, escalation, latency and cost. Explicit gates; model grade not sole truth.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

Needs Architect Decision: freeze loading, empty, permission-denied, validation-error, dependency-error, success, and retry states with exact user-visible copy and accessibility behavior before builder handoff.

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Frontend, Serviq: AI, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2501-containerize-apiworkergateway-with-production-images"></a>

### V2.5.01 — Containerize API/worker/gateway with production images

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Multi-stage OCI, non-root, pinned dependencies, health/readiness, small runtime, SBOM input.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Multi-stage OCI, non-root, pinned dependencies, health/readiness, small runtime, SBOM input.

#### Frozen source requirements

Multi-stage OCI, non-root, pinned dependencies, health/readiness, small runtime, SBOM input. CI builds all. No Kubernetes yet.

#### Exact scope

Multi-stage OCI, non-root, pinned dependencies, health/readiness, small runtime, SBOM input. CI builds all. No Kubernetes yet.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Backend, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2502-add-local-multi-replica-load-profile"></a>

### V2.5.02 — Add local multi-replica load profile

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Multiple stateless API/worker replicas behind local proxy; verify session/tenant behavior not process-memory dependent.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Multiple stateless API/worker replicas behind local proxy; verify session/tenant behavior not process-memory dependent. Sticky vs non-sticky tests.

#### Exact scope

Multiple stateless API/worker replicas behind local proxy; verify session/tenant behavior not process-memory dependent. Sticky vs non-sticky tests.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

verify session/tenant behavior not process-memory dependent. tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Backend, Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2503-add-clustered-valkey-test-profile"></a>

### V2.5.03 — Add clustered Valkey test profile

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Optional V2 profile proving rate/cache logic across replicas.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Optional V2 profile proving rate/cache logic across replicas. Test node outage and safe fallback.

#### Exact scope

Optional V2 profile proving rate/cache logic across replicas. Test node outage and safe fallback.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test node outage and safe fallback.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2504-add-kafka-partitioningconsumer-group-scale-tests"></a>

### V2.5.04 — Add Kafka partitioning/consumer-group scale tests

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Freeze partition keys by tenant/conversation/aggregate; multiple consumers; verify ordering, idempotency, lag and rebalance.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Freeze partition keys by tenant/conversation/aggregate; multiple consumers; verify ordering, idempotency, lag and rebalance.

#### Frozen source requirements

Freeze partition keys by tenant/conversation/aggregate; multiple consumers; verify ordering, idempotency, lag and rebalance.

#### Exact scope

Freeze partition keys by tenant/conversation/aggregate; multiple consumers; verify ordering, idempotency, lag and rebalance.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

verify ordering, idempotency, lag and rebalance.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Backend, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2505-create-first-aws-single-region-terraform-foundation"></a>

### V2.5.05 — Create first AWS single-region Terraform foundation

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

After deployment ADR define network, container runtime, managed Postgres/cache/broker/object storage/secrets, IAM, logs/OTel with no manual prod-critical resources.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

After deployment ADR define network, container runtime, managed Postgres/cache/broker/object storage/secrets, IAM, logs/OTel with no manual prod-critical resources. `terraform plan` only until explicit spend approval. Human Needed.

#### Exact scope

After deployment ADR define network, container runtime, managed Postgres/cache/broker/object storage/secrets, IAM, logs/OTel with no manual prod-critical resources. `terraform plan` only until explicit spend approval. Human Needed.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Security, Serviq: Infrastructure, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2506-deploy-v2-staging-in-one-aws-region"></a>

### V2.5.06 — Deploy V2 staging in one AWS region

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Only after explicit budget/credential approval.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Only after explicit budget/credential approval. Deploy immutable images, migrations, synthetic data, smoke/E2E/security, cost estimate and teardown. Human Needed.

#### Exact scope

Only after explicit budget/credential approval. Deploy immutable images, migrations, synthetic data, smoke/E2E/security, cost estimate and teardown. Human Needed.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Security, Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v2507-add-horizontal-autoscaling-policy-and-test"></a>

### V2.5.07 — Add horizontal autoscaling policy and test

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Configure API/worker autoscaling using frozen CPU/concurrency/queue-lag.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Configure API/worker autoscaling using frozen CPU/concurrency/queue-lag. Controlled staging load; record scale latency, saturation, errors, cost. Do not infer 10M.

#### Exact scope

Configure API/worker autoscaling using frozen CPU/concurrency/queue-lag. Controlled staging load; record scale latency, saturation, errors, cost. Do not infer 10M.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Backend, Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v2508-publish-v2-single-region-benchmark-report"></a>

### V2.5.08 — Publish V2 single-region benchmark report

> **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Compare V1 baseline vs V2 single region with reproducible fake-provider workload.

#### Why this exists

This item closes one bounded part of the remaining V2 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Compare V1 baseline vs V2 single region with reproducible fake-provider workload. Record infrastructure/cost, RPS/connections, p95/p99, queue lag, DB/cache saturation, bottlenecks and V3 target.

#### Exact scope

Compare V1 baseline vs V2 single region with reproducible fake-provider workload. Record infrastructure/cost, RPS/connections, p95/p99, queue lag, DB/cache saturation, bottlenecks and V3 target.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 2 — V2 Integrations & Omnichannel Scale
- **Labels:** Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2.0.01, V2.0.02, and V2.0.03 must be complete; the exact V2 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

## Phase 3 — V3 Autonomous Enterprise Operations

<a id="v3001-run-post-v2-repository-audit"></a>

### V3.0.01 — Run post-V2 repository audit

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT/BLOCKED — phase-gate planning ticket; start only after the predecessor release evidence named below exists.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Refresh repo_context with actual service extraction, integrations, channels, deployment, metrics, costs, bottlenecks and tests.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Refresh repo_context with actual service extraction, integrations, channels, deployment, metrics, costs, bottlenecks and tests. No stale paths.

#### Exact scope

Refresh repo_context with actual service extraction, integrations, channels, deployment, metrics, costs, bottlenecks and tests. No stale paths.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V2 release and benchmark gates must be accepted before this audit starts.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Backend, Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V2 release and benchmark gates must be accepted before this audit starts.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3002-write-v3-prd-delta"></a>

### V3.0.02 — Write V3 PRD delta

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT/BLOCKED — phase-gate planning ticket; start only after the predecessor release evidence named below exists.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Freeze proactive support, multi-agent, multilingual, enterprise isolation, quality/coaching and partitioning scope, measurable outcomes and non-goals.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Freeze proactive support, multi-agent, multilingual, enterprise isolation, quality/coaching and partitioning scope, measurable outcomes and non-goals.

#### Frozen source requirements

Freeze proactive support, multi-agent, multilingual, enterprise isolation, quality/coaching and partitioning scope, measurable outcomes and non-goals.

#### Exact scope

Freeze proactive support, multi-agent, multilingual, enterprise isolation, quality/coaching and partitioning scope, measurable outcomes and non-goals.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01 must be complete and its repository audit accepted.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: AI, Serviq: Security, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01 must be complete and its repository audit accepted.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3003-write-v3-architecturecontract-delta"></a>

### V3.0.03 — Write V3 architecture/contract delta

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT/BLOCKED — phase-gate planning ticket; start only after the predecessor release evidence named below exists.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Freeze workflow-run schema/events, agent handoff, localization model, isolation modes, partition routing, QA/coaching events and service extraction boundaries.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Freeze workflow-run schema/events, agent handoff, localization model, isolation modes, partition routing, QA/coaching events and service extraction boundaries.

#### Exact scope

Freeze workflow-run schema/events, agent handoff, localization model, isolation modes, partition routing, QA/coaching events and service extraction boundaries.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01 and V3.0.02 must be complete and accepted.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Security, Serviq: Data, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01 and V3.0.02 must be complete and accepted.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3101-create-proactive-workflow-definitionversion-schema"></a>

### V3.1.01 — Create proactive workflow definition/version schema

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Versioned tenant workflow definitions, triggers, status, schedule/event conditions, policy refs, agent/tool refs and published versions.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Versioned tenant workflow definitions, triggers, status, schedule/event conditions, policy refs, agent/tool refs and published versions. Immutable published versions.

#### Exact scope

Versioned tenant workflow definitions, triggers, status, schedule/event conditions, policy refs, agent/tool refs and published versions. Immutable published versions.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Versioned tenant workflow definitions, triggers, status, schedule/event conditions, policy refs, agent/tool refs and published versions. Immutable published versions.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Data, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3102-implement-proactive-trigger-evaluator"></a>

### V3.1.02 — Implement proactive trigger evaluator

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Evaluate only approved triggers such as delayed order/repeated failure/SLA/subscription/external event.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Evaluate only approved triggers such as delayed order/repeated failure/SLA/subscription/external event.

#### Frozen source requirements

Evaluate only approved triggers such as delayed order/repeated failure/SLA/subscription/external event. Creates durable workflow command, never direct message/mutation. Idempotency/replay tests.

#### Exact scope

Evaluate only approved triggers such as delayed order/repeated failure/SLA/subscription/external event. Creates durable workflow command, never direct message/mutation. Idempotency/replay tests.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

proved triggers such as delayed order/repeated failure/SLA/subscription/external event. tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3103-implement-proactive-workflow-run-persistence"></a>

### V3.1.03 — Implement proactive workflow run persistence

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Create workflow_runs/steps with status, correlation, tenant, trigger, current step, retries, deadline and terminal outcome.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Create workflow_runs/steps with status, correlation, tenant, trigger, current step, retries, deadline and terminal outcome. State-transition tests.

#### Exact scope

Create workflow_runs/steps with status, correlation, tenant, trigger, current step, retries, deadline and terminal outcome. State-transition tests.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Create workflow_runs/steps with status, correlation, tenant, trigger, current step, retries, deadline and terminal outcome. State-transition tests.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3104-implement-workflow-step-executor-shell"></a>

### V3.1.04 — Implement workflow step executor shell

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Execute one frozen step type through existing retrieval/model/tool/policy contracts.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Execute one frozen step type through existing retrieval/model/tool/policy contracts. Enforce budgets/deadlines/persist transitions. No arbitrary code/script step.

#### Exact scope

Execute one frozen step type through existing retrieval/model/tool/policy contracts. Enforce budgets/deadlines/persist transitions. No arbitrary code/script step.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Execute one frozen step type through existing retrieval/model/tool/policy contracts. Enforce budgets/deadlines/persist transitions. No arbitrary code/script step.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: AI, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v3105-implement-proactive-outbound-notification-gate"></a>

### V3.1.05 — Implement proactive outbound notification gate

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Verify tenant policy, channel consent/opt-out, quiet hours, frequency cap and eligibility before proactive message.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Verify tenant policy, channel consent/opt-out, quiet hours, frequency cap and eligibility before proactive message.

#### Frozen source requirements

Verify tenant policy, channel consent/opt-out, quiet hours, frequency cap and eligibility before proactive message. Denied trigger ends safely with audit reason.

#### Exact scope

Verify tenant policy, channel consent/opt-out, quiet hours, frequency cap and eligibility before proactive message. Denied trigger ends safely with audit reason.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Verify tenant policy, channel consent/opt-out, quiet hours, frequency cap and eligibility before proactive message.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3106-add-proactive-workflow-client-ui"></a>

### V3.1.06 — Add proactive workflow client UI

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

List workflows, draft/published versions, triggers, allowed tools/channels, test event, publish/rollback and run history.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

List workflows, draft/published versions, triggers, allowed tools/channels, test event, publish/rollback and run history.

#### Frozen source requirements

List workflows, draft/published versions, triggers, allowed tools/channels, test event, publish/rollback and run history. Published immutable. Standard UI states.

#### Exact scope

List workflows, draft/published versions, triggers, allowed tools/channels, test event, publish/rollback and run history. Published immutable. Standard UI states.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

Needs Architect Decision: freeze loading, empty, permission-denied, validation-error, dependency-error, success, and retry states with exact user-visible copy and accessibility behavior before builder handoff.

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

test event, publish/rollback and run history.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Frontend, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3107-add-proactive-workflow-e2e-suite"></a>

### V3.1.07 — Add proactive workflow E2E suite

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Trigger -> policy -> outbound message/action -> customer response -> escalation using synthetic events, including duplicate trigger and opt-out denial.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Trigger -> policy -> outbound message/action -> customer response -> escalation using synthetic events, including duplicate trigger and opt-out denial.

#### Frozen source requirements

Trigger -> policy -> outbound message/action -> customer response -> escalation using synthetic events, including duplicate trigger and opt-out denial.

#### Exact scope

Trigger -> policy -> outbound message/action -> customer response -> escalation using synthetic events, including duplicate trigger and opt-out denial.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3201-freeze-specialist-agent-registry-contract"></a>

### V3.2.01 — Freeze specialist-agent registry contract

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Define specialist roles, exact input/output, allowed tools/handoffs.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Define specialist roles, exact input/output, allowed tools/handoffs. No free-form agent spawning.

#### Exact scope

Define specialist roles, exact input/output, allowed tools/handoffs. No free-form agent spawning.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: AI, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3202-add-specialist-agent-configuration-schema"></a>

### V3.2.02 — Add specialist-agent configuration schema

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Versioned specialist configs, parent refs, model alias, budgets, tools and allowed handoff targets.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Versioned specialist configs, parent refs, model alias, budgets, tools and allowed handoff targets. Migration/isolation tests.

#### Exact scope

Versioned specialist configs, parent refs, model alias, budgets, tools and allowed handoff targets. Migration/isolation tests.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Versioned specialist configs, parent refs, model alias, budgets, tools and allowed handoff targets. Migration/isolation tests.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Security, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3203-implement-agent-to-agent-handoff-contract"></a>

### V3.2.03 — Implement agent-to-agent handoff contract

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Bounded handoff with tenant, parentRun, childAgentVersion, task summary, evidence/tool refs, budget allocation and correlation.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Bounded handoff with tenant, parentRun, childAgentVersion, task summary, evidence/tool refs, budget allocation and correlation. Child returns structured result only unless contract permits.

#### Exact scope

Bounded handoff with tenant, parentRun, childAgentVersion, task summary, evidence/tool refs, budget allocation and correlation. Child returns structured result only unless contract permits.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: AI, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3204-implement-child-run-budget-inheritance"></a>

### V3.2.04 — Implement child-run budget inheritance

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Parent allocates bounded subset of remaining model/tool/time/token budget.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Parent allocates bounded subset of remaining model/tool/time/token budget. Child cannot exceed parent. Nested/cancel tests.

#### Exact scope

Parent allocates bounded subset of remaining model/tool/time/token budget. Child cannot exceed parent. Nested/cancel tests.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: AI, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3205-implement-parallel-read-only-specialist-execution"></a>

### V3.2.05 — Implement parallel read-only specialist execution

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Approved independent read-only children concurrently with bounded fan-out/deadline.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Approved independent read-only children concurrently with bounded fan-out/deadline. Deterministic merge. Mutations serialized through policy coordinator.

#### Exact scope

Approved independent read-only children concurrently with bounded fan-out/deadline. Deterministic merge. Mutations serialized through policy coordinator.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

proved independent read-only children concurrently with bounded fan-out/deadline.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Foundation, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v3206-implement-multi-agent-trace-visualization"></a>

### V3.2.06 — Implement multi-agent trace visualization

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Client diagnostic view parent/child runs, timings, evidence/tool summary, model aliases and outcomes without chain-of-thought.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Client diagnostic view parent/child runs, timings, evidence/tool summary, model aliases and outcomes without chain-of-thought. Permission/redaction tests.

#### Exact scope

Client diagnostic view parent/child runs, timings, evidence/tool summary, model aliases and outcomes without chain-of-thought. Permission/redaction tests.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

Needs Architect Decision: freeze loading, empty, permission-denied, validation-error, dependency-error, success, and retry states with exact user-visible copy and accessibility behavior before builder handoff.

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Frontend, Serviq: AI, Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3207-add-multi-agent-regressionevaluation-suite"></a>

### V3.2.07 — Add multi-agent regression/evaluation suite

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Cases prove handoff selection, no recursive runaway, budget containment, deterministic merge, mutation serialization and escalation on child failure.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Cases prove handoff selection, no recursive runaway, budget containment, deterministic merge, mutation serialization and escalation on child failure.

#### Frozen source requirements

Cases prove handoff selection, no recursive runaway, budget containment, deterministic merge, mutation serialization and escalation on child failure.

#### Exact scope

Cases prove handoff selection, no recursive runaway, budget containment, deterministic merge, mutation serialization and escalation on child failure.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

prove handoff selection, no recursive runaway, budget containment, deterministic merge, mutation serialization and escalation on child failure.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: AI, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3301-add-tenant-localelanguage-configuration-schema"></a>

### V3.3.01 — Add tenant locale/language configuration schema

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Persist supported customer languages, default locale, knowledge-language metadata and localization settings.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Persist supported customer languages, default locale, knowledge-language metadata and localization settings.

#### Frozen source requirements

Persist supported customer languages, default locale, knowledge-language metadata and localization settings. No translation yet.

#### Exact scope

Persist supported customer languages, default locale, knowledge-language metadata and localization settings. No translation yet.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Persist supported customer languages, default locale, knowledge-language metadata and localization settings. No translation yet.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Backend, Serviq: Data, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3302-implement-language-detection-contract"></a>

### V3.3.02 — Implement language detection contract

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Bounded model/deterministic detector with strict language-code output.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Bounded model/deterministic detector with strict language-code output.

#### Frozen source requirements

Bounded model/deterministic detector with strict language-code output. Low-confidence/unsupported follows PRD fallback.

#### Exact scope

Bounded model/deterministic detector with strict language-code output. Low-confidence/unsupported follows PRD fallback.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: AI, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3303-add-multilingual-knowledge-ingestion-metadata"></a>

### V3.3.03 — Add multilingual knowledge ingestion metadata

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Store document/chunk language; preserve original text; prevent cross-language retrieval unless configured.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Store document/chunk language; preserve original text; prevent cross-language retrieval unless configured. Migration/backfill/tests.

#### Exact scope

Store document/chunk language; preserve original text; prevent cross-language retrieval unless configured. Migration/backfill/tests.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Store document/chunk language; preserve original text; prevent cross-language retrieval unless configured. Migration/backfill/tests.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3304-implement-language-aware-retrieval"></a>

### V3.3.04 — Implement language-aware retrieval

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Filter/prefer correct-language chunks with approved fallback.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Filter/prefer correct-language chunks with approved fallback.

#### Frozen source requirements

Filter/prefer correct-language chunks with approved fallback. Test English/Hindi/Spanish fixtures and unsupported isolation.

#### Exact scope

Filter/prefer correct-language chunks with approved fallback. Test English/Hindi/Spanish fixtures and unsupported isolation.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

proved fallback. Test English/Hindi/Spanish fixtures and unsupported isolation.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: AI, Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3305-implement-localized-response-generation-guard"></a>

### V3.3.05 — Implement localized response generation guard

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Respond in selected language; citations reference original approved sources; tool args remain canonical; IDs/codes not translated.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Respond in selected language; citations reference original approved sources; tool args remain canonical; IDs/codes not translated.

#### Exact scope

Respond in selected language; citations reference original approved sources; tool args remain canonical; IDs/codes not translated.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

proved sources; tool args remain canonical; IDs/codes not translated.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Foundation, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3306-internationalize-customerclient-ui-framework"></a>

### V3.3.06 — Internationalize customer/client UI framework

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Translation resources, locale routing/selection, localized validation/error copy, date/number formatting.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Translation resources, locale routing/selection, localized validation/error copy, date/number formatting.

#### Frozen source requirements

Translation resources, locale routing/selection, localized validation/error copy, date/number formatting. No hard-coded strings in target surfaces.

#### Exact scope

Translation resources, locale routing/selection, localized validation/error copy, date/number formatting. No hard-coded strings in target surfaces.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `apps/customer-web/src/app/page.tsx` — current customer-web entry surface.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

Needs Architect Decision: freeze loading, empty, permission-denied, validation-error, dependency-error, success, and retry states with exact user-visible copy and accessibility behavior before builder handoff.

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Frontend, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v3307-add-multilingual-e2eevaluation-suite"></a>

### V3.3.07 — Add multilingual E2E/evaluation suite

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Grounded FAQ, status, confirmation, escalation, human takeover in selected languages.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Grounded FAQ, status, confirmation, escalation, human takeover in selected languages.

#### Frozen source requirements

Grounded FAQ, status, confirmation, escalation, human takeover in selected languages. Score grounding separately from fluency.

#### Exact scope

Grounded FAQ, status, confirmation, escalation, human takeover in selected languages. Score grounding separately from fluency.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: AI, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3401-add-tenant-isolation-mode-configuration"></a>

### V3.4.01 — Add tenant isolation mode configuration

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Persist logical/dedicated-schema/dedicated-database/dedicated-worker-pool and provisioning status.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Persist logical/dedicated-schema/dedicated-database/dedicated-worker-pool and provisioning status. Default logical; changes platform operator only.

#### Exact scope

Persist logical/dedicated-schema/dedicated-database/dedicated-worker-pool and provisioning status. Default logical; changes platform operator only.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Persist logical/dedicated-schema/dedicated-database/dedicated-worker-pool and provisioning status. Default logical; changes platform operator only.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Backend, Serviq: Security, Serviq: Data, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3402-implement-tenant-data-source-routing-interface"></a>

### V3.4.02 — Implement tenant data-source routing interface

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Repositories obtain data source through placement resolver, not global direct DB.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Repositories obtain data source through placement resolver, not global direct DB. Logical maps current DB; dedicated modes use provisioned refs. Callers don't know physical placement.

#### Exact scope

Repositories obtain data source through placement resolver, not global direct DB. Logical maps current DB; dedicated modes use provisioned refs. Callers don't know physical placement.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Foundation, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3403-implement-dedicated-worker-pool-routing"></a>

### V3.4.03 — Implement dedicated worker-pool routing

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Events/jobs carry placement/workload class for isolated tenants.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Events/jobs carry placement/workload class for isolated tenants. Unknown placement fails safe.

#### Exact scope

Events/jobs carry placement/workload class for isolated tenants. Unknown placement fails safe.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Backend, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3404-add-per-tenant-encryption-key-reference-support"></a>

### V3.4.04 — Add per-tenant encryption-key reference support

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Extend secret/object/data encryption adapters to tenant key refs where required.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Extend secret/object/data encryption adapters to tenant key refs where required.

#### Frozen source requirements

Extend secret/object/data encryption adapters to tenant key refs where required. No custom crypto; rotation metadata/audit. Premium security review.

#### Exact scope

Extend secret/object/data encryption adapters to tenant key refs where required. No custom crypto; rotation metadata/audit. Premium security review.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3405-implement-enterprise-data-retention-overrides-within-platform-bounds"></a>

### V3.4.05 — Implement enterprise data-retention overrides within platform bounds

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Tenant-specific approved retention only within min/max/legal/platform constraints.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Tenant-specific approved retention only within min/max/legal/platform constraints. Versioned/audited; cleanup consumes effective policy.

#### Exact scope

Tenant-specific approved retention only within min/max/legal/platform constraints. Versioned/audited; cleanup consumes effective policy.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Tenant-specific approved retention only within min/max/legal/platform constraints. Versioned/audited; cleanup consumes effective policy.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

proved retention only within min/max/legal/platform constraints.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Data, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3406-build-enterprise-isolation-controls-in-platform-console"></a>

### V3.4.06 — Build enterprise isolation controls in platform console

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Platform operator inspect/provision/change allowed isolation/placement with confirmation/audit.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Platform operator inspect/provision/change allowed isolation/placement with confirmation/audit.

#### Frozen source requirements

Platform operator inspect/provision/change allowed isolation/placement with confirmation/audit. Tenant admin view only.

#### Exact scope

Platform operator inspect/provision/change allowed isolation/placement with confirmation/audit. Tenant admin view only.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `apps/platform-console/src/app/page.tsx` — separate platform-operator trust surface.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

Needs Architect Decision: freeze loading, empty, permission-denied, validation-error, dependency-error, success, and retry states with exact user-visible copy and accessibility behavior before builder handoff.

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Frontend, Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3407-add-enterprise-isolation-adversarial-test-suite"></a>

### V3.4.07 — Add enterprise isolation adversarial test suite

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Run isolation suite across logical/dedicated modes including cache/event/object placement.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Run isolation suite across logical/dedicated modes including cache/event/object placement.

#### Frozen source requirements

Run isolation suite across logical/dedicated modes including cache/event/object placement. No fallback to wrong shared tenant data.

#### Exact scope

Run isolation suite across logical/dedicated modes including cache/event/object placement. No fallback to wrong shared tenant data.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v3501-create-quality-review-sample-schema"></a>

### V3.5.01 — Create quality-review sample schema

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Persist sampled conversations, status, rubric version, reviewer, AI evaluator result, human score, defect categories and coaching recommendations.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Persist sampled conversations, status, rubric version, reviewer, AI evaluator result, human score, defect categories and coaching recommendations.

#### Frozen source requirements

Persist sampled conversations, status, rubric version, reviewer, AI evaluator result, human score, defect categories and coaching recommendations.

#### Exact scope

Persist sampled conversations, status, rubric version, reviewer, AI evaluator result, human score, defect categories and coaching recommendations.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Persist sampled conversations, status, rubric version, reviewer, AI evaluator result, human score, defect categories and coaching recommendations.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Backend, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3502-implement-deterministic-conversation-sampling-job"></a>

### V3.5.02 — Implement deterministic conversation sampling job

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Frozen sampling by volume/channel/escalation/risk; no duplicates; exclude privacy-deleted content; async review tasks.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Frozen sampling by volume/channel/escalation/risk; no duplicates; exclude privacy-deleted content; async review tasks.

#### Frozen source requirements

Frozen sampling by volume/channel/escalation/risk; no duplicates; exclude privacy-deleted content; async review tasks.

#### Exact scope

Frozen sampling by volume/channel/escalation/risk; no duplicates; exclude privacy-deleted content; async review tasks.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Backend, Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3503-implement-ai-quality-evaluator-with-strict-rubric-output"></a>

### V3.5.03 — Implement AI quality evaluator with strict rubric output

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Versioned evaluator prompt/schema scores grounding, policy, tool correctness, escalation, tone and resolution.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Versioned evaluator prompt/schema scores grounding, policy, tool correctness, escalation, tone and resolution. Advisory only; evidence refs, no hidden reasoning.

#### Exact scope

Versioned evaluator prompt/schema scores grounding, policy, tool correctness, escalation, tone and resolution. Advisory only; evidence refs, no hidden reasoning.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Backend, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3504-implement-human-qa-review-api"></a>

### V3.5.04 — Implement human QA review API

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Reviewer scores rubric, adds tags/comments, overrides AI score, marks reviewed.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Reviewer scores rubric, adds tags/comments, overrides AI score, marks reviewed. Permission/audit and immutable history.

#### Exact scope

Reviewer scores rubric, adds tags/comments, overrides AI score, marks reviewed. Permission/audit and immutable history.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Backend, Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3505-build-qa-review-workspace"></a>

### V3.5.05 — Build QA review workspace

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Queue/list/detail with conversation/evidence/action context, AI advisory score, rubric form, coaching note and filters.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Queue/list/detail with conversation/evidence/action context, AI advisory score, rubric form, coaching note and filters.

#### Frozen source requirements

Queue/list/detail with conversation/evidence/action context, AI advisory score, rubric form, coaching note and filters. Internal only.

#### Exact scope

Queue/list/detail with conversation/evidence/action context, AI advisory score, rubric form, coaching note and filters. Internal only.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

Needs Architect Decision: freeze loading, empty, permission-denied, validation-error, dependency-error, success, and retry states with exact user-visible copy and accessibility behavior before builder handoff.

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Frontend, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3506-implement-support-agent-coaching-summary-job"></a>

### V3.5.06 — Implement support-agent coaching summary job

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Aggregate reviewed defect categories by agent/team/date and generate safe coaching summary from structured data.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Aggregate reviewed defect categories by agent/team/date and generate safe coaching summary from structured data.

#### Frozen source requirements

Aggregate reviewed defect categories by agent/team/date and generate safe coaching summary from structured data. Do not infer protected traits.

#### Exact scope

Aggregate reviewed defect categories by agent/team/date and generate safe coaching summary from structured data. Do not infer protected traits.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Backend, Serviq: AI, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3507-extract-retrieval-service-behind-stable-network-contract"></a>

### V3.5.07 — Extract retrieval service behind stable network contract

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Only if measurements justify.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Only if measurements justify. Move retrieval module to independent service preserving exact C-3 and contract tests. Caller change only transport adapter.

#### Exact scope

Only if measurements justify. Move retrieval module to independent service preserving exact C-3 and contract tests. Caller change only transport adapter.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3508-extract-tool-execution-service-behind-stable-network-contract"></a>

### V3.5.08 — Extract tool execution service behind stable network contract

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Move tool coordinator/adapters to isolated service preserving C-5/C-6/idempotency/audit.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Move tool coordinator/adapters to isolated service preserving C-5/C-6/idempotency/audit.

#### Frozen source requirements

Move tool coordinator/adapters to isolated service preserving C-5/C-6/idempotency/audit. Failure/circuit tests.

#### Exact scope

Move tool coordinator/adapters to isolated service preserving C-5/C-6/idempotency/audit. Failure/circuit tests.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Backend, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v3509-introduce-partition-key-routing-for-high-volume-tenant-data"></a>

### V3.5.09 — Introduce partition-key routing for high-volume tenant data

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Using benchmark evidence, freeze shard/partition key for conversations/events/analytics.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Using benchmark evidence, freeze shard/partition key for conversations/events/analytics.

#### Frozen source requirements

Using benchmark evidence, freeze shard/partition key for conversations/events/analytics. Implement routing helper and shadow-read validation before physical move.

#### Exact scope

Using benchmark evidence, freeze shard/partition key for conversations/events/analytics. Implement routing helper and shadow-read validation before physical move.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v3510-publish-v3-partitioned-single-region-benchmark"></a>

### V3.5.10 — Publish V3 partitioned single-region benchmark

> **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Measure service-extracted partition-aware architecture.

#### Why this exists

This item closes one bounded part of the remaining V3 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Measure service-extracted partition-aware architecture. Report API/SSE/event/retrieval/tool throughput, DB/cache/broker saturation, cost and multi-region bottlenecks.

#### Exact scope

Measure service-extracted partition-aware architecture. Report API/SSE/event/retrieval/tool throughput, DB/cache/broker saturation, cost and multi-region bottlenecks.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 3 — V3 Autonomous Enterprise Operations
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Data, Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3.0.01, V3.0.02, and V3.0.03 must be complete; the exact V3 contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

## Phase 4 — V4 Hyperscale & Multi-Region

<a id="v4001-run-post-v3-hyperscale-repositoryinfrastructure-audit"></a>

### V4.0.01 — Run post-V3 hyperscale repository/infrastructure audit

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT/BLOCKED — phase-gate planning ticket; start only after the predecessor release evidence named below exists.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Update repo_context with topology, partitioning, traffic profiles, DB/cache/broker limits, SSE measurements, IaC/deployment, costs and bottlenecks.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Update repo_context with topology, partitioning, traffic profiles, DB/cache/broker limits, SSE measurements, IaC/deployment, costs and bottlenecks. V4 targets measured constraints only.

#### Exact scope

Update repo_context with topology, partitioning, traffic profiles, DB/cache/broker limits, SSE measurements, IaC/deployment, costs and bottlenecks. V4 targets measured constraints only.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Update repo_context with topology, partitioning, traffic profiles, DB/cache/broker limits, SSE measurements, IaC/deployment, costs and bottlenecks. V4 targets measured constraints only.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V3 release and scale evidence must be accepted before this audit starts.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Backend, Serviq: Data, Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V3 release and scale evidence must be accepted before this audit starts.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4002-write-v4-scale-prd-with-explicit-capacity-definitions"></a>

### V4.0.02 — Write V4 scale PRD with explicit capacity definitions

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT/BLOCKED — phase-gate planning ticket; start only after the predecessor release evidence named below exists.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Freeze separate targets for registered users, concurrent clients, active conversations, REST RPS, agent runs/s, provider calls/s, tool calls/s, retrieval QPS, event throughput, availability, RTO/RPO.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Freeze separate targets for registered users, concurrent clients, active conversations, REST RPS, agent runs/s, provider calls/s, tool calls/s, retrieval QPS, event throughput, availability, RTO/RPO.

#### Frozen source requirements

Freeze separate targets for registered users, concurrent clients, active conversations, REST RPS, agent runs/s, provider calls/s, tool calls/s, retrieval QPS, event throughput, availability, RTO/RPO. 10M staged target, not claim.

#### Exact scope

Freeze separate targets for registered users, concurrent clients, active conversations, REST RPS, agent runs/s, provider calls/s, tool calls/s, retrieval QPS, event throughput, availability, RTO/RPO. 10M staged target, not claim.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01 must be complete and its capacity evidence accepted.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: AI, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01 must be complete and its capacity evidence accepted.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4003-write-multi-region-architecture-and-data-placement-contracts"></a>

### V4.0.03 — Write multi-region architecture and data-placement contracts

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** DRAFT/BLOCKED — phase-gate planning ticket; start only after the predecessor release evidence named below exists.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Freeze region model, tenant home region, global/regional control plane, routing, write ownership, replication, failover, residency, global IDs, event routing, cache and unsupported cross-region operations.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Freeze region model, tenant home region, global/regional control plane, routing, write ownership, replication, failover, residency, global IDs, event routing, cache and unsupported cross-region operations.

#### Frozen source requirements

Freeze region model, tenant home region, global/regional control plane, routing, write ownership, replication, failover, residency, global IDs, event routing, cache and unsupported cross-region operations.

#### Exact scope

Freeze region model, tenant home region, global/regional control plane, routing, write ownership, replication, failover, residency, global IDs, event routing, cache and unsupported cross-region operations.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01 and V4.0.02 must be complete and accepted.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: AI, Serviq: Infrastructure, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01 and V4.0.02 must be complete and accepted.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4101-add-tenant-regionplacement-metadata-migration"></a>

### V4.1.01 — Add tenant region/placement metadata migration

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Persist home region, allowed regions, placement version, failover region, residency policy and provisioning state.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Persist home region, allowed regions, placement version, failover region, residency policy and provisioning state. Backfill existing tenants to current region.

#### Exact scope

Persist home region, allowed regions, placement version, failover region, residency policy and provisioning state. Backfill existing tenants to current region.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Persist home region, allowed regions, placement version, failover region, residency policy and provisioning state. Backfill existing tenants to current region.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Backend, Serviq: Data, Serviq: Infrastructure, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4102-implement-region-aware-tenant-placement-resolver"></a>

### V4.1.02 — Implement region-aware tenant placement resolver

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Trusted tenant -> allowed regional service/data endpoints from authoritative metadata.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Trusted tenant -> allowed regional service/data endpoints from authoritative metadata. Versioned cache. Unknown/stale placement fails closed or frozen redirect, never guesses.

#### Exact scope

Trusted tenant -> allowed regional service/data endpoints from authoritative metadata. Versioned cache. Unknown/stale placement fails closed or frozen redirect, never guesses.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Backend, Serviq: Infrastructure, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4103-add-signed-global-routing-token-contract"></a>

### V4.1.03 — Add signed global routing token contract

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Short-lived server-generated routing token: tenant, region, placement version, audience, expiry.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Short-lived server-generated routing token: tenant, region, placement version, audience, expiry. Edge/regional ingress validates; clients cannot choose region. Premium security review.

#### Exact scope

Short-lived server-generated routing token: tenant, region, placement version, audience, expiry. Edge/regional ingress validates; clients cannot choose region. Premium security review.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Security, Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v4104-implement-region-aware-api-ingress-middleware"></a>

### V4.1.04 — Implement region-aware API ingress middleware

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Global request -> trusted tenant placement -> local or approved redirect/proxy.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Global request -> trusted tenant placement -> local or approved redirect/proxy. Reject mismatch/stale token. Preserve request/trace/idempotency.

#### Exact scope

Global request -> trusted tenant placement -> local or approved redirect/proxy. Reject mismatch/stale token. Preserve request/trace/idempotency.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

proved redirect/proxy.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Backend, Serviq: Infrastructure, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4105-implement-region-aware-customer-sse-routing"></a>

### V4.1.05 — Implement region-aware customer SSE routing

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Route stream to home region, preserve reconnect/resume and process independence.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Route stream to home region, preserve reconnect/resume and process independence.

#### Frozen source requirements

Route stream to home region, preserve reconnect/resume and process independence. Test wrong region, reconnect, failover prep and tenant isolation.

#### Exact scope

Route stream to home region, preserve reconnect/resume and process independence. Test wrong region, reconnect, failover prep and tenant isolation.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `apps/customer-web/src/app/page.tsx` — current customer-web entry surface.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test wrong region, reconnect, failover prep and tenant isolation.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Security, Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4106-add-regional-service-discovery-configuration-to-iac"></a>

### V4.1.06 — Add regional service-discovery configuration to IaC

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Terraform outputs regional endpoints, DNS/discovery, health targets, certs and env config without hard-coded app region lists.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Terraform outputs regional endpoints, DNS/discovery, health targets, certs and env config without hard-coded app region lists.

#### Exact scope

Terraform outputs regional endpoints, DNS/discovery, health targets, certs and env config without hard-coded app region lists.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Backend, Serviq: Infrastructure, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4107-build-platform-placement-view"></a>

### V4.1.07 — Build platform placement view

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Platform console home/failover region, placement version/state, health and safe operator migration/failover actions.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Platform console home/failover region, placement version/state, health and safe operator migration/failover actions.

#### Frozen source requirements

Platform console home/failover region, placement version/state, health and safe operator migration/failover actions. Tenant admin cannot mutate region.

#### Exact scope

Platform console home/failover region, placement version/state, health and safe operator migration/failover actions. Tenant admin cannot mutate region.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `apps/platform-console/src/app/page.tsx` — separate platform-operator trust surface.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

Needs Architect Decision: freeze loading, empty, permission-denied, validation-error, dependency-error, success, and retry states with exact user-visible copy and accessibility behavior before builder handoff.

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Frontend, Serviq: Backend, Serviq: Data, Serviq: Infrastructure, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4201-freeze-conversationdatabase-shard-key-adr"></a>

### V4.2.01 — Freeze conversation/database shard key ADR

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Using V3 benchmark select physical shard strategy/routing key.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Using V3 benchmark select physical shard strategy/routing key.

#### Frozen source requirements

Using V3 benchmark select physical shard strategy/routing key. Document rebalancing, hotspots, global IDs and non-spanning query constraints.

#### Exact scope

Using V3 benchmark select physical shard strategy/routing key. Document rebalancing, hotspots, global IDs and non-spanning query constraints.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Using V3 benchmark select physical shard strategy/routing key. Document rebalancing, hotspots, global IDs and non-spanning query constraints.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Backend, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4202-implement-shard-routing-library-with-shadow-validation"></a>

### V4.2.02 — Implement shard routing library with shadow validation

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Repositories request shard handle.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Repositories request shard handle.

#### Frozen source requirements

Repositories request shard handle. Initially shadow-compute target and compare to current location, metric only, no customer move.

#### Exact scope

Repositories request shard handle. Initially shadow-compute target and compare to current location, metric only, no customer move.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Data, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4203-add-shard-placement-metadata-and-migration-tooling"></a>

### V4.2.03 — Add shard-placement metadata and migration tooling

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Persist shard assignment/range/hash-ring version plus resumable migration state/checksums.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Persist shard assignment/range/hash-ring version plus resumable migration state/checksums. No live move yet. Up/down/rollback tests.

#### Exact scope

Persist shard assignment/range/hash-ring version plus resumable migration state/checksums. No live move yet. Up/down/rollback tests.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Persist shard assignment/range/hash-ring version plus resumable migration state/checksums. No live move yet. Up/down/rollback tests.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Backend, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4204-implement-online-tenant-shard-migration-copy-phase"></a>

### V4.2.04 — Implement online tenant shard migration copy phase

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Copy tenant/partition in bounded batches with checkpoints/checksums while source authoritative.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Copy tenant/partition in bounded batches with checkpoints/checksums while source authoritative. No dual-write. Restart/throttle tests.

#### Exact scope

Copy tenant/partition in bounded batches with checkpoints/checksums while source authoritative. No dual-write. Restart/throttle tests.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Copy tenant/partition in bounded batches with checkpoints/checksums while source authoritative. No dual-write. Restart/throttle tests.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Backend, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v4205-implement-shard-migration-change-capturereplay-phase"></a>

### V4.2.05 — Implement shard migration change-capture/replay phase

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Capture/replay writes during copy via frozen outbox/CDC.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Capture/replay writes during copy via frozen outbox/CDC. Preserve aggregate ordering/idempotency. Duplicate/out-of-order tests.

#### Exact scope

Capture/replay writes during copy via frozen outbox/CDC. Preserve aggregate ordering/idempotency. Duplicate/out-of-order tests.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Capture/replay writes during copy via frozen outbox/CDC. Preserve aggregate ordering/idempotency. Duplicate/out-of-order tests.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Backend, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4206-implement-shard-cutover-and-rollback-switch"></a>

### V4.2.06 — Implement shard cutover and rollback switch

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

After consistency checks, atomically change placement version/authoritative shard, keep rollback window, invalidate caches, monitor.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

After consistency checks, atomically change placement version/authoritative shard, keep rollback window, invalidate caches, monitor. Premium database/security review.

#### Exact scope

After consistency checks, atomically change placement version/authoritative shard, keep rollback window, invalidate caches, monitor. Premium database/security review.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

After consistency checks, atomically change placement version/authoritative shard, keep rollback window, invalidate caches, monitor. Premium database/security review.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Backend, Serviq: Security, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4207-add-hot-tenant-workload-isolation"></a>

### V4.2.07 — Add hot-tenant workload isolation

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Route approved high-volume tenants to dedicated API/worker/retrieval pools without contract changes.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Route approved high-volume tenants to dedicated API/worker/retrieval pools without contract changes. Test hot tenant cannot starve standard.

#### Exact scope

Route approved high-volume tenants to dedicated API/worker/retrieval pools without contract changes. Test hot tenant cannot starve standard.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

proved high-volume tenants to dedicated API/worker/retrieval pools without contract changes. Test hot tenant cannot starve standard.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Security, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4208-partition-kafka-topicsconsumer-groups-for-hyperscale-load"></a>

### V4.2.08 — Partition Kafka topics/consumer groups for hyperscale load

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Measured partition counts/keys, consumer autoscaling, lag alarms and rebalance tuning.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Measured partition counts/keys, consumer autoscaling, lag alarms and rebalance tuning. Ordering/idempotency tests; document max tested throughput.

#### Exact scope

Measured partition counts/keys, consumer autoscaling, lag alarms and rebalance tuning. Ordering/idempotency tests; document max tested throughput.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests; document max tested throughput.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Backend, Serviq: Data, Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4209-add-distributed-sse-connection-registrymetrics"></a>

### V4.2.09 — Add distributed SSE connection registry/metrics

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Track active connections by region/tenant shard using rebuildable state/metrics only as needed.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Track active connections by region/tenant shard using rebuildable state/metrics only as needed.

#### Frozen source requirements

Track active connections by region/tenant shard using rebuildable state/metrics only as needed. Not authoritative conversation state. Node death cleanup/cardinality tests.

#### Exact scope

Track active connections by region/tenant shard using rebuildable state/metrics only as needed. Not authoritative conversation state. Node death cleanup/cardinality tests.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tests.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Data, Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4210-implement-edge-safe-deterministicsemantic-cache-strategy"></a>

### V4.2.10 — Implement edge-safe deterministic/semantic cache strategy

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Only explicitly cache-safe public/deterministic responses at edge/regional cache with tenant/agent/knowledge version keys.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Only explicitly cache-safe public/deterministic responses at edge/regional cache with tenant/agent/knowledge version keys. Private/tool responses not edge-cacheable without new contract.

#### Exact scope

Only explicitly cache-safe public/deterministic responses at edge/regional cache with tenant/agent/knowledge version keys. Private/tool responses not edge-cacheable without new contract.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Only explicitly cache-safe public/deterministic responses at edge/regional cache with tenant/agent/knowledge version keys. Private/tool responses not edge-cacheable without new contract.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: AI, Serviq: Infrastructure, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4301-define-and-test-regional-healthfailover-decision-contract"></a>

### V4.3.01 — Define and test regional health/failover decision contract

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Freeze health inputs, quorum/timeout, authority, auto/manual conditions, cooldown and split-brain prevention.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Freeze health inputs, quorum/timeout, authority, auto/manual conditions, cooldown and split-brain prevention. Simulation evaluator before traffic switch.

#### Exact scope

Freeze health inputs, quorum/timeout, authority, auto/manual conditions, cooldown and split-brain prevention. Simulation evaluator before traffic switch.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4302-implement-regional-control-plane-configuration-replication"></a>

### V4.3.02 — Implement regional control-plane configuration replication

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Replicate tenant/agent/provider metadata/policy/placement config per architecture.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Replicate tenant/agent/provider metadata/policy/placement config per architecture. Secrets follow approved design. Explicit version/conflict rules.

#### Exact scope

Replicate tenant/agent/provider metadata/policy/placement config per architecture. Secrets follow approved design. Explicit version/conflict rules.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

proved design.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: AI, Serviq: Security, Serviq: Infrastructure, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v4303-implement-regional-knowledgeindex-replication-workflow"></a>

### V4.3.03 — Implement regional knowledge/index replication workflow

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Replicate/rebuild object/document/chunk/index using versioned hashes.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Replicate/rebuild object/document/chunk/index using versioned hashes.

#### Frozen source requirements

Replicate/rebuild object/document/chunk/index using versioned hashes. Retrieval serves only complete ready regional version. Test stale/incomplete.

#### Exact scope

Replicate/rebuild object/document/chunk/index using versioned hashes. Retrieval serves only complete ready regional version. Test stale/incomplete.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Replicate/rebuild object/document/chunk/index using versioned hashes. Retrieval serves only complete ready regional version. Test stale/incomplete.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Test stale/incomplete.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: AI, Serviq: Data, Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4304-implement-regional-conversation-failover-policy"></a>

### V4.3.04 — Implement regional conversation failover policy

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

On home-region failure follow frozen supported mode: read-only degraded, new-conversation alternate, or full replicated failover.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

On home-region failure follow frozen supported mode: read-only degraded, new-conversation alternate, or full replicated failover.

#### Frozen source requirements

On home-region failure follow frozen supported mode: read-only degraded, new-conversation alternate, or full replicated failover. Never invent cross-region writes.

#### Exact scope

On home-region failure follow frozen supported mode: read-only degraded, new-conversation alternate, or full replicated failover. Never invent cross-region writes.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Infrastructure, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4305-implement-provider-routing-by-region"></a>

### V4.3.05 — Implement provider-routing by region

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Select provider/model respecting BYOK, regional availability/data policy, latency, quotas and fallback.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Select provider/model respecting BYOK, regional availability/data policy, latency, quotas and fallback. Regulated data never unapproved region.

#### Exact scope

Select provider/model respecting BYOK, regional availability/data policy, latency, quotas and fallback. Regulated data never unapproved region.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

proved region.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: AI, Serviq: Infrastructure, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4306-add-backuprestore-automation-and-deletion-replay-verification"></a>

### V4.3.06 — Add backup/restore automation and deletion replay verification

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Encrypted backups, isolated restore, replay privacy deletions before exposure, integrity checks, measure RTO/RPO, evidence/runbook.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Encrypted backups, isolated restore, replay privacy deletions before exposure, integrity checks, measure RTO/RPO, evidence/runbook.

#### Exact scope

Encrypted backups, isolated restore, replay privacy deletions before exposure, integrity checks, measure RTO/RPO, evidence/runbook.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Security, Serviq: Data, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4307-add-controlled-regional-failover-runbook-automation"></a>

### V4.3.07 — Add controlled regional failover runbook automation

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Operator workflow validates target health, freezes/redirects traffic, updates placement, monitors and rolls back.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Operator workflow validates target health, freezes/redirects traffic, updates placement, monitors and rolls back. Confirmation/audit. No unchecked destructive action.

#### Exact scope

Operator workflow validates target health, freezes/redirects traffic, updates placement, monitors and rolls back. Confirmation/audit. No unchecked destructive action.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4308-run-scheduled-disaster-recovery-game-day"></a>

### V4.3.08 — Run scheduled disaster-recovery game day

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Simulate region/service/DB/cache/broker failures in staging, execute runbook, measure actual RTO/RPO/consistency, create fix tasks from gaps.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Simulate region/service/DB/cache/broker failures in staging, execute runbook, measure actual RTO/RPO/consistency, create fix tasks from gaps.

#### Exact scope

Simulate region/service/DB/cache/broker failures in staging, execute runbook, measure actual RTO/RPO/consistency, create fix tasks from gaps.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Backend, Serviq: Infrastructure, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4401-create-distributed-load-generator-architecture"></a>

### V4.4.01 — Create distributed load-generator architecture

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Multiple k6/load-generator nodes so generator not bottleneck.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Multiple k6/load-generator nodes so generator not bottleneck. Aggregate synchronized results and prove generator CPU/network headroom. Record topology.

#### Exact scope

Multiple k6/load-generator nodes so generator not bottleneck. Aggregate synchronized results and prove generator CPU/network headroom. Record topology.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

prove generator CPU/network headroom.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Infrastructure, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4402-add-staged-sse-concurrency-test-100k-connections"></a>

### V4.4.02 — Add staged SSE concurrency test: 100k connections

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Fake-provider/minimal-message workload at 100k concurrent connected clients.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Fake-provider/minimal-message workload at 100k concurrent connected clients.

#### Frozen source requirements

Fake-provider/minimal-message workload at 100k concurrent connected clients. Record success, memory, network, p95 delivery, reconnect, error and cost.

#### Exact scope

Fake-provider/minimal-message workload at 100k concurrent connected clients. Record success, memory, network, p95 delivery, reconnect, error and cost.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `apps/client-console/src/app/page.tsx` — current tenant/client console entry surface.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v4403-add-staged-sse-concurrency-test-500k-connections"></a>

### V4.4.03 — Add staged SSE concurrency test: 500k connections

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Only after 100k passes and fixes.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Only after 100k passes and fixes. Same evidence format; record infrastructure changes and cost.

#### Exact scope

Only after 100k passes and fixes. Same evidence format; record infrastructure changes and cost.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4404-add-staged-sse-concurrency-test-1m-connections"></a>

### V4.4.04 — Add staged SSE concurrency test: 1M connections

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Only after 500k passes.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Only after 500k passes. Do not change workload invisibly. Publish failure if unmet.

#### Exact scope

Only after 500k passes. Do not change workload invisibly. Publish failure if unmet.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4405-add-staged-concurrency-tests-beyond-1m-toward-10m"></a>

### V4.4.05 — Add staged concurrency tests beyond 1M toward 10M

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Create procedure/template for later separate 2M, 5M, 10M steps only after evidence and explicit spend approval.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Create procedure/template for later separate 2M, 5M, 10M steps only after evidence and explicit spend approval. This ticket creates procedure, not false result. Human Needed.

#### Exact scope

Create procedure/template for later separate 2M, 5M, 10M steps only after evidence and explicit spend approval. This ticket creates procedure, not false result. Human Needed.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4406-add-restapi-high-throughput-cached-path-benchmark"></a>

### V4.4.06 — Add REST/API high-throughput cached-path benchmark

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Measure stateless cached/deterministic RPS separately from SSE/AI.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Measure stateless cached/deterministic RPS separately from SSE/AI. Increase to first bottleneck; p50/p95/p99/errors/cache hit/CPU/network/DB avoidance. Never call AI RPS.

#### Exact scope

Measure stateless cached/deterministic RPS separately from SSE/AI. Increase to first bottleneck; p50/p95/p99/errors/cache hit/CPU/network/DB avoidance. Never call AI RPS.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Backend, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4407-add-agent-worker-throughput-benchmark-with-fake-providers"></a>

### V4.4.07 — Add agent-worker throughput benchmark with fake providers

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Measure agent runs/s through queues, retrieval mocks/real local index, policy/tool mocks and fake model.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Measure agent runs/s through queues, retrieval mocks/real local index, policy/tool mocks and fake model. Record queue lag/worker saturation.

#### Exact scope

Measure agent runs/s through queues, retrieval mocks/real local index, policy/tool mocks and fake model. Record queue lag/worker saturation.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Measure agent runs/s through queues, retrieval mocks/real local index, policy/tool mocks and fake model. Record queue lag/worker saturation.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Backend, Serviq: AI, Serviq: Data, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4408-add-retrieval-hyperscale-benchmark"></a>

### V4.4.08 — Add retrieval hyperscale benchmark

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Measure lexical/vector/hybrid QPS across realistic corpus sizes/shards, topK, p95/p99, cache hit, CPU/memory plus quality checks.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Measure lexical/vector/hybrid QPS across realistic corpus sizes/shards, topK, p95/p99, cache hit, CPU/memory plus quality checks.

#### Exact scope

Measure lexical/vector/hybrid QPS across realistic corpus sizes/shards, topK, p95/p99, cache hit, CPU/memory plus quality checks.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: AI, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4409-add-chaos-test-for-apiworker-node-loss-under-load"></a>

### V4.4.09 — Add chaos test for API/worker node loss under load

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Randomly terminate bounded stateless replicas under staging load.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Randomly terminate bounded stateless replicas under staging load. Verify autoscaling/retry/idempotency, no duplicate protected mutation and acceptable recovery.

#### Exact scope

Randomly terminate bounded stateless replicas under staging load. Verify autoscaling/retry/idempotency, no duplicate protected mutation and acceptable recovery.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Verify autoscaling/retry/idempotency, no duplicate protected mutation and acceptable recovery.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Backend, Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4410-add-chaos-test-for-cachebroker-degradation"></a>

### V4.4.10 — Add chaos test for cache/broker degradation

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Inject cache loss and broker lag/partition/rebalance.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Inject cache loss and broker lag/partition/rebalance. Verify authoritative fallback, backpressure, DLQ/idempotency and no unlimited flood.

#### Exact scope

Inject cache loss and broker lag/partition/rebalance. Verify authoritative fallback, backpressure, DLQ/idempotency and no unlimited flood.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Inject cache loss and broker lag/partition/rebalance. Verify authoritative fallback, backpressure, DLQ/idempotency and no unlimited flood.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Verify authoritative fallback, backpressure, DLQ/idempotency and no unlimited flood.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Data, Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---

<a id="v4411-add-chaos-test-for-database-shard-impairment"></a>

### V4.4.11 — Add chaos test for database shard impairment

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Simulate one shard slow/unavailable.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Simulate one shard slow/unavailable. Verify bulkheads, tenant-scoped blast radius, safe errors/escalation and no wrong-shard routing. Premium database/security review.

#### Exact scope

Simulate one shard slow/unavailable. Verify bulkheads, tenant-scoped blast radius, safe errors/escalation and no wrong-shard routing. Premium database/security review.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

Simulate one shard slow/unavailable. Verify bulkheads, tenant-scoped blast radius, safe errors/escalation and no wrong-shard routing. Premium database/security review.

Needs Architect Decision: paste exact table/column/type/nullability/default/constraint/index names and the exact migration filename before builder handoff.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Verify bulkheads, tenant-scoped blast radius, safe errors/escalation and no wrong-shard routing.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Backend, Serviq: Security, Serviq: Data, Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4412-add-cost-per-million-requestsconnections-model"></a>

### V4.4.12 — Add cost-per-million-requests/connections model

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Using measured bills/metrics calculate cost by workload: cached REST, SSE connected-hour, retrieval, fake-agent infra and real provider AI separately.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Using measured bills/metrics calculate cost by workload: cached REST, SSE connected-hour, retrieval, fake-agent infra and real provider AI separately. Document assumptions/sensitivity.

#### Exact scope

Using measured bills/metrics calculate cost by workload: cached REST, SSE connected-hour, retrieval, fake-agent infra and real provider AI separately. Document assumptions/sensitivity.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/worker/app/main.py` — active worker composition root; trace the relevant handler before freezing edit paths.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: AI, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4413-publish-v4-multi-regionhyperscale-evidence-report"></a>

### V4.4.13 — Publish V4 multi-region/hyperscale evidence report

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Every tested stage, commit, regions, services, instance classes, shard/partition counts, generator topology, dataset, duration, p50/p95/p99, errors, failover, RTO/RPO and cost.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

None directly (internal capability or delivery evidence).

#### Frozen source requirements

Every tested stage, commit, regions, services, instance classes, shard/partition counts, generator topology, dataset, duration, p50/p95/p99, errors, failover, RTO/RPO and cost. State highest actually demonstrated capacity only.

#### Exact scope

Every tested stage, commit, regions, services, instance classes, shard/partition counts, generator topology, dataset, duration, p50/p95/p99, errors, failover, RTO/RPO and cost. State highest actually demonstrated capacity only.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

Needs Architect Decision: freeze the method, path, authentication/authorization rule, request shape, response/error shapes, idempotency behavior, and rate limits. Do not infer them from the title.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

tested stage, commit, regions, services, instance classes, shard/partition counts, generator topology, dataset, duration, p50/p95/p99, errors, failover, RTO/RPO and cost.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: Data, Serviq: Infrastructure, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
<a id="v4414-run-independent-releasesecurity-architecture-review"></a>

### V4.4.14 — Run independent release/security architecture review

> **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
> **Linear status:** Backlog candidate
> **Ticket intake:** BLOCKED — downstream implementation must wait for this phase's audit, PRD delta, and architecture/contract delta.
> **Source:** `Serviq_Remaining_Linear_Tickets_V1.3.05_to_V4.md` captured 2026-08-13.

#### Goal

Premium Product Builder review + threat model + infrastructure/data-consistency review across routing, secrets, region placement, sharding, failover, privacy, idempotency and load evidence.

#### Why this exists

This item closes one bounded part of the remaining V4 roadmap. It is intentionally isolated so implementation, review, rollback, and acceptance evidence can be evaluated without silently absorbing adjacent roadmap work.

#### Estimated effort

Needs re-estimation during architect intake. The source backlog's general 1–3 hour target is not repeated as a commitment; split the ticket if the accepted scope cannot fit one focused session and roughly five edited files.

#### Read before coding

1. `docs/repo_context.md`.
2. `docs/PRD.md`.
3. `docs/ARCHITECTURE.md`.
4. Every ADR/CCR and predecessor contract explicitly named in the frozen source requirements below.
5. The repository's backend, frontend, database, API, testing, security, execution, and product-quality rules that apply to the accepted scope.

If current code differs from a frozen contract, stop with `Needs Architect Decision`.

#### User-facing behavior

Premium Product Builder review + threat model + infrastructure/data-consistency review across routing, secrets, region placement, sharding, failover, privacy, idempotency and load evidence.

#### Frozen source requirements

Premium Product Builder review + threat model + infrastructure/data-consistency review across routing, secrets, region placement, sharding, failover, privacy, idempotency and load evidence. Critical/High findings become micro-fix issues and block V4 claim/release.

#### Exact scope

Premium Product Builder review + threat model + infrastructure/data-consistency review across routing, secrets, region placement, sharding, failover, privacy, idempotency and load evidence. Critical/High findings become micro-fix issues and block V4 claim/release.

Before builder handoff, this section must be converted into an exhaustive list of exact behaviors and bounded outputs. The frozen source requirements above may be clarified but not weakened.

#### Out of scope

- Any other numbered roadmap ticket.
- Unnamed API, database, event, provider, permission, security, deployment, or UX contract changes.
- A new service, framework, dependency, or abstraction unless the accepted architecture explicitly requires it.
- Production vendor access, real customer data, real payment movement, or public launch unless this ticket explicitly owns that evidence.

#### Files to inspect first

- `docs/repo_context.md` — current implemented boundaries, conventions, and evidence gaps.
- `docs/PRD.md` — frozen product intent and trust-surface rules.
- `docs/ARCHITECTURE.md` — current contracts, sequencing, and system boundaries.
- `services/api/alembic/versions/20260819_0009_knowledge_permissions.py` — current Alembic revision style and downgrade expectations.
- `services/api/app/main.py` — active API composition root; use it to locate the owning module through the code graph.
- `services/llm-gateway/app/main.py` — active gateway composition root and internal boundary.
- `infra/docker/compose.yml` — current local infrastructure contract and profiles.
- `.github/workflows/ci.yml` and `.github/workflows/security.yml` — current quality and security gates.

Use the codebase knowledge graph to trace active callers/consumers from these roots. Do not treat similarly named dead code or design documents as runtime evidence.

#### Files to create or edit

Needs Architect Decision: freeze the complete, exact allowlist after repository/graph inspection. A builder must not start with guessed paths, wildcard directories, or an open-ended permission to refactor.

#### Data model

None expected. Stop if implementation would require a schema, migration, cache-key, or persistence-contract change not explicitly frozen in this ticket.

#### API contract

None directly, unless the accepted architecture delta assigns this ticket an API surface. If it does, paste the complete contract here before builder handoff.

#### Event, job, cache, and integration contracts

Preserve every event name, contract identifier, idempotency key, ordering rule, consistency boundary, and dependency named in the frozen source requirements. For every missing payload/schema/error/retry detail: `Needs Architect Decision`; do not invent it.

#### UI states

None (internal/non-UI ticket).

#### Validation rules

Apply exact numeric and field-level limits from the frozen source requirements. Any missing size, timeout, retry, concurrency, pagination, rate, retention, or copy limit is a blocking architect decision—not a builder default.

#### Error handling

- Fail closed for authorization, tenant scope, policy, placement, and contract-validation failures.
- Keep retryable and terminal failures distinguishable with bounded safe codes.
- Do not acknowledge durable work before its required state/event/object transaction boundary is satisfied.
- Preserve a recoverable user/operator-visible state for partial failure; do not suppress cleanup, rollback, or reconciliation failures.

#### Auth and permissions

Use the existing trust-surface-specific authentication boundary and server-resolved tenant/actor context. Paste the exact guard/dependency and required permission into this ticket before builder handoff. Customer, workforce, and platform-operator authority are never interchangeable.

#### Dependencies

V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

Every dependency must be merged and acceptance-verified. A written ticket, design document, or Linear status alone is not implementation evidence.

#### Do not change

- Existing public contracts outside this ticket.
- Historical migrations already applied; add a new reversible migration when required.
- Provider credential write-only behavior and current trust-surface separation.
- Demo non-affiliation, synthetic-data, and no-real-money constraints.
- Unrelated user changes in the working tree.

#### Step-by-step implementation

1. Run ticket intake against current code and dependency evidence.
2. Resolve every `Needs Architect Decision`; paste exact contracts and an exact file allowlist into this ticket.
3. Re-run intake; stop unless every item passes.
4. Add the smallest failing automated tests that prove the frozen happy path, boundary failures, tenant isolation, and replay/failure behavior.
5. Implement only the exact scope using existing repository patterns and dependencies.
6. Run focused tests, then the affected service's lint/type-check/full suite.
7. Run the required real integration, browser, load, security, migration, or recovery evidence for this ticket; record anything environment-blocked as not verified.
8. Perform a Staff Engineer review for security/privacy, idempotency, migrations, observability, rollback, deployment, and real integration evidence.
9. Update source-of-truth documentation and attach evidence without changing contracts silently.

#### Required automated tests

Needs Architect Decision: enumerate concrete happy-path, validation, authorization, tenant-isolation, retry/idempotency, and failure-path cases after contracts and edit paths are frozen.

Also include authorization/permission failure, cross-tenant isolation, malformed input/contract, dependency failure, and replay/idempotency cases wherever those boundaries exist. Name exact test files during intake.

#### Security and privacy requirements

- Preserve server-owned tenant and actor context; never trust organization, customer, region, or ownership identifiers supplied by an untrusted caller.
- Do not log or return secrets, raw tokens, unrestricted PII, raw prompts, raw knowledge content, tool payloads, or chain-of-thought.
- Deny cross-tenant and cross-trust-surface access in the data query/operation itself, not only in UI filtering.
- Use stable operation identities and prove replay/idempotency behavior before enabling retries.
- Use the approved secret/crypto boundary; credentials remain write-only and must not be persisted or serialized in plaintext.
- Define derived-data, backup, replica, cache, and audit handling so deletion/retention and placement claims are end-to-end.


#### Observability and operations

Emit bounded identifiers, outcome codes, retry counts, timings, and correlation IDs only. Define alert/metric ownership, cardinality bounds, rollback/recovery, and deployment evidence when this ticket changes production behavior. Logs and traces are not a secondary data store.

#### Manual QA

1. Exercise the accepted happy path through the real boundary named by this ticket.
2. Exercise the highest-risk authorization/tenant/security/failure path.
3. Verify user/operator-visible state and observability contain no prohibited data.
4. Verify rollback, retry, reconnect, replay, or recovery behavior if applicable.
5. Record environment, command/request, expected result, actual result, and evidence link.

Needs Architect Decision: replace these categories with exact commands or click paths and expected output before builder handoff.

#### Acceptance criteria

- [ ] Every frozen source requirement is implemented and demonstrated by the named automated and manual evidence.
- [ ] Every frozen source requirement above is implemented or explicitly removed through architect-approved contract change control.
- [ ] Exact file paths, contracts, limits, permissions, and failure behavior are frozen before coding.
- [ ] Named automated tests pass, including tenant/security and replay/failure boundaries where applicable.
- [ ] Required real integration/UI/load/security/migration/recovery evidence is attached; skipped checks remain explicit blockers.
- [ ] No Critical or High Staff Engineer review finding remains open.
- [ ] Linear status reflects merged and acceptance-verified evidence, not file presence or a green narrow unit test.

#### Stop conditions

Stop and return `Needs Architect Decision: [specific decision]` if:

- a route, payload, schema, permission, event, cache key, provider behavior, UX state, numeric limit, data-placement rule, or file allowlist is missing;
- the repository differs from the frozen source requirement;
- a dependency is not merged and acceptance-verified;
- the ticket requires more than one focused session or crosses frontend/backend/service boundaries without an already frozen contract;
- credentials, vendor/cloud spend, legal/privacy judgment, production data, or destructive migration/recovery work lacks explicit human authorization;
- implementation would weaken tenant isolation, security, privacy, idempotency, rollback, observability, or release evidence.

#### Definition of done

- [ ] Ticket intake has zero FAIL items.
- [ ] Scope and out-of-scope boundaries are preserved.
- [ ] Code, migrations, tests, documentation, and contract records are complete.
- [ ] Focused and affected full quality checks pass.
- [ ] Real integration/deployment/operational evidence required by this ticket is recorded.
- [ ] Rollback/recovery is documented and demonstrated in proportion to risk.
- [ ] Staff Engineer review is resolved.
- [ ] PR contains only this ticket and is merged.
- [ ] Linear status and dependencies are reconciled after acceptance.

#### Suggested Linear metadata

- **Milestone:** Phase 4 — V4 Hyperscale & Multi-Region
- **Labels:** Serviq: AI, Serviq: Security, Serviq: Data, Serviq: Infrastructure, Serviq: Testing, Human Needed
- **Priority:** Needs Architect Decision during tranche planning
- **Estimate:** Re-estimate after intake; split if not one focused session
- **Initial state:** Backlog
- **blockedBy:** V4.0.01, V4.0.02, and V4.0.03 must be complete; the exact V4 placement, consistency, failover, and capacity contracts must be pasted into this ticket before implementation.

#### Ticket intake result

**FAIL — do not hand to a builder yet.** This conversion supplies the full Serviq/Linear issue structure and preserves the source requirements, but the compact roadmap does not consistently freeze exact edit paths, complete contracts, numeric limits, dependency evidence, or manual QA. Resolve every marked decision and re-run the intake checklist before changing this status to builder-ready.

---
