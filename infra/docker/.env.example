# Local Docker development defaults only. Copy to infra/docker/.env if overrides are needed.
# Do not reuse these values in staging or production.
POSTGRES_DB=serviq
POSTGRES_USER=serviq
POSTGRES_PASSWORD=serviq-local-dev-only
POSTGRES_PORT=5432
